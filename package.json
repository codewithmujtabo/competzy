{
  "name": "competzy",
  "private": true,
  "scripts": {
    "backend": "cd backend && pnpm dev",
    "web":     "cd web && pnpm dev",
    "app":     "cd app && pnpm exec expo start",
    "install:all": "pnpm -C backend install && pnpm -C web install && pnpm -C app install"
  }
}
