# Hoist all @types/* packages to the top-level node_modules so TypeScript
# can resolve transitive type references through pnpm's strict symlink
# layout (same reason backend/.npmrc does this — prevents TS2742 errors).
public-hoist-pattern[]=@types/*
