# Competzy — Claude Project Brief

Indonesia's unified K-12 academic competition platform. Students, parents, teachers, and organizers all in one place. Replaces fragmented per-competition websites (EMC, ISPO, OSEBI, Komodo, Owlypia, etc.).

**GitHub:** https://github.com/codewithmujtabo/competzy  
**Phase 1 deadline:** July 10, 2026  
**Owner split:** Mujtabo → mobile app (`app/`) + backend (`backend/`). Teammate → web portals (`web/`).

---

## Monorepo Structure

```
competzy/
├── app/          React Native (Expo) — student, parent, teacher (light)
├── web/          Next.js 16 App Router — admin, organizer, school, rep, referral portals
├── backend/      Express.js 5 + PostgreSQL — shared API for both app and web (port 3000)
├── docs/         PROJECT_PLAN.md (task board) + PROJECT_PLAN.docx
└── package.json  Root convenience scripts
```

**There is no separate backend for the web. Both app and web call the same Express backend on port 3000.**

---

## How to Run

> **Package manager: pnpm** (v11+). Migrated from npm in SPRINT 38 — see
> "Known Issues / Quirks" below for the pnpm-specific gotchas (the
> `pnpm-workspace.yaml` per subproject holding `allowBuilds` whitelist
> for native modules like bcrypt/sharp, plus the `@types/*` hoist in
> `.npmrc`). Install pnpm globally: `npm install -g pnpm` or via
> Homebrew `brew install pnpm`. Node ≥ 20 ships corepack so you can
> also `corepack enable && corepack prepare pnpm@11.2.2 --activate`.

```bash
# From each subdirectory:
cd backend && pnpm dev           # Express backend — port 3000 (3010 in local dev)
cd web     && pnpm dev           # Next.js web — port 3001
cd app     && pnpm start         # Expo mobile — opens Expo dev tools

# Or from the monorepo root:
pnpm backend
pnpm web
pnpm app

# Install all deps at once:
pnpm install:all
```

**Web `.env.local`** (`web/.env.local`):
```
BACKEND_URL=http://localhost:3000
```

**App `.env.local`** (`app/.env.local`):
```
EXPO_PUBLIC_API_URL=http://<MAC_LAN_IP>:3000/api
```
> The Mac's LAN IP changes when reconnecting to WiFi. Run `ipconfig getifaddr en0` to get the current IP and update this file. For iOS Simulator only, `http://localhost:3000/api` works.

**Backend `.env`** (`backend/.env`) — not committed. Copy from `backend/.env.example`.

---

## Deployment

Production runs on **`<PROD_HOST>`** using a **host-nginx + Coolify hybrid** topology — Coolify (v4) supervises the four service containers, **host nginx 1.24 + certbot** handles every public domain and SSL termination. Coolify on this server is **not** running Traefik; each app binds directly to a host port and nginx reverse-proxies into it. The same server already hosts the `competzy.com` landing page (port 3000, separate repo `eduversal-team/competzy-web`) and an unrelated `pathvance.com` tenant (ports 3001/4000) — port choices below avoid those.

| Service        | Coolify resource     | Host port | Internal port | Public domain                       |
| -------------- | -------------------- | --------- | ------------- | ----------------------------------- |
| PostgreSQL 16  | Database             | —         | `5432`        | — (internal docker network only)    |
| MinIO          | Service              | `9000`/`9001` | `9000`/`9001` | `https://minio.arena.competzy.com` |
| Backend (API)  | Application (Docker) | `3010`    | `3000`        | `https://api.competzy.com`          |
| Web (Next.js)  | Application (Docker) | `3011`    | `3001`        | `https://arena.competzy.com`        |

**Domain split:**
- `competzy.com` — landing page, **separate repo** `eduversal-team/competzy-web` (already deployed on this server at port 3000); this repo never touches it.
- `arena.competzy.com` — the portal in this repo (`web/`); login + manage competitions, registrations, exams.
- `api.competzy.com` — backend API (this repo's `backend/`).
- `minio.arena.competzy.com` — public S3 endpoint for presigned URLs that the backend hands to browsers.

`app/` (Expo React Native) does **not** deploy here — mobile binaries are built via EAS Build (`eas build --profile production`) from a developer machine. The Expo "web export" mode is not used in production.

**SSL** is provisioned with `certbot --nginx -d api.competzy.com -d arena.competzy.com -d minio.arena.competzy.com` (one shot for the trio), auto-renewed by `certbot.timer`. The nginx server-block template lives at `deploy/nginx.conf`.

**Coolify "Domains" field stays empty on every service** in this stack — host nginx is the public face. Each service's "Exposed Ports" controls the host port nginx proxies into.

See `deploy/COOLIFY.md` for the full step-by-step (provision DB + MinIO, env vars, migrations, nginx + certbot wiring, troubleshooting).

---

## Tech Stack

| Layer | Technology |
|---|---|
| Mobile | React Native + Expo SDK 52 + TypeScript + Expo Router (file-based) |
| Web | Next.js 16 App Router + React 19 + TypeScript + Tailwind v4 + shadcn/ui + recharts (EMC Wave 2) |
| Backend | Express.js 5 + TypeScript + node-pg (raw SQL, no ORM) |
| Database | PostgreSQL — self-hosted (Coolify Postgres service) |
| Auth | JWT (7-day) + Email OTP + Phone OTP via Twilio |
| Payments | Midtrans Snap (GoPay, OVO, Dana, Bank VA, QRIS) |
| File storage | MinIO (S3-compatible) as a Coolify service; backend falls back to local disk `/app/uploads` (Coolify persistent volume) when MinIO env vars are absent |
| Push notifications | Expo Push Service |
| Email | Nodemailer (SMTP / Gmail) |
| Regions data | Static JSON from emsifa.com (called directly from app, not proxied) |
| School search | api.co.id (requires `API_CO_ID_KEY`) — falls back to DB when key not set |

---

## Rules — Always Follow

1. **Never propose Supabase, Neon, RDS, PlanetScale, or any managed DBaaS.** Self-hosted PostgreSQL only — provisioned as a Coolify Database service in production.
2. **Never use an ORM.** Raw SQL with `node-pg` (`pool.query()`). Migrations are plain `.sql` files in `backend/migrations/`.
3. **All web pages must have `'use client'`** if they use React state, hooks, or browser APIs. Next.js App Router defaults to Server Components.
4. **Web calls backend via Next.js rewrite** (`/api/*` → `http://localhost:3000/api/*`). Never hardcode backend URLs in web code — always use relative `/api/...` paths.
5. **Backend routes follow REST conventions.** Admin routes are under `/api/admin/`. Organizer routes under `/api/organizers/`. Never expose admin endpoints without `adminOnly` middleware.
6. **No TypeScript `any` in new code** unless there is truly no alternative (e.g. third-party payloads).
7. **Phone numbers are always normalized to E.164 (`+62...`)** before storing. Use `toE164()` in `twilio.service.ts`.
8. **Dev OTP bypass:** When `TWILIO_VERIFY_SID` is not set, phone OTP is bypassed and the code `000000` always works. Email OTP uses a real SMTP server even in dev.

---

## Important Codebase Context

### Authentication
- **Web (since Sprint 14):** JWT issued in an `httpOnly + SameSite=Lax + Secure-in-prod` cookie named `competzy_token`. The cookie is set automatically by every login endpoint (email/password, email-OTP, phone-OTP) via the `issueAuthCookie()` helper in `auth.routes.ts`. localStorage is no longer used. `POST /api/auth/logout` clears the cookie. `web/lib/auth/{context,organizer-context,school-context}.tsx` hydrate via `GET /api/auth/me` on mount; the same shared cookie powers all three portals.
- **Single-session implication:** because all three web portals share one cookie, a browser cannot be admin and organizer simultaneously. Log out to switch roles.
- **Mobile:** JWT stored in `SecureStore` and sent as `Authorization: Bearer …`. The auth middleware reads the Bearer header first, then falls back to the cookie — so mobile is unaffected by the web change.
- **CORS:** backend now opts in to `credentials: true` with an explicit origin allowlist (`CORS_ORIGINS` env, defaults to `http://localhost:3000,http://localhost:3001`). Empty Origin (server-to-server, mobile) is allowed.
- **Soft-deleted users are 401-rejected** by the auth middleware, so deleting an account immediately invalidates all live sessions.
- Role-based: `student`, `parent`, `teacher`, `school_admin`, `admin`, `organizer`, `country_representative` — **7 roles** (`country_representative` added 2026-05-18, Komodo Wave 2 / SPRINT 33). (`question_maker` + `supervisor` were added 2026-05-15 as an EMC-port role foundation, then both removed — `question_maker` in Wave 6 (`1749300000000`), `supervisor` in Wave 8 (`1749400000000`); the question bank / exams / venues / proctoring are all managed by admin + organizer, no dedicated roles.) `users.role` is a plain TEXT column with a `users_role_check` CHECK constraint (no Postgres ENUM, no TS union type).
- `GET /api/auth/me` is the user hydration endpoint on app startup. Now returns `kid` (KX-2026-NNNNNNN) and, for `school_admin`, `schoolVerificationStatus` + `schoolRejectionReason`.
- **Password reset (Sprint 20 Phase B):** `POST /api/auth/forgot-password` accepts `{email}`, always returns 200 (no enumeration), and emails a `/reset-password?token=…` link when the email matches a live user. Tokens live in `password_reset_tokens` (migration `1748050000000`), SHA-256 hashed at rest, single-use, 15-min TTL. `POST /api/auth/reset-password` accepts `{token, password}`, requires ≥ 8 chars, marks the token used + invalidates any other outstanding tokens for the same user. Both endpoints rate-limited (5/15min per IP+identifier).
- **Phone OTP on web (Sprint 20 Phase B):** the `/` login page now has an Email ↔ Phone mode toggle. Phone path reuses the existing `POST /api/auth/phone/{send,verify}-otp` endpoints; same cookie issuance flow. Dev OTP bypass still applies (`000000` when `TWILIO_VERIFY_SID` is unset).
- **Super-admin impersonation (2026-05-19):** the configured super-admin (`SUPER_ADMIN_EMAIL` env, default `admin@eduversal.com`) can sign in as any user. `POST /api/auth/impersonate/:userId` issues the target a **short-lived (1h)** JWT carrying an `imp` claim — the super-admin's id; `POST /api/auth/stop-impersonation` re-issues a normal token for the original admin. `generateToken(userId, {impersonatorId})` embeds `imp`; the auth middleware sets `req.impersonatorId` from it. `GET /api/auth/me` returns `isSuperAdmin` + `impersonating` + `impersonatedBy`. Self-impersonation and nested impersonation are rejected; both transitions are written to `audit_log` (`admin.user.impersonate` / `admin.user.stop_impersonation`). Web: an **Impersonate** action on the admin `/users` page (shown only to the super-admin) + a sticky `ImpersonationBanner` (root layout, self-fetches `/auth/me`) with a one-click "Stop impersonating". `destinationFor()` lives in `web/lib/auth/role-destination.ts` (shared with the login page). Web-only — admins don't use the mobile app.

### Database
- Schema is in `backend/src/db/schema.sql`.
- Tables: `users`, `students`, `parents`, `teachers`, `competitions`, `competition_rounds`, `registrations`, `payments`, `documents`, `notifications`, `otp_codes`, `invitations`, `parent_student_links`, `teacher_student_links`, `bulk_registration_jobs`, `favorites`, `schools`, `historical_participants`, `school_payment_batches`, `school_payment_batch_items`, `organizers`, **`audit_log` (Sprint 14)**, **`payment_webhook_events` (Sprint 14)**, **`password_reset_tokens` (Sprint 20 Phase B)**. **EMC port (Sprint 20 Phase D, 31 tables):** `subjects`, `topics`, `subtopics`, `questions`, `answers`, `question_topics`, `proofreads`, `areas`, `test_centers`, `area_user`, `test_center_user`, `exams`, `exam_question`, `sessions`, `periods`, `answer_keys`, `paper_exams`, `paper_answers`, `webcams`, `voucher_groups`, `vouchers`, `products`, `orders`, `order_items`, `referrals`, `clicks`, `announcements`, `materials`, `suggestions`, `settings`, `accesses`. **Komodo Wave 2 (SPRINT 33):** `country_representatives`, `rep_payment_batches`.
- Migrations live in `backend/migrations/` as timestamped `.sql` files. Run with `npm run db:migrate`.
- `DATABASE_URL` format: `postgresql://user:password@localhost:5432/competzy`. Local DB has been renamed to `competzy` ✅. **VPS still needs**: `ALTER DATABASE kompetix RENAME TO competzy;` (or `beyond_classroom RENAME TO competzy;` if VPS skipped the May 6 rename) + update `DATABASE_URL` in VPS `backend/.env`.
- **Latest migration:** `1751500000000_normalize-empty-nisn.sql` (SPRINT 37 — flips every `students.nisn = ''` to NULL so the partial-unique index `idx_students_nisn WHERE nisn IS NOT NULL` doesn't trip on the next profile save; code now coerces `'' → null` in `PUT /api/users/me`. Idempotent). Prior: `1751400000000_komodo-mark-international.sql` (forcibly UPDATEs Komodo's `is_international = true` — production seed-komodo ran the OLD compiled JS before the flag was added, so existing Komodo row stayed NULL and the international-student catalog filter dropped it). `1751300000000_komodo-finalise.sql` (PR #47 — `competition_rounds.description`, `payments.provider + currency` for future Stripe/Xendit, `voucher_groups.country + vouchers.country` for per-country Komodo vouchers, drop city/province/supervisor from Komodo required fields). `1751200000000_qb-multilang-and-tags.sql` (question-bank multi-language `content4/5/6` + `questions.tags JSONB`). `1751100000000_re-add-question-maker-role.sql`. `1751000000000_round-international-fee.sql` (SPRINT 36 — USD round pricing). `1750900000000_round-age-cutoff.sql` (Komodo creature age cutoff per round). `1750800000000_student-country-and-required-fields.sql` (`users.country` ISO-2 + `competitions.required_profile_fields`). `1750700000000_normalize-user-phones.sql` (SPRINT 34 — back-fills every `users.phone` to the canonical local `0`-prefixed format; signup + `PUT /users/me` normalise on write via `toLocalPhone()`). Prior: `1750600000000_round-activation.sql` (multi-round QA follow-up — adds `competition_rounds.is_active`, a per-round operator visibility toggle; backfills existing `fast_track` / `global` rounds to inactive). `1750500000000_rep-payment-batches.sql` (Komodo Wave 2 — country-rep bulk payment). **SPRINT 33 multi-round + Komodo batch** (all applied locally, run on VPS): `1750000000000_grades-numeric` (SD/SMP/SMA → numeric grades 1–12), `1750100000000_competition-rounds-gating` (round gating + `exams.round_id`), `1750200000000_competition-logo`, `1750300000000_registrations-round-id` (per-round registration), `1750400000000_komodo-round-model` (round categories + qualifying score + the `country_representative` role + `country_representatives` table), `1750500000000_rep-payment-batches`. Earlier: `1749900000000_certificates.sql` (Wave 12 Phase 1 — the `certificates` table + `certificate_number_seq`: a verifiable Certificate of Participation/Achievement, auto-issued when a student finishes a competition exam; T1 strict `comp_id`, soft-delete, partial-unique `(comp_id,user_id)` live index). Earlier: `1749800000000_referral-commission-rate.sql` (Wave 10 Phase 1 — adds `referrals.commission_per_paid NUMERIC`, the commission earned per paid registration). `1749700000000_payments-order-kind.sql` (Wave 9 Phase 4 — `payments.registration_id` made nullable + a `kind` TEXT discriminator `registration`|`order` + a CHECK that `registration`-kind rows still carry a `registration_id`), `1749600000000_registration-voucher-code.sql` (Wave 9 Phase 3 — adds `registrations.voucher_code TEXT`, the applied registration-fee voucher), `1749500000000_session-camera-flag.sql` (Wave 8 — `sessions.camera_available`), `1749400000000_remove-supervisor-role.sql` (Wave 8 — drops `supervisor` from `users_role_check`, now 6 roles), `1749300000000_remove-question-maker-role.sql` (Wave 6 — drops `question_maker`). Wave 4–6: `1749000000000_competition-flows`, `1749100000000_affiliated-competitions`, `1749200000000_backfill-school-location`. `1748900000000_backfill-schools-from-students.sql` (2026-05-15 — backfills the `schools` directory from students who registered with an NPSN, deduped by NPSN). Earlier: `1748700000000_emc-roles.sql` (adds `supervisor` + `question_maker` to `users_role_check`; `question_maker` later removed by `1749300000000`), `1748600000000_emc-config.sql` (Sprint 20 Phase D, last of the 6-migration EMC schema batch). Sprint 20 Phase D batch: `1748100000000_emc-content`, `1748200000000_emc-venues`, `1748300000000_emc-exam-delivery`, `1748400000000_emc-commerce`, `1748500000000_emc-marketing`, `1748600000000_emc-config`. Sprint 20 Phases A & B: `1748000000000_add-competition-slug` (de-branded seed), `1748010000000_debrand-existing-emc-row` (renames locally-seeded Eduversal row), `1748050000000_password-reset-tokens`. Earlier Sprint 14–17 batch: `1746500000000_teacher-student-links`, `1746800000000_rebrand-registration-prefix-to-CTZ`, `1746900000000_audit-log`, `1747000000000_soft-delete`, `1747100000000_payment-webhook-events`, `1747200000000_add-person-kid`, `1747300000000_drop-orphan-parent-school-id`, `1747400000000_add-payer-user-id`, `1747500000000_school-verification`, `1747600000000_promote-legacy-approved-registrations`. Run all on VPS.
- **EMC multi-tenancy (Sprint 20 Phase D):** content/state tables in the EMC schema carry a `comp_id TEXT` column tying them to a row in `competitions`. Three tiers: **T1 strict** (22 tables — `comp_id NOT NULL REFERENCES competitions(id) ON DELETE CASCADE`, indexed): subjects, topics, subtopics, questions, answers, proofreads, exams, sessions, periods, answer_keys, paper_exams, paper_answers, webcams, voucher_groups, vouchers, products, orders, order_items, referrals, clicks, suggestions, accesses. **T2 nullable** (2 tables — `comp_id NULL`, NULL = platform broadcast): announcements, materials. **T3 global** (7 tables — no `comp_id` column): areas, test_centers, settings + 4 pivots (area_user, test_center_user, exam_question, question_topics). Per-comp UNIQUE keys: `(comp_id, code)` on questions/exams/voucher_groups/orders/referrals; `(comp_id, slug)` on products; `(comp_id, user_id)` on accesses. Soft-delete (`deleted_at TIMESTAMPTZ NULL`) on every non-pivot table, partial live indexes throughout. `compFilter(tableOrAlias, paramIndex)` in `query-helpers.ts` (added 2026-05-15) returns the `comp_id` scoping fragment mirroring `liveFilter` — `comp_id` stays a bound parameter (default `$1`). The `softDelete`/`restore` table whitelist (now a shared `SOFT_DELETE_TABLES` const) also covers the EMC question-bank tables (`subjects`, `topics`, `subtopics`, `questions`, `answers`, `proofreads`) + `competition_flows`.
- **Question Bank multi-language model (PR #41 + #42 + #43 + #44 — Komodo-parity rollout, 2026-05-22):** both `questions` and `answers` carry **6 parallel content columns** — `content` (English, required) plus `content2` (Bahasa) / `content3` (Russian) / `content4` (Spanish) / `content5` (French) / `content6` (Kazakh). The 6 columns existed on `questions` since the EMC port; PR #41's migration `1751200000000_qb-multilang-and-tags.sql` added `content4/5/6` to `answers` and a `questions.tags JSONB NOT NULL DEFAULT '[]'` column (+ GIN index on live rows). **Single source of truth on web:** `web/lib/question-bank/languages.ts` exports `LANGS` (the canonical 6-entry list), `LANG_TO_COL` (`'en'→'content'`, `'id'→'content2'`, …), and `pickLang(row, langCode)` which returns the chosen column's content **with English (`content`) as the fallback** when the chosen language is empty. The mobile mirror lives at `app/lib/exam-languages.ts`. **Authoring (PR #43):** the question editor at `/question-bank/questions/[id]` has a top-of-card tab strip — one active language at a time across the stem, every MC option, and the SA key (a single TipTap instance per slot, the bound HTML swaps with the tab). Each tab shows a green dot when filled; non-English tabs surface a "Copy from English" button. English content is required; the rest are optional. **Student runner (PR #44):** `sessions.language` (ISO 639 code, nullable) is set once at session-start via either the POST `/exams/:examId/sessions` body or the new one-shot PUT `/sessions/:id/language` endpoint. Once set the column is locked. `GET /sessions/:id` returns all 6 content columns on every question + every MC option AND the top-level `language` field — the client picks at render time, no re-fetch on switch. Web uses `dangerouslySetInnerHTML` (KaTeX math from PR #42 renders via the `katex/dist/katex.min.css` imported in the root layout). Mobile renders the chosen language's HTML stripped to plain text via `htmlToPlainText()` — math comes through as raw LaTeX source for now; a future phase will swap in `react-native-render-html` + a math renderer.
- **Rich-text + math editor (PR #42 — Komodo-parity rollout):** Standard editor for any operator-authored long-form HTML is `web/components/editor/rich-text-editor.tsx` — a headless TipTap (`@tiptap/react` + StarterKit + Image + Link + Placeholder + Mathematics) + KaTeX setup styled with shadcn tokens. Toolbar gives B / I / Strike / H2 / H3 / UL / OL / inline Code / Link / Math (Σ) / Image-by-URL; `Cmd/Ctrl+Shift+M` inserts a `$$` math skeleton; math uses `$…$` delimiters that the Mathematics extension auto-recognises + renders synchronously via KaTeX (no async re-flow flicker). Always **dynamic-imported** via `next/dynamic({ ssr: false })` so the ~120 KB ProseMirror + KaTeX bundle only ships on pages that mount the editor. `editable={false}` flips it into a read-only renderer for review / grading / student-exam display so math renders consistently across surfaces. See `web/components/editor/README.md` for the "how to add a new TipTap extension" recipe.
- **Soft delete (Sprint 14):** `users`, `students`, `parents`, `teachers`, `registrations`, `payments`, `documents`, `historical_participants`, `notifications` all have a nullable `deleted_at TIMESTAMPTZ`. Live queries must filter `deleted_at IS NULL` — use `liveFilter("alias")` from `backend/src/db/query-helpers.ts`. Helpers `softDelete(table, id)` and `restore(table, id)` are also exported (whitelisted to the 9 tables above).
- **Person-KID (Sprint 15):** `users.kid` is a `KX-2026-NNNNNNN` immutable identifier (sequence-backed, UNIQUE, NOT NULL). All 19 existing users were backfilled. Distinct from `registrations.registration_number` (`CTZ-2026-XXXXX`).
- **students table school column:** The column is `school_name TEXT` (not `school`). There is also a `school_id UUID` FK to the `schools` table. Always use `COALESCE(sc.name, s.school_name)` with a `LEFT JOIN schools sc ON s.school_id = sc.id` when you need the school name in queries.
- **Schools verification (Sprint 16):** `schools.verification_status` ∈ {`pending_verification`, `verified`, `rejected`}. Existing rows defaulted to `verified`. Self-signed-up schools start `pending_verification` until an admin approves at `/schools-pending`.
- **Payments payer attribution (Sprint 15):** `payments.payer_user_id` (FK users.id, nullable) + `payments.payer_kind` ∈ {`self`, `parent`, `school`, `sponsor`}. Existing rows backfilled to `(user_id, 'self')`. The mobile pay screen and school bulk-batch flow set this on insert.

### Registration Flow (updated May 2026)
1. Student registers → status = `pending_approval`
2. Admin approves on web (`/registrations` page) → status = `registered` (paid comp) or `paid` (free comp)
3. Student gets push notification to pay
4. Student pays via Midtrans Snap → webhook fires → status = `paid` automatically
5. No manual proof upload — Midtrans webhook is the only confirmation mechanism

### Payments
- Midtrans Snap: `POST /api/payments/snap` creates a transaction and returns a `snap_token`. Body now also accepts optional `payerKind` (`self` | `parent` | `school` | `sponsor`) and `payerUserId` (Sprint 15).
- Snap is blocked with 400 if registration is still `pending_approval` (not yet admin-approved).
- Webhook at `POST /api/payments/webhook` — verifies Midtrans signature **and** dedupes by `(provider, order_id, signature_key)` against the `payment_webhook_events` table (Sprint 14). Duplicate retries return `200 OK (duplicate)` without re-processing.
- **Payment proof upload has been removed** — Midtrans auto-confirms.
- VA expiry: webhook `expire` event resets registration back to `registered` (T10 fix).
- **Verify endpoint:** `GET /api/payments/verify/:registrationId` — calls Midtrans Status API directly and force-updates DB to `paid` when settled. Used by the app after browser close to sync status without relying on the webhook (which can't reach localhost in sandbox). In production the webhook still handles it; verify is a belt-and-suspenders backup.
- `pay.tsx` flow (Sprint 15): on screen mount, shows a "Dibayar Oleh" radio selector (4 options) **before** opening Snap. User picks payer attribution then taps "Lanjutkan ke Pembayaran". Auto-launch removed.
- After browser close, `pay.tsx` calls verify (both "Return to merchant" and user-dismiss paths). Only shows "Payment Completed!" when DB confirms `paid`. Polls up to 6× with 3s gaps (~18s total).
- **School-batch payments (Sprint 16.4):** Midtrans receipt is now issued in the SCHOOL'S name (was the coordinator's personal name); coordinator email remains the contact. This is what reimbursement workflows need.
- Sandbox keys are in `.env`. Switch to production keys before launch.

### File Storage (Sprint 14 — signed URLs)
- Files stored locally in `backend/uploads/<userId>/` in dev, or in MinIO/S3 when `MINIO_ENDPOINT` is set.
- **All client-facing URLs are now signed and 15-min expiry** (Sprint 14):
  - S3/MinIO: real presigned GET URLs via `@aws-sdk/s3-request-presigner`
  - Local disk dev: JWT-token URLs at `/uploads-signed/<token>` served by an endpoint in `index.ts` with path-traversal guard
- `GET /api/documents` returns signed URLs in the `fileUrl` field. Raw `/uploads/...` static path still served for backward compat in dev. In production the `MINIO_*` env vars are set on the Coolify backend service, so all client URLs route through MinIO presigned GETs — the local-disk static path is never exposed publicly.
- `backend/src/services/storage.service.ts` exposes `storeFile`, `deleteFile`, `getSignedUrl`, `verifySignedUrlToken`, and `isS3Configured`.

### Audit Log (Sprint 14)
- Append-only `audit_log` table records every privileged write. Columns: `user_id`, `user_role` (snapshot), `action`, `resource_type`, `resource_id`, `ip`, `user_agent`, `payload` (JSONB, with secrets redacted), `created_at`.
- Wired into 16 admin/organizer routes via `audit({ action, resourceType, resourceIdParam })` middleware (`backend/src/middleware/audit.ts`). Action labels look like `admin.competition.create`, `organizer.registration.approve`, `admin.school.verify`, etc.
- Logs only on 2xx responses; never blocks the request (fire-and-forget).
- Redacts: `password`, `current_password`, `token`, `snap_token`, `signature_key`, OTP `code`, parent invite `pin`.
- Retention: 5 years (purged by retention cron, see below).

### Retention Cron (Sprint 14)
- `backend/src/services/cron.service.ts` → `scheduleRetentionEnforcement()` runs daily at 02:00.
- Soft-deletes `documents` whose competition ended > 1 year ago.
- Hard-deletes `audit_log` rows older than 5 years.
- Hard-deletes already-read `notifications` older than 1 year.

### Rate Limiting
- `express-rate-limit` is wired to: `/api/auth/{signup,login}` (20/15min), `/api/auth/{send,verify}-otp` (5–10/window), `/api/auth/{forgot,reset}-password` (5/15min per IP+identifier — Sprint 20 Phase B), parent PIN verify (5/15min per email), and `/api/bulk-registration/upload` (3/hour per user — Sprint 14 addition). Returns 429 with friendly JSON.

### Bulk Registration
- CSV upload → `POST /api/bulk-registration/upload` (rate-limited 3/hour) → creates a job in `bulk_registration_jobs`.
- Background cron (every 1 min) in `backend/src/services/bulk-processor.service.ts` processes pending jobs.
- **Hard-match dedup (Sprint 15):** if NISN + email both miss but `LOWER(full_name) + school + grade` matches exactly one existing student, the row links to that user instead of creating a duplicate. Multiple matches throw "Ambiguous hard-match" so an operator can resolve manually.
- ~~Known gaps (Sprint 0 Task 3): bulk processor doesn't check competition fee, doesn't email temp password to new users, doesn't insert school_name~~ — all fixed in earlier sprints.

### Parent-Student Linking
- Student sends invite to parent email → `POST /api/parents/invite-parent` → generates 6-digit PIN → emails parent.
- Parent accepts → `POST /api/parents/accept-invitation` with PIN.
- Links stored in `parent_student_links` table.
- Debug PIN endpoint (`GET /api/parents/debug-pin/:userId`) blocked in production.

### Teacher-Student Roster (added May 2026)
- Teacher explicitly links students they supervise — no automatic school-based scoping.
- `POST /api/teachers/link-student` — teacher adds student by email (direct link, no PIN).
- `DELETE /api/teachers/link-student/:studentId` — remove from roster.
- Links stored in `teacher_student_links` table (migration `1746500000000_teacher-student-links.sql` — **already applied locally and on VPS**).
- **All teacher dashboard data is scoped to linked students only** — `GET /api/teachers/my-students`, `GET /api/teachers/my-competitions`, `GET /api/teachers/dashboard-summary`.
- App: `teacher-students.tsx` → manage roster; `teacher-analytics.tsx` → competitions my students joined.
- Teacher portal is **monitoring-only** — no bulk registration or bulk payment actions from the app. All write operations go through the web portal or admin.

### Profile Edit (role-aware, updated May 2026)
- `app/app/(tabs)/profile/edit.tsx` renders different fields per role — do NOT add student-specific fields for teacher/parent.
- **Student**: Personal Details (name, DOB, phone, email, city, interests, referral) + Student Card upload + School Details + Supervisor/Teacher + Parent/Guardian sections.
- **Teacher**: Personal Details (name, phone, email, city) + Professional Info (school, subject, department).
- **Parent**: Personal Details only (name, phone, email, city).
- Backend `PUT /api/users/me` saves `school` + `department` for teachers (not just `subject`).

### Regions (Province/City)
- App calls emsifa.com **directly** (not through backend) with in-memory cache.
- `app/services/regions.service.ts` — provinces and regencies cached for the session.
- Backend still has `GET /api/regions/provinces` and `GET /api/regions/regencies/:code` but the app no longer uses them.

### School Search on Signup
- Calls `GET /api/schools/search?name=...&regencyCode=...`
- Uses api.co.id when `API_CO_ID_KEY` is set.
- Falls back to querying the local `schools` DB table when key is not set (returns up to 20 matches by name).

### Web Portal Structure
- `web/app/page.tsx` — **Unified login** (Sprint 20): split-screen branded, email OR phone-OTP mode toggle, **auto-redirects via `window.location.assign(destinationFor(role))`** when `/auth/me` finds a live cookie on mount OR after successful login. The hard nav (instead of `router.replace`) is REQUIRED — the per-role `AuthProvider`s only hydrate once on mount, so a client-side nav after login leaves them stuck on the pre-login `user=null` state and the destination layout bounces back to `/`. Light/dark toggle, forgot-password link, footer (Privacy / Terms / Contact). Skeleton on hydrate.
- `web/app/forgot-password/page.tsx` — **NEW Sprint 20 Phase B:** email-only form that posts to `/api/auth/forgot-password`; success state shows "if your email matches…" copy.
- `web/app/reset-password/page.tsx` — **NEW Sprint 20 Phase B:** reads `?token=…`, password + confirm fields (min 8 chars, must match), posts to `/api/auth/reset-password`; success state redirects to `/` after 2.2s.
- `web/app/privacy/page.tsx` — UU PDP-aware Privacy Policy placeholder (DRAFT, needs counsel review) — Sprint 14. De-branded to Competzy-only in Sprint 20.
- `web/app/terms/page.tsx` — Terms of Service placeholder (DRAFT) — Sprint 14. De-branded in Sprint 20.
- `web/app/design-system/page.tsx` — **NEW Session 12:** public `/design-system` showcase — the Competzy design language (brand color palette, typography, radius scale, the shadcn component set) rendered live from the `globals.css` token layer, with a page-scoped light/dark toggle (`.dark` on a wrapper — no global side effect). Standalone public route, like `/privacy` + `/terms`.
- `web/app/(dashboard)/` — Admin portal (cookie-guarded by `(dashboard)/layout.tsx`)
- `web/app/(dashboard)/dashboard/page.tsx` — **Rewritten in Sprint 15**: 4 KPI cards (Registrations, Paid Rate, Revenue, Avg Time to Pay) + 90-day registrations sparkline + Top-3 competitions panel + 6-link grid
- `web/app/(dashboard)/registrations/page.tsx` — All registrations with status filter tabs (All/Pending/Registered/Paid/Rejected); approve/reject only for `pending_approval`
- `web/app/(dashboard)/competitions/page.tsx` — Competition management (create/edit/delete)
- `web/app/(dashboard)/users/page.tsx` — User management
- `web/app/(dashboard)/schools/page.tsx` — Schools management
- `web/app/(dashboard)/schools-pending/page.tsx` — **NEW Sprint 16:** verification queue for school applications (verify / reject with reason)
- `web/app/(dashboard)/segments/page.tsx` — **NEW Sprint 15:** 3 pre-built audiences (lapsed >1y, multi-comp veterans, EMC-only never tried KMC) for cross-sell campaigns
- `web/app/(dashboard)/notifications/page.tsx` — Broadcast notifications
- `web/app/(organizer)/` — Organizer portal (cookie-guarded by `(organizer)/layout.tsx`)
- `web/app/(organizer)/organizer-login/page.tsx` — Organizer login
- `web/app/(organizer)/organizer-dashboard/page.tsx` — Organizer dashboard
- `web/app/(organizer)/organizer-competitions/page.tsx` — Competitions list (publish/close actions)
- `web/app/(organizer)/organizer-competitions/new/page.tsx` — **Built (teammate) + Sprint 15 added `postPaymentRedirectUrl` field**
- `web/app/(organizer)/organizer-competitions/[id]/page.tsx` — Competition detail view (built by teammate)
- `web/app/(organizer)/organizer-competitions/[id]/edit/page.tsx` — Edit form (built; same redirect-URL field added)
- `web/app/(organizer)/participants/page.tsx` — Participants view per competition (approve/reject)
- `web/app/(organizer)/revenue/page.tsx` — Revenue overview (stat cards + per-competition table)
- `web/app/(school)/` — School portal (cookie-guarded by `(school)/layout.tsx` with verification gating)
- `web/app/(school)/school-login/page.tsx` — School login (has "New school? Apply for an account" link)
- `web/app/(school)/school-signup/page.tsx` — **NEW Sprint 16:** self-signup form for school + coordinator account
- `web/app/(school)/school-pending/page.tsx` — **NEW Sprint 16:** shown to logged-in coordinators while their school is `pending_verification` or `rejected`
- `web/app/(school)/school-dashboard/page.tsx` — School dashboard (stats + quick links; **Achievement PDF tile added in Sprint 16**)
- `web/app/(school)/{bulk-registration,bulk-payment,school-students,school-my-students,school-my-competitions,school-registrations,school-deadline}/page.tsx` — gated behind `verification_status = 'verified'`
- `web/lib/api/client.ts` — **Rewritten in Sprint 14:** single shared http function with `credentials: 'include'`; `adminHttp`/`organizerHttp`/`schoolHttp` aliases
- `web/lib/auth/{context,organizer-context,school-context}.tsx` — **Rewritten in Sprint 14** to hydrate via `/api/auth/me` cookie auth (no more localStorage)
- `web/components/Sidebar.tsx` — Admin nav with Dashboard, Registrations, Competitions, **Segments**, **Pending Schools**, Send Notification, Schools, Users
- `web/next.config.mjs` — API proxy config

**Test accounts (local dev):**
- Admin: `admin@eduversal.com` / `admin123` (`npm run db:create-admin`)
- Organizer: `organizer@eduversal.com` / `organizer123` (`npm run db:create-organizer`)
- Student / Parent / Teacher / School-admin / Country-rep: `{student,parent,teacher,schooladmin,rep}@test.local` / `Test123!` — all five seeded by `npm run db:create-test-accounts` (idempotent; `rep@test.local` is a `country_representative` who manages Komodo's Malaysia local round; teacher + school-admin link to the first verified school; the parent + teacher are also linked to a 6-student roster — the test student + 5 random real students — so the My Children / My Students screens have data).
- The question bank, exams, venues + proctoring have no dedicated role — they are managed by the admin + organizer accounts above.
- **EMC demo data:** `npm run db:seed:emc-demo` (`backend/src/db/seed-emc-demo.ts`) populates the EMC competition with venues (3 areas, 5 test centers), the question-bank taxonomy (3 subjects / 6 topics / 4 subtopics) + 12 questions (8 approved, 2 submitted, 2 draft), 2 exams (`EMC-R1`, `EMC-PRAC`), one finished exam attempt awaiting grading, (Wave 9) 4 commerce products (`PRD-D01`–`D04`), a 10-code voucher batch (`VG-D01`), a sample paid order (`ORD-D01`), and (Wave 10) 2 affiliate referrals (`REF-D01/D02`, with funnel + clicks), 2 announcements (1 competition / 1 platform-wide), 3 study materials, and 2 student suggestions. Idempotent — keyed by code/name, safe to re-run. **Every EMC feature ships with example data via this seeder** (see memory `feedback-seed-demo-data`) — extend it when adding a feature so the review screens are never empty.
- **Test-environment seeder (Session 12):** `npm run db:seed:test-competitions` (`backend/src/db/seed-test-competitions.ts`) — **dev/test only; it `TRUNCATE`s all competition-scoped data.** Resets the platform to **4 native competitions** — EMC (`/emc`), ISPO (`/ispo`), OSEBI (`/osebi`) and the **multi-round Komodo** (`/komodo`, 6 rounds — see SPRINT 33). EMC/ISPO/OSEBI are each fully wired for the lifecycle: a 6-step flow, 8 approved MC questions, an exam open today (`<TAG>-R1`), 2 products, a 5-code voucher batch, an announcement + materials, and 2 fresh zeroed affiliate referral codes (`<TAG>-AFF1/2`). `users` + `historical_participants` (the 63k legacy records) are **not** touched. `web/lib/competitions/registry.ts` carries matching `emc`/`ispo`/`osebi` portal branding. Purpose: test the full register → pay → exam → certificate flow + the referral funnel end-to-end. Supersedes `db:seed:emc-demo` for a multi-competition test env (that seeder targets the now-removed `comp-1-eduversal-mathematics-competition` id).

### Historical Data (IMPORTED ✅)
- 63,365 real participant records imported into `historical_participants` table.
- Excel file lives at `/Users/mujtabo/Desktop/All/Internship Eduversal/beyond-classroom/Eduversal_Database.xlsx` — NOT in the repo.
- Identity matching: email (88.6% coverage) + WhatsApp/phone (96.9% coverage).
- Auto-link fires at login (`/me` and `/phone/verify-otp`) — matches email OR phone, skips if already linked.
- Manual claim via `GET /api/historical/search` + `POST /api/historical/:id/claim`.
- Mobile: `app/app/(tabs)/profile/history.tsx` — "My Records" tab + "Find & Claim" tab.
- **Smart phone login (added May 2026):** If phone OTP succeeds but no `users` account, backend checks `historical_participants.phone`. If matched, returns `{ historicalMatch: true, fullName, email, phone }`. App routes to `app/(auth)/claim-account.tsx` which pre-fills name/email from historical data — user only needs to set a password. Account is created and historical records are auto-linked on signup.

### File Storage
- **Dev (default):** local disk `backend/uploads/<userId>/`, served as static by Express.
- **Production (MinIO):** set `MINIO_ENDPOINT`, `MINIO_ACCESS_KEY`, `MINIO_SECRET_KEY`, `MINIO_BUCKET`, `MINIO_PUBLIC_URL` in `backend/.env`. The code automatically switches to S3 when these are set.
- All three upload routes (users, documents, payments) now use `multer.memoryStorage()` + `storeFile()` from `storage.service.ts`.
- T21 (production MinIO) is now done as a Coolify service — provision via the Coolify UI, see `deploy/COOLIFY.md` step 3. The backend reaches it over the docker network (`MINIO_ENDPOINT=http://minio:9000`); browsers reach it over the public domain via `MINIO_PUBLIC_URL`.

---

## Current Task Status (as of May 24, 2026 — Sessions 18–19)

**Latest — Teacher/School approval queue — STEP 2 (2026-06-25, branch `feature/teacher-school-approval`, stacked on the role-selector branch):** the admin/organizer side of the role-selector. New `backend/src/routes/verification.routes.ts` (mounted `/api`, `authMiddleware` + `requireRole("admin","organizer")`) — a unified `/api/verification/*` namespace with pending-list + verify + reject for BOTH schools and teachers (the legacy admin-only `/api/admin/schools/*` endpoints stay; this opens approval to organizers too and adds teachers). verify/reject are `audit()`-wrapped and push-notify the applicant. Web: a shared `web/components/verification-queue.tsx` (Schools | Teachers tabs, reject-with-reason dialog) mounted at `/pending-approvals` (admin — replaces the old "Pending Schools" sidebar link) and `/organizer-approvals` (new organizer "Approvals" nav item). `opnav.approvals` i18n added (en/id). Smoke-tested end to end: admin + organizer list/verify a pending teacher (DB flips to `verified`, `verified_by` set), student → 403, reject-without-reason → 400. `tsc` clean (backend/web) + web `next build` clean. **Still open:** mobile parity (operator approval is web-only by design); the old `/schools-pending` route file is left in place but unlinked.

**Prior — Signup role selector (Student / Teacher / School) — STEP 1 of N (2026-06-25, branch `feature/signup-role-selector`):** the competition register page (`/competitions/[slug]/register`) was student-only; now a 3-tab segmented control (Student / Teacher / School) picks the account type. **Student** → created + auto-enrolled, straight to dashboard (no approval). **Teacher / School** → created **pending admin/organizer approval** and bounced to `/school-pending` until verified, so a student can't self-assign teacher/school tooling. Minimal extra fields: teacher = School name + NPSN + Subject; school = School name + NPSN (+ coordinator name via the shared name field). Backend: `/auth/signup` now handles `teacher` (pending) + `school_admin` (creates pending school in-txn, mirrors `/api/schools/signup`) + a `SIGNUP_ROLES` allowlist blocking privileged-role self-signup; new migration `1752900000000_teacher-verification.sql` adds `teachers.{npsn,verification_status,applied_at,verified_at,verified_by_user_id,rejection_reason}` (DEFAULT 'verified' grandfathers existing teachers); `fetchUserWithRole` returns `teacherVerificationStatus`/`teacherRejectionReason`; the `(school)` layout + `/school-pending` page gate on it (role-aware copy). Email-verify (send-code) step kept for all three roles. **OPEN / next steps:** no admin/organizer UI+endpoint to approve *pending teachers* yet (schools reuse the existing `/schools-pending` queue) — a pending teacher is un-approvable until that ships; mobile parity (the app register already has role selection, needs the same verification gate); make the school-verify endpoints organizer-reachable (currently admin-only). Backend smoke-tested end-to-end (teacher/school pending, NPSN dup → 409, student regression, admin self-signup → 400); `tsc` clean (backend/web/app) + web `next build` clean.

**Prior — SPRINT 37 production hardening + UX audit + nav IA + Users CRUD (2026-05-22/24):** local dev setup unstuck (port 3010 backend / 3001 web pinned), Coolify path-based auto-deploy wired (`backend/**` / `web/**` Watch Paths so each app deploys only when its directory changes), two production firefights (`express-rate-limit` v8 X-Forwarded-For trust-proxy hard throw on every rate-limited endpoint + empty-string NISN tripping `idx_students_nisn` partial unique), reusable `dbErrorResponse` PG→HTTP 4xx mapper, comprehensive UX audit + WCAG AA hardening pass (muted-foreground contrast, global `prefers-reduced-motion`, focus-visible everywhere, aria semantics, alt text, status-pill icons), navigation IA refresh (user identity moved to top-bar dropdown, Account Settings link added for every role, per-role sidebar reorg with Test Centers rename + Revenue moved to admin), universal `/account/profile` (operators see slim Photo+Personal, students keep full form), Users admin CRUD (Edit dialog with role-aware school/parent-student assignments — admin can finally edit users, not just impersonate), and a non-dismissible **school-pick blocking modal** for teachers/school_admins without an associated school. Also pulled PRs #47 (Komodo finalise — international Midtrans IDR-equivalent, country-scoped vouchers, round-bound exams), #48 (Komodo intl dashboard fix), #51 (mobile catalog correctness — auth header + intl filter on recommendations), #52 (mobile profile country picker + Indonesia location cascade). See **SPRINT 37** below.

**Prior — Multi-Round Competitions + Komodo Wave 2 COMPLETE (2026-05-18/19, Session 13):** a multi-round competition engine (per-round registration / payment / round-scoped exam access + configurable gating), numeric grades replacing SD/SMP/SMA, the Komodo competition's full round model (3 online rounds + Fast Track + a per-country Local Round + a medal-gated Bali Global Round), the `country_representative` role + portal (bulk register / bulk pay / CSV score import), operator round-config + medalist management, and the web student portal restructured onto a left-sidebar `AppShell`. All on `feature/multi-round-competitions` (`eduversal-team/competzy`). See **SPRINT 33** below.

**Sprints 0–16 fully complete locally + Sprints 19–20 shipped + EMC Port Wave 1 & Wave 2 COMPLETE. Wave 2 = the professional web UI/UX redesign — every web surface on one shared design system.**
**Latest milestone — EMC Wave 2 RE-SCOPED to a professional web UI/UX redesign (2026-05-15, Session 11):** after reviewing the running portals the user redirected Wave 2 from feature work to a full UI/UX overhaul — rebuild every web surface (admin, organizer, school, student portal, auth/public pages) on ONE shared design system: Tailwind v4 + shadcn/ui + recharts + lucide-react, a shared `AppShell`, teal/indigo brand, DM fonts — keeping all behavior identical. The original Wave 2 feature scope (catalog, question bank, step-flow) moves to Wave 4+; the mobile re-theme is Wave 3. **Wave 2 COMPLETE (2026-05-16):** all 6 phases shipped — Phase 0 EMC-port role foundation; Phase 1 Tailwind v4 + shadcn/ui design system + `web/` upgraded to Next 16 + React 19; Phases 2–5 every web surface (admin, organizer, school portals + student competition portal + `/` login + auth/public pages) rebuilt on one shared `AppShell` / design system. `web/` Schools directory now auto-populates from student registrations (backend side-task). Legacy CSS fully stripped — `globals.css` is purely the shadcn token layer. Wave 2 plan file: `/Users/mujtabo/.claude/plans/synthetic-dreaming-boot.md`. Next: Wave 3 (mobile app re-theme).
**Prior milestone — EMC Wave 1 COMPLETE (Phases A + B + C + D):** Sprint 20 shipped end-to-end — unified email/password + phone-OTP login at `/` with role-based auto-routing, full forgot-password flow (`password_reset_tokens` + `/forgot-password` + `/reset-password` pages, 15-min token TTL, single-use, rate-limited), polished register page (inline validation + 8-char password + duplicate-email handling + mobile-responsive split-screen), competition portal generalized to `/competitions/[slug]/{register,dashboard,admin}` driven by a slug→config registry, and the 31-table multi-tenant schema for the question bank + exam delivery + commerce + marketing + config (6 migrations `1748100000000`–`1748600000000`). All user-visible "Eduversal" references stripped — platform is Competzy-only.

### EMC Port (begins 2026-05-13) — Waves 1–13 ✅ COMPLETE · EMC port complete (Wave 14 found unnecessary)

We're porting the feature set of the legacy `eduversal-team/emc` Laravel app onto Competzy's existing stack. 9 waves total, ~6–10 weeks. Wave 1 was re-scoped from "schema-only" into four sequential phases.

- **Plan files:** Wave 1 — `/Users/mujtabo/.claude/plans/resumption-prompt-paste-playful-bachman.md` (archived). Wave 2 — `/Users/mujtabo/.claude/plans/synthetic-dreaming-boot.md` (active).
- **Cadence:** wave-by-wave — plan-mode session → ship → plan next wave. No mega-plan. Within a wave, one commit per phase + push to `feature/legacy-cleanup`.
- **Wave 1 phases (all shipped 2026-05-13):** (A) cleanup + de-brand to Competzy-only ✅; (B) login + register polish (forgot-password, phone OTP, mobile-responsive) ✅; (C) generalize `/emc/*` → `/competitions/[slug]/*` ✅; (D) 6 new migrations (`1748100000000`–`1748600000000`) creating 31 multi-tenant tables ✅.
- **Wave 2 = professional web UI/UX unification ✅ COMPLETE (re-scoped 2026-05-15, shipped 2026-05-16).** Every web surface rebuilt on one shared design system — Tailwind v4 + shadcn/ui + recharts + lucide-react, a shared `AppShell` (sectioned sidebar + top bar), teal/indigo brand, DM fonts. Phases all shipped: (0) EMC-port role foundation; (1) design system + Next 16/React 19 upgrade + admin dashboard; (2) admin portal; (3) organizer portal; (4) school portal; (5) student competition portal + `/` login + auth/public pages. Presentation only — behavior preserved; legacy CSS stripped. See the SPRINT 21 table for the per-phase commit log.
- **Catalog URL (built in Wave 4 Phase 1):** `/competitions` is the catalog (auth-gated; student/parent post-login lands here and picks a competition); admin competitions-management moved from `/competitions` to `/admin/competitions`. `DEFAULT_COMPETITION_SLUG` in `web/lib/competitions/registry.ts` is no longer the post-login destination (still used by `/`'s "create a student account" register link).
- **Wave 1 schema scope (Phase D):** 6 migrations `1748100000000`–`1748600000000` creating 31 tables (subjects, topics, subtopics, questions, answers, question_topics, proofreads, areas, test_centers, area_user, test_center_user, exams, exam_question, sessions, periods, answer_keys, paper_exams, paper_answers, webcams, voucher_groups, vouchers, products, orders, order_items, referrals, clicks, announcements, materials, suggestions, settings, accesses). UUID PKs, JSONB for legacy TEXT-JSON, soft-delete pattern matches Sprint 14.
- **Skipped this wave:** `representatives` (already in our `schools` table), `tags`/`taggables` (Spatie polymorphic — not essential), Spatie permission tables (we keep `users.role` enum-as-text).
- **Wave 2–13 preview** (Wave 4 re-scoped 2026-05-16; affiliated-competitions wave inserted as Wave 5 — later waves shift +1): (2) professional web UI/UX unification ✅; (3) mobile app re-theme ✅; (4) `/competitions` catalog + admin move + per-competition step-flow engine; (5) **affiliated competitions** — register-on-Competzy-compete-elsewhere (competition `kind` flag, per-registration credentials + secure delivery, external-access step renderer, organizer/teacher credential-issuing UI); (6) question-bank authoring UI; (7) online + paper exam delivery; (8) test-center / area / webcam proctoring ✅; (9) vouchers + products + orders UI ✅; (10) referrals + announcements + materials ✅; (11) mobile rollout of student surfaces ✅; (12) certificate PDF + QR verify + barcode ✅; (13) mobile merchandise store ✅; (14) ~~data import from legacy `kompetisi.net` MySQL~~ — **investigated 2026-05-17, found unnecessary and not done**: no `kompetisi.net` MySQL dump exists anywhere, and Sprint 4's historical import (`import-historical.ts` → `historical_participants`, 63,365 rows from `Eduversal_Database.xlsx`) **already covers every competition** — EMC 37,649 / ISPO 11,361 / OSEBI 11,568 / Komodo 8,659 / STEM Olympiad 1,610 / Owlypia 728 — so the claim-your-records feature is already complete. (A trial import of `Master_Database_Unified.xlsx` was run and reverted: it duplicated already-imported data.) **The EMC port is complete at Wave 13** — do not re-attempt Wave 14.
- **Locked design decisions** (per user May 13, 2026 evening replanning): **Multi-tenant schema from day one** — content tables carry `comp_id` (tiered: 24 strict NOT NULL, 2 nullable on `announcements`/`materials`, 5 global on `areas`/`test_centers`/`settings`/pivots). **One app + one website for ALL competitions** — routes are slug-keyed `/competitions/[slug]/…`. **Per-competition step-flow engine (Wave 4).** **Two competition kinds — native (full lifecycle inside Competzy) vs affiliated (students register + pay on Competzy, then receive credentials + a link and compete on an external site); affiliated tooling is Wave 5.** Both online AND paper exam from day one. No `PortalUser`/cross-site SSO. Legacy users keep their data (Wave 9 import — now simpler thanks to multi-tenancy: every legacy row gets `comp_id = 'comp_emc_2026_main'`). Mobile in lockstep but operator-only features stay web-only. UX matches `competzy.com` marketing site palette + typography. **Brand is Competzy-only — no user-visible "Eduversal" references anywhere.**

### Repository state (2026-05-17)

The Wave 3–12 stacked-PR backlog — 51 PRs — was merged bottom-up into `main` on 2026-05-17, followed by the mobile role-guard fix. **`origin/main` is now fully current** — 0 open PRs, every merged feature branch pruned, `origin` carries only `main`. (An earlier "8 commits ahead of `origin/main`" note here is obsolete — there is no longer an unpushed backlog.) The only remaining rollout work is the VPS / production checklist below.

### Manual rollout still required (Coolify deployment — 2026-05-18 refresh)
Follow `deploy/COOLIFY.md` end-to-end. Server is `<PROD_HOST>` (Coolify already installed). Summary of external prerequisites:
- **DNS:** A records pointing at `<PROD_HOST>`:
  - `api.competzy.com`
  - `arena.competzy.com`
  - `minio.arena.competzy.com`
  - (`competzy.com` is already pointed and served by the separate `eduversal-team/competzy-web` Coolify app on port 3000 — don't touch.)
- **Coolify project setup:** create `competzy` project → provision Postgres → provision MinIO (root creds, bucket `competzy`, non-root access keys, exposed ports 9000/9001) → deploy `backend/` (exposed port `3010:3000`) and `web/` (exposed port `3011:3001`) from Dockerfile, with env vars per `backend/.env.example` and the build arg `NEXT_PUBLIC_API_URL=https://api.competzy.com/api` on the web service. **Leave Coolify "Domains" field empty on every service** — host nginx handles routing.
- **Host nginx + certbot:** SSH to server, copy `deploy/nginx.conf` to `/etc/nginx/sites-available/competzy-arena`, symlink into `sites-enabled`, `sudo nginx -t && sudo systemctl reload nginx`, then `sudo certbot --nginx -d api.competzy.com -d arena.competzy.com -d minio.arena.competzy.com`. Certbot auto-renews via `certbot.timer`.
- **Run all migrations** on the Coolify backend container terminal: `npm run db:migrate` (applies `1746500000000` through `1749900000000`).
- **Seed/admin scripts** (`db:create-admin`, `db:seed:*`) use `ts-node` which is pruned from the production image. The compiled JS still ships in `dist/db/`, so run them straight inside the backend container: `docker exec <backend> node dist/db/create-admin.js` (same pattern for `create-organizer`, `create-test-accounts`, `seed-test-competitions`). `db:import-historical` additionally needs `xlsx` (devDep, also pruned) — `docker exec <backend> npm install --no-save xlsx` first, then `node dist/db/import-historical.js /path/to/Eduversal_Database.xlsx` (`scp` the Excel onto the server beforehand). The "run from a developer machine + SSH tunnel" path still works but is rarely needed.
- **Midtrans production keys** + webhook URL `https://api.competzy.com/api/payments/webhook`.
- **EAS init:** `cd app && npx eas init` (needs expo.dev account). Fill in `appleId`, `ascAppId`, `appleTeamId` in `app/eas.json`. Mobile rilis is independent of the server.
- **Apple Developer + Play Console** for App Store / Play Store submission.
- **api.co.id production key** (`API_CO_ID_KEY` env on the backend service).
- **Privacy + Terms legal review** of `web/app/privacy/page.tsx` and `web/app/terms/page.tsx` (currently DRAFT).
- **Load test against staging** once it's up: `k6 run loadtest/k6-registration.js --env BASE=https://staging-api.competzy.com`.

### Deployment record — 2026-05-18 evening (Phase 1 LIVE) ✅
Server `<PROD_HOST>` came up in one session. SSH `<DEPLOY_USER>` (sudo + docker group). Server already runs the `competzy.com` landing page (port `3000`, repo `eduversal-team/competzy-web`) and an unrelated `pathvance.com` tenant (`3001`/`4000`) — new ports chosen to avoid the collision.

| Layer | Service | Host port | Container (Coolify name) | Domain | TLS |
| --- | --- | --- | --- | --- | --- |
| DB | Postgres 16 | — internal | `<POSTGRES_CONTAINER>` | — | — |
| Object store | MinIO | `9000`, `9001` | `minio-<MINIO_CONTAINER>` | `https://minio.arena.competzy.com` | Let's Encrypt (host nginx) |
| API | Backend (Express) | `3010 → 3000` | `<BACKEND_CONTAINER>-…` | `https://api.competzy.com` | Let's Encrypt (host nginx) |
| Web | Next.js | `3011 → 3001` | `<WEB_CONTAINER>-…` | `https://arena.competzy.com` | Let's Encrypt (host nginx) |

- **TLS** — single Let's Encrypt SAN cert covering all three subdomains (api + arena + minio.arena), valid through `2026-08-16`, auto-renewed by `certbot.timer`. Provisioned with `sudo certbot --nginx -d api.competzy.com -d arena.competzy.com -d minio.arena.competzy.com` against the `competzy-arena` server-block template (`deploy/nginx.conf` → `/etc/nginx/sites-available/competzy-arena`, symlinked into `sites-enabled`).
- **MinIO** — provisioned via Coolify "Docker Compose Empty" (not "Docker Image" — that resource type has no CMD-override field, so the required `server /data --console-address :9001` argument couldn't be passed). Compose file pins `image: minio/minio:latest`, declares `command: server /data --console-address :9001`, mounts a Docker named volume `minio-data:/data`, and binds 9000+9001 to host. Bucket `competzy` + a non-root service account were created via `mc admin user svcacct add` from inside the MinIO container (the slim console UI doesn't expose Access Keys management).
- **Database** — 60 tables migrated (`1744070400000` → `1750000000000_grades-numeric`) via `docker exec <backend> npm run db:migrate`. Seeds run via the **compiled JS** (the production image prunes `ts-node`, but `tsc` already emits `dist/db/*.js`):
  - `node dist/db/create-admin.js` → `admin@eduversal.com` / `admin123`
  - `node dist/db/create-organizer.js` → `organizer@eduversal.com` / `organizer123`
  - `node dist/db/create-test-accounts.js` → `student/parent/teacher/schooladmin@test.local` / `Test123!` (parent + teacher rosters pre-linked to the test student)
  - `node dist/db/seed-test-competitions.js` → 3 native competitions (EMC, ISPO, OSEBI) with full lifecycle wiring (8 questions each, an open exam `<TAG>-R1`, 2 products, voucher batch `<TAG>-VG1`, 2 affiliate referrals `<TAG>-AFF1/2`, areas + test centers, the 6-step flow)
- **Smoke test** — `curl -X POST https://api.competzy.com/api/auth/login` with admin creds returns 200, the `competzy_token` cookie (`HttpOnly; Secure; SameSite=Lax`), and the user payload (`kid: KX-2026-0000001`, `role: admin`). CORS allow-origin echoes `https://arena.competzy.com`, JWT, and the 20/15min rate-limiter are all wired.
- **In-flight fix during deploy** — `web/Dockerfile` had `COPY --from=builder /app/public ./public` but the repo ships no `public/` folder, so the first web deploy failed at runtime stage 4/6 with `failed to calculate checksum of ref "/app/public": not found`. Fixed in commit `e53da3a` by adding `RUN mkdir -p public` before `npm run build` in the builder stage. Future Dockerfile templates: don't `COPY` a directory you don't guarantee the source ships.
- **Skipped / deferred:**
  - `db:seed:emc-demo` — fails by design (targets the now-removed `comp-1-eduversal-mathematics-competition` id, **superseded by `db:seed:test-competitions`** — don't run on this server).
  - `db:import-historical` — the 63k-row `Eduversal_Database.xlsx` is off-repo PII and not on the server. To run: `scp` the file onto the server (`/home/<DEPLOY_USER>/`), `docker exec <backend> npm install --no-save xlsx`, then `docker exec <backend> node dist/db/import-historical.js /home/<DEPLOY_USER>/Eduversal_Database.xlsx`. Until run, the claim-your-records feature has zero hits.
  - Production keys for SMTP / Twilio Verify / Midtrans / Sentry / `API_CO_ID_KEY` — placeholder env vars on the backend service; those vendor features are dark until the real keys land + container restarts.
- **Coolify quirk** — this Coolify install has no proxy (no Traefik), and the "Domains" field on each service is left empty — host nginx is the public face, and Coolify "Exposed Ports" controls the host port nginx proxies into. The `Coolify-helper:1.0.13` container is the one that performs the per-deploy `git clone` + `docker build`; if a build hangs you can target it directly to read the logs.
- **Coolify root login** — the only admin on the dashboard is `<COOLIFY_ADMIN_EMAIL>`. If the password gets lost, reset it from the server with `docker exec -it coolify php artisan root:reset-password` (interactive; no flag override). Same command works for any future admin too.

### Deployment record — 2026-05-19 (Sprint 33 + My Account suite + phone normalization) ✅
Two redeploys landed on production within hours of the Phase 1 launch — `main` advanced from `3cd8bef` → `94c1320` → `d45bb05` (a teammate's 24-commit batch).

**Wave 1 — Sprint 33 (commit `94c1320`):** multi-round competitions + Komodo. 6 migrations applied via `docker exec <backend> npm run db:migrate`:
- `1750100000000_competition-rounds-gating.sql` — per-round gating + prerequisites
- `1750200000000_competition-logo.sql` — competition logo URL column
- `1750300000000_registrations-round-id.sql` — registrations attach to a specific round
- `1750400000000_komodo-round-model.sql` — Komodo-style multi-round schema (online / fast-track / local / global tiers)
- `1750500000000_rep-payment-batches.sql` — country-rep payment batches (mirrors school batches)
- `1750600000000_round-activation.sql` — `competition_rounds.is_active` toggle; existing `fast_track` + `global` rows backfilled to `is_active=false` so the operator activates them per cohort
- New seed `node dist/db/seed-komodo.js` — non-destructive, idempotent (`Komodo already exists` no-op on re-run). Inserts `comp-komodo` (slug `komodo`, native) with 6 rounds: 3 Online (active) + Fast Track (inactive) + Local Malaysia offline (active) + Bali Global (inactive).

**Wave 2 — My Account suite + super-admin impersonation + phone normalization (commit `d45bb05`):**
- 1 migration applied: `1750700000000_normalize-user-phones.sql` — rewrites every `users.phone` from E.164 (`+62...`) to local 0-prefixed (`08...`) using `regexp_replace`; the migration ships a diagnostic SQL block (commented out) for finding post-normalization collisions, and PR #11 made the script collision-safe before merge.
- Web fitur baru live di `https://arena.competzy.com`: **My Account** for students (documents, notifications, competitions, historical-records claim, parent-family linking — PRs #4/#7/#8/#9/#10), catalog favorites, **super-admin user impersonation**, phone-login normalization (PR #5).
- Mobile: per-round registration UI for the Komodo flow.

**State after both waves:**
- 68 migrations applied total (up from 61 after Phase 1 launch).
- 6 competitions in `competitions`: EMC, ISPO, OSEBI (from `seed:test-competitions`), Komodo (from `seed:komodo`), plus two operator-created via UI (`Owlypia Online Round 8` affiliated, `EMC — Mathematics Competition Final` native).
- 7 user phones present + correctly normalized — these include real account testers (mujtabo05@upi.edu, sizu@eduversal.org, plus a few @gmail tester accounts), confirming people are actively poking at the live portal.
- Latest backend container: `<BACKEND_CONTAINER>-153523268184` (image tag `d45bb05…`), web container also on `d45bb05…`. Smoke test admin login: 200 / 256ms.

Both redeploys triggered manually from the Coolify UI (auto-deploy webhook still not wired). Pattern from Phase 1 held: `npm run db:migrate` runs cleanly inside the new container, seeds run via `node dist/db/<name>.js` (no ts-node install needed).

**Pre-launch watch-list (still open):**
- Change the `admin` + `organizer` passwords through the UI — `admin123` / `organizer123` are dev fixtures fine for soak testing only.
- Wire SMTP, Twilio Verify, Midtrans production keys, Sentry DSN, and `API_CO_ID_KEY` once those vendor accounts go live; redeploy backend to pick them up. Reminder: only `MIDTRANS_SERVER_KEY` / `MIDTRANS_CLIENT_KEY` / `MIDTRANS_IS_PRODUCTION` go on the backend service — never on web/app — and the Midtrans dashboard's webhook URL must be set to `https://api.competzy.com/api/payments/webhook` for the settle flow to land.
- Run `db:import-historical` once the Excel file is on the server.
- Mobile binaries via EAS Build (`cd app && eas build --profile production`).
- Privacy + Terms legal review (`web/app/privacy/page.tsx`, `web/app/terms/page.tsx`).
- Operator action on Komodo: flip `Fast Track` + `Bali Global Round` to active in the round-activation toggle UI when each round opens for registration.

---

## Sprint Plan (Full Roadmap)

### SPRINT 37 — Production hardening + UX audit + nav IA + Users CRUD (May 22–24, 2026 — Sessions 18–19) ✅ COMPLETE

Long session covering local dev setup, two production firefights, a comprehensive UX audit + fix pass, navigation IA refresh across every portal, and Users-panel CRUD + assignments.

| Task | What | Key files |
|---|---|---|
| 37.1 | **Local dev quirks resolved.** Postgres.app on macOS (CLI shortcut: user added `pg-start` alias). Backend pinned to port **3010** in `backend/.env` because port 3000 is occupied by the user's unrelated Pathvance Next.js dev server (different repo). Web pinned to port **3001** via `next dev -H 0.0.0.0 -p 3001` in `web/package.json` so it never collides. `web/.env.local` carries `BACKEND_URL=http://localhost:3010` so the Next.js `/api/*` rewrite points at the right port. A fresh `competzy` DB was created locally (legacy `beyond_classroom` existed but had only 9 tables — pre-migration baseline), all migrations applied through `1751500000000`. | `backend/.env` (untracked), `web/.env.local` (untracked), `web/package.json` |
| 37.2 | **Coolify path-based auto-deploy wired.** Both prod apps (`competzy-backend` + `competzy-arena`) now have **"Watch Paths"** set in Coolify so each deploys only when its directory changes — `backend/**` triggers backend, `web/**` triggers web, and a dual-touch commit triggers both in parallel. Initially tried `**` (matched repo-root everywhere → both deployed on every push, defeating the point). Verified across all three scenarios (backend-only commit → only backend; web-only → only web; dual → both). The Auto-deploy toggle is on; the GitHub webhook fires automatically on push to main. Watch Paths in Coolify v4 are evaluated **relative to repo root**, not Base Directory — the placeholder hint in the UI is misleading. | (Coolify UI config only — no repo files) |
| 37.3 | **Favicon: brand purple "C".** Replaced the placeholder ICO (from `competzy-web`, which contained 12 sparse pixels) with `web/app/icon.svg` — purple `#4a148c` rounded square + white stroked C arc. Matches the landing-page favicon brand. Next.js App Router auto-serves it. | `web/app/icon.svg` (new) |
| 37.4 | **Production fire #1: "Failed to update profile" toast when changing country.** Initial diagnosis from a parallel Claude session: missing migrations on prod. Actual root cause uncovered from container logs: `idx_students_nisn` is a partial unique index `WHERE nisn IS NOT NULL` — but empty string is NOT NULL, and the profile-update route pushed `''` straight from the request body. Multiple legacy rows had `nisn = ''`, so saving any field re-applied `''` to the row's own nisn → 23505 collision → 500 → generic toast. Fix in `users.routes.ts:171` coerces `'' → null`; migration `1751500000000_normalize-empty-nisn.sql` cleans existing rows idempotently. Lesson: **always pull `docker logs` first before hypothesising at the schema/migration level**. | `backend/src/routes/users.routes.ts`, `backend/migrations/1751500000000_normalize-empty-nisn.sql` (new) |
| 37.5 | **Production fire #2: HTTP 500 on login after rate-limit upgrade.** `express-rate-limit` was bumped to v8 which **throws `ERR_ERL_UNEXPECTED_X_FORWARDED_FOR`** as a hard error when a request carries `X-Forwarded-For` but Express isn't told to trust any proxy. Prod's host nginx terminates TLS and always adds the header, so every rate-limited endpoint (login, OTP send/verify, password reset, parent PIN, bulk upload) was 500-ing. Login succeeded server-side (cookie set) but client got a 500 toast right after. Fix: `app.set('trust proxy', 1)` near the top of `index.ts` — trusts exactly one hop (host nginx). Local dev never sees the header so it's a no-op there. **Lesson: error codes flagged as "warning noise" can actually be hard throws** (I dismissed this from the same log earlier in the session). | `backend/src/index.ts` |
| 37.6 | **Reusable DB error → HTTP status mapper.** New `backend/src/lib/db-errors.ts` exports `dbErrorResponse(err, constraints?)` mapping Postgres SQLSTATE codes to HTTP statuses: `23505→409`, `23503/23502/23514/22001/22P02→400`, others fall through (route's generic 500 catches). Takes an optional `{ constraintName: friendlyMessage }` map so callers can phrase known unique-index collisions ("Email is already registered", "That NISN is already linked", etc.). Adopted in `auth.routes.ts` signup (was returning generic 500 on email-taken — the web's `/already registered/i` regex never matched) + `users.routes.ts` profile update + `admin.routes.ts` user-update. | `backend/src/lib/db-errors.ts` (new), `backend/src/routes/{auth,users,admin}.routes.ts` |
| 37.7 | **Pagination NaN guards.** `GET /schools/students` + `GET /schools/registrations` ran `parseInt(req.query.page)` and `parseInt(req.query.limit)` without NaN-bounding. A malformed query (`?page=abc`) poisoned the SQL `LIMIT/OFFSET` and 500'd. Wrapped both with `Math.max(1, parseInt(...) || 1)` + `Math.min(200, …)`. Same shape adopted in `GET /admin/users` pagination. | `backend/src/routes/{schools,admin}.routes.ts` |
| 37.8 | **UX audit + WCAG AA hardening pass.** Three parallel Explore agents audited backend constraints, web toast-error sources, and shared-component error handling. Synthesis: design-token contrast fix (`--muted-foreground` darkened from `#6b6480` (4.17:1) to `#5a5067` (5.87:1, passes AA on lavender bg); a global `@media (prefers-reduced-motion: reduce)` rule that snaps every animation/transition to ~instant (WCAG 2.3.3); dark-mode body radial-gradient parity; `<Input>` + `<Dialog>` close-button focus rings switched to `focus-visible:` and bumped to `ring-ring/75` for AA 3:1 contrast; `<Pager>` wrapped in `<nav aria-label="Pagination">` with `aria-current="page"` + Prev/Next `aria-label`. Page-level: login password validation 6 → 8 (matched backend); dead "Remember me" checkbox removed (state never made it into the POST body); phone inputs gained `inputMode="tel"` + dual-format hint; payment polling block got `role="status" aria-live="polite"`; certificate status badges gained ShieldAlert/CheckCircle2 icons (no longer color-only); profile photo `alt=""` → `alt="Profile photo"`; documents upload added client-side 10 MB guard; KPI card + organizer dashboard link cards got `focus-visible:` rings + focus-mirrored hover-lift; admin participants rejection-reason textarea gained `aria-invalid` when empty + a hint about being emailed to student; q-bank paper-exams table dropped `truncate` from `<TableHead>` + made clickable rows keyboard-operable (`role="button" tabIndex={0}` + Enter/Space handler); q-bank exams delete confirm enriched with exam name + consequence + "cannot be undone". | `web/app/globals.css`, `web/components/ui/{input,dialog}.tsx`, `web/components/shell/pager.tsx`, `web/app/{page,(competitions)/competitions/[slug]/{register,pay,certificates}/page,(account)/account/{profile,documents}/page,(dashboard)/dashboard/page,(organizer)/{organizer-dashboard,participants}/page,(competitions)/competitions/page,(question-bank)/question-bank/{paper,exams}/page}.tsx` |
| 37.9 | **Navigation IA refresh — unified shell + per-role reorg.** **AppShell rewrite** (`web/components/shell/app-shell.tsx`): user identity moved out of the sidebar footer into a **top-bar avatar dropdown** (full name + email + Account Settings link + Sign out). Sidebar is nav-only now. **Impersonation banner** moved from root layout to **inside `SidebarInset`** (renders above the sticky header) so it pushes content down naturally — no more overlay over the brand mark. Banner CSS dropped its `sticky top-0 z-[100]` for a `border-b` divider. **Per-role IA**: Admin grouped as Overview / Competitions (incl. **`Test Centers`** — renamed from Venues) / People / Commerce (Revenue moved here from organizer per brief) / Marketing; Organizer trimmed to Dashboard + Workspace (My Competitions / Participants / Question Bank), **Revenue removed**; School Admin and Teacher rebuilt with Dashboard / Roster / Track / Reports (same shape for muscle-memory); Country Rep mirrors the school-admin shape; Student/Parent get **All Competitions** (their home) first, then My Account underneath. Parent role parity: `(account)` layout gate now allows `parent` alongside `student`, and the catalog page merged parent + student render paths — both roles now see the full `AppShell` sidebar. | `web/components/{shell/app-shell,impersonation-banner}.tsx`, `web/app/layout.tsx`, `web/app/(dashboard)/layout.tsx`, `web/app/(organizer)/layout.tsx`, `web/app/(school)/layout.tsx`, `web/app/(account)/layout.tsx`, `web/app/(country-rep)/layout.tsx`, `web/app/(competitions)/competitions/{page,[slug]/layout}.tsx` |
| 37.10 | **Account Settings dropdown + universal `/account/profile`.** Top-bar user dropdown's "Profile" link renamed to **"Account Settings"** (separator + Sign out below); new optional `profileHref` prop on AppShell so every portal can pass it (all 7 shells do now). `/account/profile` is no longer student-only — `(account)/layout.tsx` gate loosened from `student/parent` to "any authenticated user". Operators (admin/organizer/school admin/teacher/rep) get a slimmer sidebar (a single "Back to portal" item pointing to their role home via `homeForRole()`); students/parents keep the full Browse + My Account sidebar. The profile page (`web/app/(account)/account/profile/page.tsx`) reads `useCompetitionAuth().user.role` and **hides Student card + School details + Supervisor + Parent sections** when `role !== 'student'` so operators only see Photo + Personal Details (which match the columns that actually exist on a non-student `users` row). | `web/components/shell/app-shell.tsx`, `web/app/(account)/{layout,account/profile/page}.tsx`, all 7 portal layouts |
| 37.11 | **Users admin CRUD (Edit + Assign).** Admin Users page (`/users`) previously had read-only browse + a super-admin-only Impersonate button. Now: new **School** column (LEFT JOIN of `users.school_id → schools.name`), **Edit** button per row (any admin), Impersonate button moved to secondary action. New `<UserEditDialog>` is role-aware: every role edits Full name + Phone, **student/teacher/school_admin** get a school-picker dropdown sourced from `/admin/schools`, **parent** gets a linked-students list (active `parent_student_links`) with per-row Remove + Add-by-email inline form, **country_representative** sees an info card linking to `/country-reps` (multi-comp/country assignments live there). Backend additions in `admin.routes.ts`: `GET /api/admin/users/:id` (detail incl. linked students), `PUT /api/admin/users/:id` (fullName, phone with `toLocalPhone`, schoolId — constraint errors translated via `dbErrorResponse`, `audit()`-wrapped), `POST /api/admin/users/:parentId/student-links` (admin-side parent↔student link, no PIN required), `DELETE /api/admin/users/:parentId/student-links/:studentId`. `GET /api/admin/users` list now returns `school_id` + `school_name` so the table renders without a per-row fetch. **Country-rep multi-school** intentionally **deferred** — current schema (`country_representatives(user_id, comp_id, country)`) tracks country×comp pairings, not school×rep; needs a new association table that wasn't urgent. | `backend/src/routes/admin.routes.ts`, `web/components/user-edit-dialog.tsx` (new), `web/app/(dashboard)/users/page.tsx`, `web/lib/api/index.ts`, `web/types/index.ts` |
| 37.12 | **School-pick blocking modal for unassigned teachers/admins.** `(school)/layout.tsx` detects `!user.school_id` for `teacher` / `school_admin` and gates the entire portal behind a non-dismissible `<SelectSchoolModal>` (no close button, ESC + outside-click ignored). Searchable list of verified schools, single selection, **danger-styled** "Confirm school assignment" button with warning copy ("After confirming, only an administrator can change this"). On confirm → `POST /api/users/me/select-school` (self-service: rejects 409 if caller already has a school, 400 if target isn't verified, 403 for non-teacher/school_admin) → hard nav reloads so `/auth/me` re-hydrates with the new `school_id` and the modal naturally hides. New helper endpoint `GET /api/schools/verified` (authed) — lightweight `id/name/npsn/city/province` list for the picker. `auth.routes.ts:fetchUserWithRole` now returns top-level `school_id` for every role so the gate is trivial on the client. | `backend/src/routes/{auth,users,schools}.routes.ts`, `web/app/(school)/layout.tsx`, `web/components/select-school-modal.tsx` (new) |

**Operational learnings stored as memory** (`~/.claude/projects/-Users-ululilmi-Documents-09-Dev-Projects-Dev-competzy/memory/`): "No SSH to prod — devops teammate handles" (the auto-mode classifier enforces this boundary even when phrased as "ok ssh"; user opts in by adding an explicit Bash permission rule or running the command themselves). Color palette rule remains: avoid Tailwind/AI-default palettes; user supplies hex codes (current brand: cyan family `#4BC2EC #4CBCBE #65C8DB` + warm `#F7B643 #BE65A9 #FEE404`).

**GitHub Linguist fix:** repo's Languages bar was showing HTML 56% (because `graphify-out/graph.html` is 3.3 MB of auto-generated knowledge-graph viewer). Added `.gitattributes` with `graphify-out/** linguist-generated=true` so the bar now reflects actual source code (TypeScript ~95%).

### SPRINT 37 — My-Account redesign + EMC/Komodo dashboard mockup match (June 3, 2026 — Session 18) ✅ BUILT (pending browser verification)

Mentor-driven redesign from a mockup (`competzy_v3.html`). Goal: **one shared student UI for all competitions, flow differs per competition.** Stacked PRs to `eduversal-team/competzy:main`, merge bottom-up: **#53 → #54 → #55**.

**Locked decisions (from the mentor + mockup):**
- **5-item sidebar** — All Competitions · Profile · My Competitions · My Achievements · Announcements. Documents/Records/Family → tabs under Profile; Notifications → top-bar bell; per-competition features → in-page tabs.
- **The Competzy sidebar does NOT change when you enter a competition** (was swapping to a per-comp menu). A competition's identity lives in the page **hero**, not the chrome.
- **Registration is a MODAL** on the dashboard (mockup's "Formulir Pendaftaran"), NOT a nav to `/account/profile` (the old behavior the mentor flagged as bad).
- **EMC hero keeps the real competition name "Eduversal Mathematics Competition"** (tricolor) per the mockup — this is the *competition's* name, distinct from the *platform* brand (Competzy). ⚠️ Nuances memory `feedback_brand_competzy_only` (platform stays Competzy; partner competition names are their real names). Confirm with the user if in doubt.
- **Web first**; mobile parity is a later wave. The mockup's "preview participant stage" switcher is omitted (real registration status drives the view).

| PR | Phase | What | Status |
|----|-------|------|--------|
| #53 | 1 | Shared 5-item `STUDENT_NAV` (`web/lib/nav/student-nav.ts`) on catalog + account; new `/account/achievements` (all certs via `/certificates/mine`) + `/account/announcements` (new `GET /api/announcements/mine` aggregator); Profile hub tabs (`web/components/account/account-tabs.tsx`); `AppShell` `NavItem.activePrefixes`. | ✅ pushed |
| #54 | 2 | `/competitions/[slug]/*` renders the same `STUDENT_NAV` (menu persists inside a competition); per-comp features → in-page `CompetitionTabs` (`web/components/competition/competition-tabs.tsx`). | ✅ pushed |
| #55 | 3 | Dashboard mockup match (all on this branch): **3a** branded hero · **3b** timeline status badges + active-stage highlight · **3c** path-progress bar · **3d** registration MODAL (`web/components/competition/registration-form-dialog.tsx`) replacing the profile-page nav · **3e** full hero (Eduversal tricolor + pill eyebrow + 4-stat row w/ grade) · **3f** dated-timeline groundwork (migration `1752200000000` adds `starts_on`/`ends_on`/`location` to `competition_flows` + API + frontend renders dates) · **3g** EMC custom **dated contest-stage flow** in `seed-test-competitions` (Registration → Simulation → R1/R2/R3 → Winners Announcement) + Stepper button wiring (form+pay on Registration, exams on round stages, certs on announcement) · **3h** two-column side panel (Next action + countdown · Competition path · Grade levels), dashboard widened to `max-w-5xl` · **3i** Komodo / multi-round two-column parity (`RoundsProgressCard` + creature card in the side panel). | ✅ pushed |

**Done — EMC + Komodo dashboards match the mockup (pending browser verification).** Build clean throughout.

**Operator/dev step to SEE the dated EMC timeline:** `pnpm -C backend db:migrate` (applies `1752200000000`) then `pnpm -C backend db:seed:test-competitions` (⚠️ TRUNCATEs competition data — re-seeds EMC's dated stages).

**Still open:**
- **Browser verification** of the whole redesign (mentor reviews via test accounts) — nothing has been eyeballed yet, only `next build`.
- The mockup's **"preview participant stage" switcher** is intentionally omitted (real registration status drives the view).
- **Mobile parity** (web-first was the agreed scope) — port the redesign to the app in a later wave.

### SPRINT 36 — Multi-currency round pricing + unified competition view + admin registrations overhaul (May 21, 2026 — Session 17) ✅ COMPLETE

QA-driven session — three independent bug-fix / UX-tightening tracks shipped on one branch.

| Task | What | Key files |
|---|---|---|
| 36.1 | **Multi-currency round pricing.** Migration `1751000000000_round-international-fee.sql` adds `competition_rounds.fee_international NUMERIC(10,2)` (USD, nullable — null means "no international price configured"). `RoundInput.feeInternational` plumbed through `competition-rounds.service.ts` (INSERT) and the read projections in `competitions.routes.ts` + `organizer.routes.ts` (added the column to the SELECT lists too — without that the value never round-tripped, which manifested as "save changes doesn't save" the moment the dialog was reopened). Web rounds builder gains a **"International fee (USD) — optional"** input next to the existing IDR field; IDR field re-labelled "Fee (IDR — local)". `db:seed:komodo` now seeds USD defaults — $15 for the 200k rounds, $40 for the 500k Bali Global. Student competition dashboard one-shots `GET /users/me` to read `country` and re-routes the per-round price: ID/blank → IDR (existing Midtrans flow); anything else → USD when the round has one, with the "Pay round fee" button replaced by *"International payment is offline for now. Contact the organizer to settle your $X USD fee."* — no Midtrans for non-IDR yet (decision: USD is display-only this iteration). | `backend/migrations/1751000000000_round-international-fee.sql` (new), `backend/src/services/competition-rounds.service.ts`, `backend/src/routes/{competitions,organizer}.routes.ts`, `backend/src/db/seed-komodo.ts`, `web/components/rounds-builder.tsx`, `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx` |
| 36.2 | **One competition view for everyone.** Previously the catalog linked students to `/competitions/[slug]` (a welcome landing page with a Register-now CTA), and only students with an existing registration silently redirected to `/dashboard` — so new students saw a different layout than returning ones. Decision: the dashboard already handles every state (no registration, pending payment, missed, paid, per-round multi-round flow), so the landing page becomes redundant. (1) Catalog cards now link straight to `/competitions/[slug]/dashboard` (`web/app/(competitions)/competitions/page.tsx:226`). (2) The landing page itself now silently redirects every signed-in student to the dashboard regardless of registration state (`web/app/(competitions)/competitions/[slug]/page.tsx:118`) — anonymous visitors are already bounced to login by the layout, so the landing page is effectively dead code now (kept as a redirect-only stub so any bookmarked link still lands somewhere sensible). | `web/app/(competitions)/competitions/page.tsx`, `web/app/(competitions)/competitions/[slug]/page.tsx` |
| 36.3 | **Admin registrations overhaul.** `/registrations` was an endless-scroll list with only a status-tab filter. Re-built to match the legacy site's affordances: (1) **Pagination** via the shared `<Pager>` (25 rows/page, total/page/totalPages returned by the API). (2) **Per-competition filter** dropdown (loads via `competitionsApi.list`). (3) **Per-year filter** dropdown (current year ± a few — `EXTRACT(YEAR FROM r.created_at)` on the backend). (4) **Debounced free-text search** (300 ms idle) across student name / email / phone / school / registration number. (5) **View action** on every row — opens a read-only dialog with the full registration detail (reg number, status, dates, contact, school, grade, NISN, country/city/province, local + international fees). (6) Row now also surfaces the **round name** + **registration number** under the competition name. Backend `GET /api/admin/registrations/pending` extended with `?page`, `?limit`, `?year`, `?search` params and returns `{ pendingRegistrations, pagination }`. Status tabs + Approve/Reject buttons preserved. | `backend/src/routes/admin.routes.ts`, `web/app/(dashboard)/registrations/page.tsx`, `web/lib/api/index.ts`, `web/types/index.ts` |
| 36.4 | **Rounds panel → catalog grid + Confirm-your-details dialog.** (a) The dashboard's rounds list went from a vertical stack to a responsive **catalog grid** (`grid sm:grid-cols-2 xl:grid-cols-3`) — 1 card on phones, 2 on tablets, 3 on desktops. Each card is now a fixed-height tile with a 2-column metadata table (Mode / Exam / Closes / Fee), the category chip + status pill at the top, and the CTA pinned to the bottom edge via `mt-auto` so every card's button aligns regardless of body length. (b) **Pre-payment profile review** — `GET /api/competitions/:id` now also returns `requiredProfileFields` (the full list from `competitions.required_profile_fields`). The dashboard fetches it together with the rounds, and the Register click now opens the profile dialog **proactively** with the *complete* required list — not only the missing subset — so the student can review every field with their current value pre-filled and edit anything wrong before continuing to payment. Dialog copy updated: title is now "Confirm your details", CTA is "Confirm and continue". The 409 PROFILE_INCOMPLETE fallback is kept defensively for the rare case where the required list changes between page-load and click. Comps with no required fields (EMC / ISPO / OSEBI) skip the dialog as before. | `backend/src/routes/competitions.routes.ts`, `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx`, `web/components/profile/profile-completion-dialog.tsx` |

### SPRINT 35 — Komodo profile gate + age model + manual entry + teacher parity + student dashboard (May 20–21, 2026 — Sessions 15–16) ✅ COMPLETE
| Task | What | Key files |
|---|---|---|
| 35.6 | **Follow-up — catalog sidebar + Komodo round backfill (May 21, 2026 — Session 16) ✅ COMPLETE.** Two QA-driven fixes. (1) **My Account sidebar on `/competitions`** — the Gen-Z dashboard at `/competitions` was missing the same My Account / Documents / Notifications / My Competitions / Records / Family + Browse → All competitions sidebar that wraps the per-competition portals and the `(account)/*` pages. The catalog page now wraps its student render in `AppShell` with the same `STUDENT_NAV`, matching the `(account)` layout; parents (also allowed in the catalog) keep the lightweight header since the My Account items are student-specific. (2) **`seed-komodo` truly idempotent** — it used to exit early when the Komodo competition row already existed, so on a server where Komodo was created via the admin UI without rounds (the symptom: `/competitions/komodo/dashboard` shows only the pending-payment status card, no rounds panel) re-running it did nothing. The script now keeps the comp-row idempotency but additionally **backfills the 6-step flow + the 6 rounds when either is missing** (counted by `competition_flows` + `competition_rounds`). When both are already present it logs `· left alone` and is a clean no-op — operator-edited dates / fees never get clobbered. Verified locally: re-running `npm run db:seed:komodo` is a no-op (`rounds already present`); pretending the rounds were missing populates them. `tsc` + `npm run build` clean. **Production rollout for arena.competzy.com:** ssh, then `docker exec <backend> npm run db:migrate && docker exec <backend> node dist/db/seed-komodo.js` — the first applies migrations 1750800000000 + 1750900000000 (Country + required-fields + age cutoff); the second backfills Komodo's rounds + flow. After that the rounds panel renders on `arena.competzy.com/competitions/komodo/dashboard`. | `backend/src/db/seed-komodo.ts`, `web/app/(competitions)/competitions/page.tsx` |
| 35.5 | **Phase 5 — Gen-Z student dashboard ✅ COMPLETE.** `/competitions` (the student post-login landing) is reborn as the dashboard while keeping the catalog. Hero with greeting + a profile-completion SVG ring + an indigo→pink gradient background with yellow/pink accent blobs. Four vibrant KPI tiles (Registrations / Certificates / Best score / Saved) cycling through the four palette colors with paired ink. A **"Continue where you left off"** card surfaces the most recent pending payment or unfinished exam session with a single CTA. The full competition catalog grid stays below (favorites toggle preserved, optimistic update + revert on failure). An **Achievements wall** at the bottom renders up to four recent certificates as gradient cards — yellow for achievement, indigo for participation — each linking to `/verify/[code]`. New endpoint **`GET /api/users/me/dashboard-summary`** returns the entire payload in one round-trip: counts (registrations / certificates via the Wave-12 `certificates` table / saved favorites), the student's all-time best `registrations.score` joined to its competition + round, a `continueTask` (first `pending_payment` registration → fallback to the latest unfinished `sessions` row), and the four most-recent live certificates with their `verification_code`. Non-student callers safely get empty counts. Saved KPI re-fetches the summary after toggling a favorite so the tile stays in sync. Verified curl: `/users/me/dashboard-summary` returned counts (2 regs / 0 certs / 0 saved), a Komodo continueTask, and empty arrays. `tsc` + `npm run build` clean. | `backend/src/routes/users.routes.ts`, `web/app/(competitions)/competitions/page.tsx` |
| 35.4 | **Phase 4 — Teacher feature parity ✅ COMPLETE.** Teachers gain the same registrations table, status tabs, and PDF exports school-admins have — scoped to their `teacher_student_links` roster instead of the whole school. New `GET /api/teachers/registrations` — shape parity with `/schools/registrations` (status tabs, page, status + compId filters), so the web `/school-registrations` page just swaps the endpoint when the caller is a teacher and reuses the existing tabs / filters / pager. New `GET /api/teachers/export/registrations/pdf` + **`GET /api/teachers/export/achievement.pdf`** (PDFKit) — same layout as the school exports, roster-scoped via `teacher_student_links`. Bulk processor (`bulk-processor.service.ts`) now reads the job uploader's role and, when the uploader is a `teacher`, auto-links each created / matched student into `teacher_student_links` (idempotent `ON CONFLICT DO NOTHING`) so bulk-registering through the teacher portal populates the My Students roster automatically. `/payments/school-batch` was already roster-scoped for teachers (Sprint 33). Web `/school-dashboard` teacher quick-links grew **Bulk Registration**, **Bulk Payment**, and **Achievement PDF** tiles for full parity with the school-admin layout. Verified curl: `/teachers/registrations` returned 6 roster-scoped rows with the `statusTabs` array; the Achievement PDF endpoint returned `200 OK`. `tsc` + `npm run build` clean on both subdirs. | `backend/src/routes/teachers.routes.ts`, `backend/src/services/bulk-processor.service.ts`, `web/app/(school)/{school-registrations,school-dashboard}/page.tsx` |
| 35.3 | **Phase 3 — Excel-paste manual student entry ✅ COMPLETE.** Bulk registration was CSV-only — adding one student forced operators to produce a CSV file. Now `/bulk-registration` has a **two-mode** Step 2 — `Upload CSV` (the existing flow, untouched) and `Enter manually` (new). New backend endpoint **`POST /api/bulk-registration/manual`** — accepts `{ compId, rows: [{ fullName, email, phone?, nisn?, grade?, schoolName? }] }`, validates each row (full name + valid email required), serialises to the same envelope shape the cron expects, inserts a `bulk_registration_jobs` row, and lets the existing background processor pick it up unchanged. 500-row cap, 3/hour rate limit, teacher + school-admin only. New `<ManualEntryGrid>` (`web/components/bulk/manual-entry-grid.tsx`) — six editable columns (Full name *, Email *, WhatsApp, NISN, Grade, School name) with inline per-cell validation (red ring on bad email, amber row tint on incomplete), Add row + per-row trash controls, and the **headline feature**: a multi-cell paste handler that intercepts any tab- or newline-separated clipboard text and spreads it into the grid starting at the focused cell, growing rows as needed. Matches the legacy EMC entry experience. Page polling + Step 3/4 UI reused unchanged — both modes write to the same job table. Verified curl: POST 2 rows as `teacher@test.local` → 201 `pending` with `jobId`. tsc + build clean. | `backend/src/routes/bulk-registration.routes.ts`, `web/components/bulk/manual-entry-grid.tsx` (new), `web/app/(school)/bulk-registration/page.tsx` |
| 35.2 | **Phase 2 — Komodo age cutoff + creatures ✅ COMPLETE.** Komodo groups students by **age** at a per-round cutoff date, not by grade. Migration `1750900000000_round-age-cutoff.sql` adds `competition_rounds.age_cutoff_date DATE` (nullable; ignored by grade-based comps) and backfills Komodo Round 1 → `2026-09-19` (the public Komodo page's 2026/2027 Round 1 cutoff). New `backend/src/services/komodo-creature.service.ts` exposes `ageOn(dob, cutoff)` + `classifyCreature(dob, cutoff)` returning one of five brackets — **gecko** (≤ 8), **salamander** (8 < age ≤ 10), **chameleon** (10 < age ≤ 12), **iguana** (12 < age ≤ 15), **dragon** (15 < age ≤ 18) — with the public `competzy.com/images/Komodo/{name}.webp` artwork URLs (gecko falls back to the salamander photo with `placeholder:true` until that asset ships). New endpoint **`GET /api/competitions/:id/my-creature`** (authed, any role) — for each round of the competition that carries an `age_cutoff_date`, returns the caller-scoped creature classification (`{ roundId, roundName, ageCutoffDate, creature: { key, name, ageRange, photoUrl, placeholder, ageAtCutoff } | null, missingDob }`). `competition-rounds.service.ts` `RoundInput` accepts `ageCutoffDate`; `replaceRounds` writes it; the operator-facing `GET /api/competitions/:id` + `GET /api/organizers/competitions/:id` round projections expose it. Web: `rounds-builder.tsx` grew an **Age cutoff date** input below the existing date fields (with an inline hint explaining it's age-grouped-only), `RoundDraft` + `roundsToDrafts` + `draftsToPayload` round-trip the field. New `web/lib/competitions/komodo-creatures.ts` mirrors the 5-creature registry on the frontend (key, name, age range, photo, tagline) so cards render without an extra fetch. New `<CreatureCard>` (`web/components/profile/creature-card.tsx`) — gradient indigo hero with the primary round's creature photo, name, age band, and tagline; a compact list of any additional rounds below; **"Add your date of birth"** CTA when the student has no DOB; **"Out of bracket"** badge when the computed age is outside 0–18. The competition dashboard fetches `/my-creature` on mount and renders the card at the top of the multi-round panel for any competition with an age-cutoff round. Verified end-to-end (curl): test student DOB `2014-05-01` + Round 1 cutoff `2026-09-19` → age 12 → **Chameleon** with the correct photo URL. `tsc` + `npm run build` clean. | `backend/migrations/1750900000000_round-age-cutoff.sql` (new), `backend/src/services/{komodo-creature,competition-rounds}.service.ts`, `backend/src/routes/{competitions,organizer}.routes.ts`, `web/components/rounds-builder.tsx`, `web/lib/competitions/komodo-creatures.ts` (new), `web/components/profile/creature-card.tsx` (new), `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx` |
| 35.1 | **Phase 1 — Komodo pre-payment profile gate ✅ COMPLETE.** Migration `1750800000000_student-country-and-required-fields.sql` — `users.country TEXT` (nullable ISO 3166-1 alpha-2) + `competitions.required_profile_fields JSONB NOT NULL DEFAULT '[]'`; seeds Komodo with its 9 mandatory keys (`fullName`, `email`, `phone`, `dateOfBirth`, `city`, `country`, `supervisorName`, `supervisorEmail`, `schoolName`). Backend `readiness.service.ts` gains `computeMissingProfileFields(userId, keys)` + `getRequiredProfileFields(compId)`; `computeCompleteness()` layers the per-competition extras on top of the baseline. `POST /api/registrations` runs the field check **before** the INSERT and returns **`409 { code: "PROFILE_INCOMPLETE", missingFields }`** when any are blank — empty `required_profile_fields` (EMC / ISPO / OSEBI) skip the gate. `GET/PUT /api/users/me` round-trip the `country` field (validated as a 2-letter ISO code, uppercased on write, 400 on malformed). Web `lib/api/client.ts` exports a new `HttpError` extending `Error` that carries `.status` + `.body` so callers can branch on `409 PROFILE_INCOMPLETE`. New `web/lib/constants/countries.ts` — full ISO list with flag emoji, ten priority countries (ID/MY/SG/PH/TH/VN/IN/AE/US/UK) surfaced on top. New `<CountrySelect>` — searchable popover-style picker with keyboard nav, click-outside + Esc, no extra deps. New `<ProfileCompletionDialog>` — driven by the `missingFields[]` array; pre-fetches `/users/me` to pre-fill known values; PUTs the gaps to `/users/me`; signals the caller to retry the registration. The competition dashboard's `enrollNow` / `enrollRound` handlers intercept the 409, open the dialog with a context label (e.g. `Komodo — Online Round 1`), and re-POST after the save. `/account/profile` gains the Country field in Personal Details. Verified end-to-end (curl): test student missing 4 of 9 Komodo fields → 409 with those 4 keys; PUT fills them; retry → 201 `pending_payment`; ISPO regression registers straight through with no gate. `tsc` + `npm run build` clean. | `backend/migrations/1750800000000_student-country-and-required-fields.sql` (new), `backend/src/services/readiness.service.ts`, `backend/src/routes/{users,registrations}.routes.ts`, `web/lib/api/client.ts`, `web/lib/constants/countries.ts` (new), `web/components/ui/country-select.tsx` (new), `web/components/profile/profile-completion-dialog.tsx` (new), `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx`, `web/app/(account)/account/profile/page.tsx`, `web/types/account.ts` |

### SPRINT 0 — Quick Wins ✅ COMPLETE
| Task | Status | What |
|---|---|---|
| T1 | ✅ | Rename app "Beyond Classroom" → "Kompetix" → "Competzy" (final rebrand May 8, 2026) |
| T2 | ✅ | Remove admin screens from mobile — redirect admin users to web portal |
| T3 | ✅ | Fix bulk processor: check fee, email temp pwd, insert school_name |
| T4 | ✅ | Fix quota race condition in bulk processor |

### SPRINT 1 — Critical Backend Foundations ✅ COMPLETE
| Task | Status | What |
|---|---|---|
| T5 | ✅ | Add `registration_number` column (originally `KMP-2026-XXXXX`; rebranded to `CTZ-2026-XXXXX` on May 8, 2026) |
| T6 | ✅ | Add `profile_snapshot` JSONB column to registrations |
| T7 | ✅ | Add `organizer` role + `organizers` table + `organizerOnly` middleware |

### SPRINT 2 — Organizer Backend Routes ✅ COMPLETE
| Task | Status | What |
|---|---|---|
| T8 | ✅ | All 12 organizer endpoints in `backend/src/routes/organizer.routes.ts` |

### SPRINT 3 — Payment Fixes ✅ COMPLETE
| Task | Status | What |
|---|---|---|
| T9 | ✅ | Post-payment JWT redirect endpoint (`GET /api/payments/redirect/:registrationId`) |
| T10 | ✅ | VA expiry: reset registration to `registered` on webhook expire event |

### SPRINT 4 — Historical Data ✅ COMPLETE (data imported)
| Task | Status | What |
|---|---|---|
| T11 | ✅ | `historical_participants` table migration — deployed |
| T12 | ✅ | Excel import script — 63,365 records imported into DB |
| T13 | ✅ | Auto-link at login (email + phone match, fire-and-forget) |
| T14 | ✅ | Claim system API: GET /my-records, GET /search, POST /:id/claim, POST /:id/unclaim |

### SPRINT 5 — Mobile Polish ✅ COMPLETE
| Task | Status | What |
|---|---|---|
| T15 | ✅ | "Recommended for you" horizontal scroll in competitions screen (was already implemented) |
| T16 | ✅ | `CTZ-2026-XXXXX` badge on My Competitions cards (legacy rows show `KMP-2026-XXXXX`) |
| T17 | ✅ | Profile snapshot section + "Open Competition Platform" redirect button in competition hub |
| T18 | ✅ | New screen `profile/history.tsx` — My Records tab + Find & Claim tab |
| T19 | ✅ | Teacher actions: removed in-app bulk CSV, added web portal banner |
| T20 | ✅ | `canAccessRegistration()` in payments.routes.ts — parents can pay for linked children |

### SPRINT 6 — File Storage Migration ✅ CODE COMPLETE (T21 VPS pending)
| Task | Status | What |
|---|---|---|
| T21 | ⏳ VPS manual | MinIO Docker setup on VPS — see instructions in NEXT STEP section above |
| T22 | ✅ | `storage.service.ts` rewritten with S3/local dual-mode; multer → memoryStorage in all 3 upload routes |

### SPRINT 7 — Phase 2 (after Phase 1 launch)
| Task | Status | What | Files |
|---|---|---|---|
| T23 | ✅ | school_payment_batches table + bulk pay endpoint | `1746300000000_school-payment-batches.sql` + `payments.routes.ts` |
| T24 | ✅ | Add referral_code column to registrations | `1746400000000_add-referral-code.sql` + `registrations.routes.ts` |
| T25 | ✅ | Admin refund endpoint | `midtrans.service.ts` + `admin.routes.ts` |

### SPRINT 9 — Web Portal QA Pass (May 6, 2026) ✅ COMPLETE
| Fix | What | Key files |
|---|---|---|
| DB rename | PostgreSQL `beyond_classroom` → `kompetix` locally; `.env` + `.env.example` updated | `backend/.env`, `backend/.env.example` |
| Role selector | `/` is now a role-selector landing page (Admin / Organizer / Teacher-soon) instead of redirect | `web/app/page.tsx` |
| Back buttons | `← Back` on both login pages to return to role selector | `web/app/login/page.tsx`, `web/app/(organizer)/organizer-login/page.tsx` |
| School column fix | `s.school` → `s.school_name` in all 6 queries across admin & organizer routes | `backend/src/routes/admin.routes.ts`, `backend/src/routes/organizer.routes.ts` |
| Organizer competitions | Assigned all 21 NULL-owned competitions to organizer account; `POST /admin/competitions` now sets `created_by` | `backend/src/routes/admin.routes.ts` |
| Admin registrations | Page now shows all statuses with filter tabs (All/Pending/Registered/Paid/Rejected) | `web/app/(dashboard)/registrations/page.tsx`, `backend/src/routes/admin.routes.ts` |
| Organizer login redirect | Fixed wrong redirect `/organizer/organizer-dashboard` → `/organizer-dashboard` | `web/app/(organizer)/organizer-login/page.tsx` |
| Participants status | Fixed STATUS_CLS map and approve/reject button condition (was using wrong statuses) | `web/app/(organizer)/participants/page.tsx` |
| Missing API methods | Added `competitionsApi.create/update/delete` to web API client | `web/lib/api/index.ts` |
| Null guards | Defensive `?? []` / `?? 0` on users/schools pagination state setters | `web/app/(dashboard)/users/page.tsx`, `web/app/(dashboard)/schools/page.tsx` |
| Revenue page | New organizer revenue page with stat cards + per-competition table | `web/app/(organizer)/revenue/page.tsx` |
| Dead code | Removed unused OrganizerSidebar component from organizer layout | `web/app/(organizer)/layout.tsx` |
| Types | PendingRegistration student sub-fields marked optional | `web/types/index.ts` |

### SPRINT 10 — Mobile Bug Fixes (May 6, 2026 Session 2) ✅ COMPLETE
| Fix | What | Key files |
|---|---|---|
| Payment: "Kembali" → "Back" | Indonesian string hardcoded in pay.tsx | `app/app/(payment)/pay.tsx` |
| Payment: webhook race condition | After paying and closing browser, `refreshRegistrations()` was called before the Midtrans webhook updated the DB. Fix: poll `GET /registrations/:id` up to 4× (every 2s, max ~6s) before giving up. Screen shows "Verifying payment status..." during poll. | `app/app/(payment)/pay.tsx` |
| Payment: "Another web browser is already open" | iOS keeps previous `openAuthSessionAsync` session "alive" briefly. Fix: call `WebBrowser.dismissAuthSession()` before every `openAuthSessionAsync` call. | `app/app/(payment)/pay.tsx` |
| Teacher sees Discover screen on login | `_layout.tsx` defaulted `userRole` to `"student"` before context loaded → competitions tab became visible → tab navigator landed there. Fix: default to `""` (empty string) so all role-specific tabs stay hidden during load. | `app/app/(tabs)/_layout.tsx` |
| Teacher redirect safety net | `competitions.tsx` also defaulted to "student" and had no redirect for teachers. Fix: added `useEffect` that redirects teachers to `/(tabs)/teacher-dashboard` and admins to `/(tabs)/web-portal-redirect` when `userRole` resolves. | `app/app/(tabs)/competitions.tsx` |
| Teacher "monitoring mode" | Removed Bulk Registration and Export Student Data from teacher quick actions. Replaced with monitoring-only tiles: Competitions, View Reports, Deadlines, My Students. Updated web portal banner text to remove bulk registration mention. | `app/app/(tabs)/teacher-actions.tsx` |

### SPRINT 13 — Production Infra Templates (May 9, 2026 Session 5) ✅ LOCAL ARTIFACTS DONE
| Item | What | Files |
|---|---|---|
| Backend container | Multi-stage `node:20-alpine`, tini, non-root, healthcheck `/api/health`, volume `/app/uploads` | `backend/Dockerfile`, `backend/.dockerignore` |
| Web container | Multi-stage Next.js standalone (`output: 'standalone'`), healthcheck `/`, build-arg `NEXT_PUBLIC_API_URL` | `web/Dockerfile`, `web/.dockerignore`, `web/next.config.mjs` |
| Coolify guide | Step-by-step deploy on <PROD_HOST> (Coolify hybrid + host nginx): Postgres + MinIO + backend + web, env var map, migrations, certbot, troubleshooting | `deploy/COOLIFY.md` |
| nginx host config | Server-block template for `api.competzy.com` (`:3010`), `arena.competzy.com` (`:3011`), `minio.arena.competzy.com` (`:9000`) — reverse-proxies host nginx into Coolify-exposed container ports; certbot fills the SSL halves | `deploy/nginx.conf` |
| Expo build config | development/preview/production profiles; production sets EXPO_PUBLIC_API_URL=`https://api.competzy.com/api` | `app/eas.json` |
| k6 load test | 500-VU ramp testing signup → /me → /competitions → POST /registrations; thresholds p95<2s, error<2% | `loadtest/k6-registration.js`, `loadtest/README.md` |
| Runbook | Common incident playbooks (API down, payment stuck, signed-URL 403, soft-delete recovery, audit-log forensics, rollback). **Note:** deploy steps in this file pre-date the Coolify migration — see `deploy/COOLIFY.md` for current rollout. | `docs/RUNBOOK.md` |
| _Retired 2026-05-18, restored 2026-05-18_ | `deploy/pm2.config.js` stays deleted (Coolify supervises containers). `deploy/nginx.conf` was deleted, then restored later same day after server inspection found the target host runs **host nginx + certbot** as the public proxy (no Traefik). | — |

### SPRINT 19 — Gen Z Playful Redesign + English/Back-Nav Mop-up (May 12, 2026 Session 7) ✅ COMPLETE
| Task | What | Key files |
|---|---|---|
| 19.1 | **Design tokens overhauled** — `Brand` swapped to vibrant purple (`#6F4FE8`) + navy (`#1E2A78`) + sunshine (`#F8D24A`) + coral (`#F47B5A`) + mint (`#7BD389`) + sky (`#C5D8FF`) + lavender background (`#FAF8FF`). `Radius` scale bumped (sm→10, lg→18, 2xl→28, 3xl→36, new 4xl→44). New `Shadow.playful` clay halo using `Brand.primary`. Display weights bumped to 800/900. Added `subjectColorFor()` deterministic palette picker and `Brand.{navy,sunshine,coral,mint,sky}*` aliases. | `app/constants/theme.ts` |
| 19.2 | **Shared playful primitives** — new `SubjectCircle` (colored disk with subject letter, used wherever competitions are listed), `StatTile` (label + value + icon in pastel-tinted clay block), `GeometricHeader` (SVG hero with overlapping coral/sunshine/navy shapes — Profile uses this). `Card` gains `variant="playful"` (Radius 3xl + Shadow lg). `EmptyState` gains `icon` + `tint` props for vector-icon support. `Button` primary uses `Shadow.playful` + 0.97 press-scale. | `app/components/ui/{SubjectCircle,StatTile,GeometricHeader}.tsx`, `app/components/ui/{Card,Button,EmptyState,index}.ts*` |
| 19.3 | **Screen redesigns** — Discover (chunky search, SubjectCircle on cards, vibrant category chips with letter-disks), My Competitions (Ionicon tab bar, color-coded status), Profile (GeometricHeader purple/coral/sunshine, StatTile row for Total/Active/Done, pastel-tinted menu icon disks, "Keep Learning ✨" footer), Competition Detail (white-halo hero with sunshine accent + 4xl bottom corners, `variant="playful"` section cards), Registration Details (hero gets sunshine + coral blobs, all cards `playful`), Onboarding (Ionicons hero on layered blobs, larger 168×168 icon tile), Notifications (icon-tinted disks, status-coloured left border via `accentColor`). All emoji usages converted to `@expo/vector-icons/Ionicons`. | `app/app/(tabs)/{competitions,my-competitions,notifications,my-registration-details,profile/index,competitions/[id]}.tsx`, `app/app/(onboarding)/index.tsx` |
| 19.4 | **Sprint 18 leakage mop-up** — fixed strings that escaped the English pass: `"Approved / Bergabung"` → `"Joined"`, `"Lokasi/Platform"` → `"Location/Platform"`, `compName ?? "Kompetisi"` → `"Competition"`, `"Round & Jadwal"` → `"Round & Schedule"`, `"Completedkan pembayaran…"` → `"Complete the payment…"`, `"✓ Bergabung"` → `"✓ Joined"`, `"Kategori:"` filter → `"Category:"`, `"About Kompetisi"` → `"About Competition"`, `Categories` heading. Date locale `id-ID` → `en-US` in 5 more files (my-registration-details, notifications, competitions, competitions/[id], profile/document-vault). Currency stays `id-ID` (period thousand separator). | `app/app/(tabs)/my-registration-details.tsx`, `app/app/(tabs)/my-competitions.tsx`, `app/app/(tabs)/competitions.tsx`, `app/app/(tabs)/competitions/[id].tsx`, `app/app/(tabs)/notifications.tsx`, `app/app/(tabs)/profile/document-vault.tsx`, `app/app/(tabs)/profile/history.tsx` |
| 19.5 | **Back-nav holdouts unified** — Sprint 18.4 unified back affordance on 4 screens; 5 stragglers still had `"← Back"` text links. Replaced with circular-chevron `ScreenHeader` on `profile/edit`, `profile/document-vault`, `teacher-actions`, and both wizard steps in `(auth)/register` (the multi-step register keeps `setStep("role")` as the back action instead of `router.back()`). Pay screen's footer cancel link is intentionally left as text (it's not a header). Dead `backButton`/`backButtonText`/`title`/`header` styles removed. | `app/app/(tabs)/profile/edit.tsx`, `app/app/(tabs)/profile/document-vault.tsx`, `app/app/(tabs)/teacher-actions.tsx`, `app/app/(auth)/register.tsx` |

### SPRINT 34 — Web Student Feature Parity ("My Account" area) (May 19, 2026 — Session 14) ✅ COMPLETE

Brings the **web** portal to student feature parity with the mobile app. The web was a per-competition portal that punted profile/documents/notifications to the app ("Complete your profile in the Competzy app to move forward."). This adds a global `/account/*` workspace — a new `web/app/(account)/` route group — so a student can do on the web everything the app already offers: edit their profile, upload documents, read notifications, see My Competitions + favorites, claim historical records, link a parent. **Web-only** — the backend already has every endpoint, the app already has every feature, and both share one backend + DB, so data syncs automatically. Decisions: a **global "My Account" area** (not per-competition); **all four** feature areas in scope. Plan: `/Users/mujtabo/.claude/plans/image-1-why-witty-star.md`. Stacked feature branches off `feature/multi-round-competitions`, one PR per phase.
| Task | What | Key files |
|---|---|---|
| 34.6 | **Phase 5b — parent linking — SPRINT 34 COMPLETE.** New `/account/family` — invite a parent / guardian by email (`POST /api/parents/invite-parent`; the parent receives a 6-digit PIN, and a dev-mode `debugPin` is surfaced when SMTP delivery is off) + a **Parent requests** list (`GET /api/parents/pending-invitations`) where the student approves / declines a parent who has entered the PIN (`PUT /api/parents/links/:linkId/approve` `{status:'active'|'rejected'}`). No student-facing "list active linked parents" endpoint exists, so v1 shows pending requests only — a possible future backend add. **Family** added to the My Account sidebar. Verified: `tsc` clean, `npm run build` passes (`/account/family` route). Shipped on `feature/web-account-family`. | `web/app/(account)/account/family/page.tsx` (new), `web/app/(account)/layout.tsx` |
| 34.5 | **Phase 5a — historical records.** New `/account/records` — a `Tabs` page: **My records** (`GET /api/historical/my-records` — the student's claimed legacy competition results, each with a "Remove" / unclaim action `POST /api/historical/:id/unclaim`) and **Find & claim** (a name [≥3 chars] / school / competition search → `GET /api/historical/search`, each result claimable via `POST /api/historical/:id/claim`, 409 on already-claimed). **Records** added to the My Account sidebar. Verified: `tsc` clean, `npm run build` passes (`/account/records` route). Shipped on `feature/web-account-records`. | `web/app/(account)/account/records/page.tsx` (new), `web/app/(account)/layout.tsx` |
| 34.4 | **Phase 4 — My Competitions + favorites.** New `/account/competitions` — a `Tabs` page: **Registered** (the student's registrations from `GET /api/registrations`, deduped per competition + joined to `GET /api/competitions` for name / organizer / portal link, with status badges) and **Saved** (`GET /api/favorites`, un-save inline). The `/competitions` catalog gains a **heart toggle** on every card — optimistic `POST` / `DELETE /api/favorites` (`:compId`), with `preventDefault` + `stopPropagation` since the card is wrapped in a `<Link>`. **My Competitions** added to the My Account sidebar. Verified: `tsc` clean, `npm run build` passes (`/account/competitions` route). Shipped on `feature/web-account-competitions`. | `web/app/(account)/account/competitions/page.tsx` (new), `web/app/(account)/layout.tsx`, `web/app/(competitions)/competitions/page.tsx` |
| 34.3 | **Phase 3 — notifications inbox.** New `/account/notifications` — the student's notification list (`GET /api/notifications`, paginated 30/page with a "Load more"; the API has no grand total so a full page implies more), per-item mark-read (click the row) + delete, a "Mark all read" header action — over `POST /api/notifications/:id/read` · `/read-all` · `DELETE /api/notifications/:id`; type-mapped icons + an unread tint + dot. `AppShell` gains an optional `notificationsHref` prop — when set the top-bar bell becomes a `Link` (admin/organizer portals pass nothing, so their bell stays inert); both the My Account shell and the per-competition portal shell point it at `/account/notifications`. **Notifications** added to the My Account sidebar. Verified: `tsc` clean, `npm run build` passes (`/account/notifications` route). Shipped on `feature/web-account-notifications`. | `web/app/(account)/account/notifications/page.tsx` (new), `web/components/shell/app-shell.tsx`, `web/app/(account)/layout.tsx`, `web/app/(competitions)/competitions/[slug]/layout.tsx` |
| 34.2 | **Phase 2 — document vault.** New `/account/documents` — a `docType` picker + file upload (`POST /api/documents/upload`, multipart `file` + `docType`), a table of the student's documents (`GET /api/documents`) with download + a delete-confirm dialog; the list is re-fetched after every mutation since `getSignedUrl` links expire in 15 min. `web/next.config.mjs` gains a `/uploads-signed/*` rewrite so signed document downloads resolve on the web in dev (only `/api` + `/uploads` were proxied before). The competition dashboard stepper's `documents` step stops punting to the app — an inline **Upload documents** button links to `/account/documents`; **Documents** added to the My Account sidebar. Verified: `tsc` clean, `npm run build` passes (`/account/documents` route). Shipped on `feature/web-account-documents`. | `web/app/(account)/account/documents/page.tsx` (new), `web/app/(account)/layout.tsx`, `web/next.config.mjs`, `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx` |
| 34.1 | **Phase 1 — `(account)` shell + profile editor.** New `web/app/(account)/` route group — `layout.tsx` mounts the reused `CompetitionAuthProvider` inside an `AppShell`, student-gated (non-students bounce to `/`). New `/account/profile` page fetches `GET /users/me` (the auth context only carries a snapshot) and renders the full sectioned student form — photo + student-card upload via `emcHttp.postFormData('/users/photo'|'/users/student-card')`, Personal Details + interest chips, School / Supervisor / Parent — saving via `PUT /users/me`; mirrors the app's `profile/edit.tsx`. The competition dashboard stepper's `profile` step no longer punts to the app — the hint is rewritten and an inline **"Complete your profile"** button links to `/account/profile`; a **My Account** link added to the competition portal sidebar + the `/competitions` catalog header. Verified: `tsc` clean, `npm run build` passes (`/account/profile` route). Shipped on `feature/web-account-profile`. | `web/app/(account)/{layout,account/profile/page}.tsx` (new), `web/types/account.ts` (new), `web/lib/constants/interests.ts` (new), `web/app/(competitions)/competitions/{[slug]/dashboard/page,[slug]/layout,page}.tsx` |

### SPRINT 33 — Multi-Round Competitions + Komodo Wave 2 (May 18–19, 2026 — Session 13) ✅ COMPLETE

Two programmes on one branch `feature/multi-round-competitions` (PR → `eduversal-team/competzy:main`). Migrations `1750000000000`–`1750500000000` (run on the VPS). Verified per chunk — `tsc` + builds clean, curl-tested end-to-end.

**Grade categorisation — numeric grades.** SD/SMP/SMA replaced by numeric grades **1–12** platform-wide. `1750000000000_grades-numeric` converts `competitions.grade_level`, `exams.grades`/`questions.grades`, and the `correct_score`/`wrong_score` maps. `gradeTokens()` (`grade.service.ts`) is now a plain `"09"→"9"` normaliser. New `<GradeMultiSelect>` (web) — a 1–12 chip picker — in the competition forms; exam-builder / question-bank grade pickers are numeric.

**Multi-round engine.** `competition_rounds` gains `round_category` (`online`/`fast_track`/`local`/`global`), `requires_round_id`, `gating` JSONB, `required_docs`, `country`, `exam_mode` (`online`/`offline`), `qualifying_score`, `representative_user_id`; `exams` gains `round_id`; `registrations` gains `round_id` + `score` + `is_medalist` + `medalist_locked`. **One `registrations` row per (user, comp, round)** — `round_id NULL` = a whole-competition (single-round) registration, so EMC/ISPO/OSEBI are untouched. Per-round registration + payment (`POST /payments/snap` charges `COALESCE(cr.fee, c.fee)`) + round-scoped exam access (`exam-session.routes.ts` checks a cleared registration for the exam's round). **Gating modes** — `round-gating.service.ts` `checkRoundGating`: `open`, `prerequisite` (registered / paid / completed an earlier round), `qualified` (has a medal — the Global Round), `unqualified` (no medal yet — the Fast Track). A **medal** = `score ≥ round.qualifying_score`, auto-decided on exam submit (`applyMedalFromSession`) and on rep CSV score-import; an operator override sets `medalist_locked` so the auto-paths don't revert it. `replaceRounds` (`competition-rounds.service.ts`) two-pass-inserts rounds so a round's prerequisite can reference any sibling. A **rounds builder** (`web/components/rounds-builder.tsx`) in the admin + organizer competition forms.

**Komodo** — a 4th native competition (`/komodo`), International Math Competition. 6 rounds: Online Round 1–3 (open, Rp 200k each), Fast Track (`unqualified` gating), Local Round — Malaysia (`local`, offline, Rp 200k), Bali Global Round (`global` — gated on a medal, Rp 500k). Seeded by `db:seed:test-competitions`; registry entry in `web/lib/competitions/registry.ts`.

**Student round flow.** The web competition dashboard's `RoundsPanel` — per-round register/pay, "Missed" labels (the round's registration window closed), category badges, gating display, and a Global Round form (local/international + one-day/3-day package → `registrations.meta`). The web student competition portal moved onto a left-sidebar `AppShell` — `web/app/(competitions)/competitions/[slug]/layout.tsx` (the 7 per-page guard layouts consolidated; `register`/`admin`/`exam` excluded). **Mobile** — `app/app/(competition)/rounds.tsx` is the round picker; a multi-round competition's detail-screen CTA opens it; `roundId` threads through `registrations.service.ts` + `AuthContext`.

**Country representative** — the 7th role (`country_representative`, admin-created). Manages one country's students for a `local` round. `country_representatives` table (`id` = user, `comp_id`, `country`). Backend `country-rep.routes.ts` — `/api/country-representatives/*` (admin: create / list / delete) + `/api/rep/*` (the rep: context, bulk-register students, import offline scores, `pay-batch` + `pay-batch/:id/verify`). Bulk payment — `rep_payment_batches` table; one Midtrans transaction for all unpaid students; the `REPBATCH-` order is settled by the payment webhook + a verify poll. Web: admin `/country-reps`, the rep portal `/rep-portal` (`countryRepHttp` + `country-rep-context`, its own `AppShell`). Operator **medalist management** — `/question-bank/medalists` + `GET/PUT /api/question-bank/medalists` in `exam.routes.ts` (view scored entrants, override a medal).

**Test data.** `db:seed:test-competitions` seeds 4 competitions incl. Komodo's 6 rounds (with a relative timeline — Round 1's window is already closed so "Missed" is demoable); `db:create-test-accounts` adds `rep@test.local`.

**Round activation toggle (QA follow-up, 2026-05-19 — Session 13).** Each `competition_rounds` row gains `is_active` (migration `1750600000000_round-activation`) — an operator visibility switch surfaced in the web rounds builder (`rounds-builder.tsx`, used by both the admin `/admin/competitions` dialog and the organizer `CompetitionForm`). An **inactive round is hidden from students** — the web competition dashboard's `RoundsPanel` and the mobile `(competition)/rounds.tsx` filter it out, and `checkRoundGating` rejects registration for it (single enforcement point; the operator `GET` endpoints still return all rounds so the toggle stays editable). The builder shows an **advisory hint** (`activationAdvice`) nudging the operator — e.g. keep Fast Track off while the online rounds are still open, the Global Round off until earlier rounds finish; advisory only, never blocks. The migration backfills existing `fast_track` / `global` rounds to inactive; `db:seed:test-competitions` seeds Komodo's Fast Track + Bali Global Round with `isActive: false`. New shadcn `web/components/ui/switch.tsx` added for the toggle. Also: the student competition dashboard header gained an **"All competitions"** link back to the `/competitions` catalog. **Production Komodo seed** — `npm run db:seed:komodo` (`backend/src/db/seed-komodo.ts`) is a non-destructive, idempotent script that adds the Komodo competition + its 6 rounds + the 6-step flow to a database via plain INSERTs (no TRUNCATE — **safe on production**, unlike `db:seed:test-competitions`); it skips if a `komodo` competition already exists, and resolves the owner from an existing admin/organizer (else NULL). Exams / question bank are not seeded — an operator authors those via the question-bank UI.

### SPRINT 32 — EMC Wave 13: Mobile Merchandise Store (May 17, 2026 — Session 12) ✅ COMPLETE
Wave 13 ports the Wave-9 commerce **store** (product catalog → cart → Midtrans checkout → order history) to the React Native app — deferred out of Wave 11. **Mobile-only — no backend changes, no migration, no seeder changes** (every Wave-9 endpoint exists and accepts the mobile `Authorization: Bearer` JWT; the demo seeder already populates 4 products + a sample order). Plan: `/Users/mujtabo/.claude/plans/stateful-brewing-galaxy.md`. New per-competition student screens live in the existing `app/app/(competition)/` Stack group, reached from a "Store" entry on the `my-registration-details.tsx` Competition Hub. 3 phases: (1) service + cart + catalog & cart screens; (2) checkout (Midtrans); (3) order history + detail + wave wrap.
| Task | What | Key files |
|---|---|---|
| 32.1 | **Phase 1 — commerce service + cart + catalog & cart screens.** New `app/services/commerce.service.ts` — wraps the 6 Wave-9 storefront/order endpoints (`getStorefrontProducts` / `getMyOrders` / `getOrder` / `createOrder` / `payOrder` / `verifyOrder`) + a Hermes-safe `rupiah()` helper. New `app/hooks/use-cart.ts` — `useCart(compId)`, a per-competition cart persisted to `AsyncStorage` (key `competzy.cart.<compId>`), a direct port of the web `use-cart` hook (a `ready` flag while it hydrates). New `(competition)/store.tsx` — the catalog: a React-Query product list, per-product `Card` (image / name / price / description) + an "Add" button, a footer "View cart (N) · Rp …" bar, a "My Orders" header action. New `(competition)/cart.tsx` — line items with −/qty/+ steppers + remove, total, a "Checkout" button. `checkout.tsx` + `orders.tsx` shipped as placeholder stubs so the typed-routes nav resolves (Phases 2–3 replace them). A "Store" entry added to the `my-registration-details.tsx` Competition Hub. Verified: `tsc` clean. Shipped on `feature/wave13-phase1-store-catalog`. | `app/services/commerce.service.ts` (new), `app/hooks/use-cart.ts` (new), `app/app/(competition)/{store,cart,checkout,orders}.tsx` (new), `app/app/(tabs)/my-registration-details.tsx` |
| 32.2 | **Phase 2 — checkout (Midtrans).** `(competition)/checkout.tsx` rewritten from the Phase-1 stub into the real checkout — it mirrors `pay.tsx`'s Midtrans flow: an order-summary card (cart line items + total) + a shipping form (recipient name / phone / address); "Place order & pay" → `createOrder` → `payOrder` → `WebBrowser.openAuthSessionAsync` opens the Midtrans Snap page → `pollVerify` (`verifyOrder` 6× / 3 s) → success / pending / failed / cancelled / error states (the `pay.tsx` state machine). A zero-total `covered:true` order settles with no browser; on success the cart is cleared; "Try Again" re-pays the **existing** order (an `orderId` ref guards against a duplicate order). Verified: `tsc` clean. Shipped on `feature/wave13-phase2-checkout`. | `app/app/(competition)/checkout.tsx` |
| 32.3 | **Phase 3 — order history + detail — Wave 13 COMPLETE.** `(competition)/orders.tsx` rewritten from the Phase-1 stub into the real order history — a React-Query `getMyOrders` list, per-order `Card` (code, status `Pill`, competition name, item count, total, date, tracking), each tappable. New `(competition)/order-detail.tsx` — `getOrder(id)`: a status + timeline card (ordered/paid/shipped/delivered dates, tracking), the line items, subtotal/discount/shipping/total, and the ship-to address. Verified: `tsc` clean. Shipped on `feature/wave13-phase3-orders`. | `app/app/(competition)/orders.tsx`, `app/app/(competition)/order-detail.tsx` (new) |

### SPRINT 31 — EMC Wave 12: Certificates (PDF + QR/Barcode Verification) (May 17, 2026 — Session 12) ✅ COMPLETE
Wave 12 builds the certificate layer: a student who finishes a competition exam gets an auto-issued, verifiable **Certificate of Participation** (an operator can add a free-text award label → **Certificate of Achievement**). The certificate is a branded PDF carrying a **QR code** (→ a public verify page) + a **Code128 barcode** (the certificate number). Plan: `/Users/mujtabo/.claude/plans/stateful-brewing-galaxy.md`. Decisions: **auto-issued on exam completion** (the operator only curates + revokes — no manual-issue button); participation by default, an operator-set free-text award label promotes it to achievement. The `verification_code` is the **public capability** — the verify JSON + PDF download are code-gated, not auth-gated (so QR-scan download needs no login). Render-on-demand PDF (nothing stored). Native competitions only. 5 phases: (1) schema + auto-issue engine; (2) PDF + public verify API; (3) operator management API + UI; (4) public verify page + student web portal; (5) mobile + seeder + wave wrap.
| Task | What | Key files |
|---|---|---|
| 31.1 | **Phase 1 — certificates schema + auto-issue engine.** Migration `1749900000000_certificates.sql` — the `certificates` table (T1 strict `comp_id`, soft-delete, snapshot columns `student_name`/`competition_name`/`grade`/`score`, `type` `participation`\|`achievement`, `award_label`, `revoked_at` [domain "invalid", distinct from `deleted_at`], `score_locked`) + a `certificate_number_seq` sequence; **one live cert per `(comp_id,user_id)`** via a partial-unique index (the idempotency key). New `certificate.service.ts` — `bestAttempt` (the highest-scoring finished `sessions`/`paper_exams` attempt), `issueCertificateIfEligible` (idempotent — a native comp + a cleared live registration + ≥1 finished exam → INSERT with `CTZ-CERT-2026-NNNN` + a random 20-char base32 `verification_code`), `backfillCertificates` (issues every eligible-but-missing cert + refreshes the score on unlocked, non-revoked certs). Auto-issue wired **inline** (best-effort — a failure never blocks the response) into `POST /sessions/:id/submit` (online exam) + `PUT /question-bank/paper-exams/:id/answers` (paper exam). New `scheduleCertificateBackfill` cron (daily 03:00). `certificates` added to `SOFT_DELETE_TABLES`. Verified: migration applies, `tsc` clean; `backfillCertificates` issues `CTZ-CERT-2026-0001` for the test student (score 14 = the max of 5 finished EMC sessions), and a second run is a clean no-op (idempotent). Shipped on `feature/wave12-phase1-certificates-schema`. | `backend/migrations/1749900000000_certificates.sql` (new), `backend/src/services/certificate.service.ts` (new), `backend/src/routes/{exam-session,exam}.routes.ts`, `backend/src/services/cron.service.ts`, `backend/src/db/query-helpers.ts` |
| 31.2 | **Phase 2 — PDF rendering + public verify API.** Added deps `qrcode` + `@types/qrcode` + `bwip-js`. New `certificate-pdf.service.ts` — `renderCertificatePdf` builds a one-page **landscape-A4** PDF (PDFKit) on the competzy.com identity: ivory ground, violet double border, the title (Participation/Achievement), student name, competition, a grade/score line, and a footer carrying the issue date, certificate number, a **Code128 barcode** (`bwip-js`) and a **QR code** (`qrcode`) linking to `${APP_URL}/verify/<code>`; a diagonal "REVOKED" watermark when revoked. New `certificates.routes.ts` (mounted at `/api` **before** the bare-`/api` auth routers — same mount-order rule as `marketing.routes.ts`) — two **public** endpoints, no auth (the `verification_code` IS the capability): `GET /api/certificates/verify/:code` (metadata JSON) + `GET /api/certificates/verify/:code/pdf` (the PDF). Verified: `tsc` clean; curl — verify JSON returns the cert (404 on a bad code), the PDF endpoint streams a valid 1-page `application/pdf` with the QR + barcode rendered. Shipped on `feature/wave12-phase2-pdf-verify-api`. | `backend/src/services/certificate-pdf.service.ts` (new), `backend/src/routes/certificates.routes.ts` (new), `backend/src/index.ts`, `backend/package.json` |
| 31.3 | **Phase 3 — operator management API + operator UI.** `certificates.routes.ts` gains the operator namespace — path-scoped `router.use("/certificates/manage", authMiddleware, requireRole("admin","organizer"))`, every write `hasCompAccess`-gated + `audit()`-wrapped: `GET /certificates/manage/competitions` (the caller's native comps), `GET /certificates/manage?compId=&search=&page=` (paginated list), `PUT /:id` (edit `award_label` — set ⇒ `achievement`, clear ⇒ `participation` — + `score`/`score_max`; touching the score sets `score_locked`), `POST /:id/revoke`+`/restore` (toggle `revoked_at`), `DELETE /:id` (`softDelete`), `POST /certificates/manage/backfill?compId=` (runs `backfillCertificates`). New `certificatesHttp` web client alias. New `/question-bank/certificates` operator page — `<CompetitionPicker>` scope, search, a "Run backfill" button, the certificates table, an edit `Dialog` (award label / score / max), per-row View-PDF / revoke-restore / delete; "Certificates" added to the `(question-bank)` workspace nav (beside Proctoring). Verified: backend + web `tsc` clean, `npm run build` passes (`/question-bank/certificates` route); curl — list, PUT (→ achievement + `scoreLocked`), revoke (public verify then shows `revoked:true`), restore, idempotent backfill (the score-lock is respected). Shipped on `feature/wave12-phase3-operator-ui`. | `backend/src/routes/certificates.routes.ts`, `web/lib/api/client.ts`, `web/app/(question-bank)/layout.tsx`, `web/app/(question-bank)/question-bank/certificates/page.tsx` (new) |
| 31.4 | **Phase 4 — public verify page + student web portal.** Backend: `GET /api/certificates/mine?compId=` (authed, owner-scoped) — the caller's own certificates. Web: new **public**, route-group-free `web/app/verify/[code]/page.tsx` — fetches `GET /certificates/verify/:code`, renders valid / revoked / not-found states with the cert facts + a "Download certificate PDF" button (the QR code on a certificate points here; no login). New student `(competitions)/competitions/[slug]/certificates/{layout,page}.tsx` — its own student guard (mirrors `materials`), lists the caller's certs (`/certificates/mine`) with Download + Verify links. The competition dashboard's step-flow `Stepper` special-cases the `results` step with a `CertificateBlock` (mirrors `ExamBlock`/`AccessBlock`); a "Certificate" header link added. `competitionPaths()` gains `certificate`. Verified: backend + web `tsc` clean, `npm run build` passes (`/verify/[code]` + `/competitions/[slug]/certificates` routes registered); curl — `/certificates/mine` returns the test student's certificate. Shipped on `feature/wave12-phase4-verify-student`. | `backend/src/routes/certificates.routes.ts`, `web/app/verify/[code]/page.tsx` (new), `web/app/(competitions)/competitions/[slug]/certificates/{layout,page}.tsx` (new), `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx`, `web/lib/competitions/registry.ts` |
| 31.5 | **Phase 5 — mobile certificates surface + demo seeder — Wave 12 COMPLETE.** New `app/services/certificates.service.ts` — `getMine(compId)` + `certificatePdfUrl(code)` (the public, auth-free PDF URL). New `app/app/(competition)/certificates.tsx` — a React-Query list of the student's certificates; each card shows the participation/achievement title + award label + `Pill`s (valid/revoked, grade, score) + a "View / Download" button opening the PDF via `WebBrowser`. A "My Certificate" entry added to the `my-registration-details.tsx` Competition Hub. The demo seeder (`seed-emc-demo.ts`) extended — ensures the test student holds a cleared registration, auto-issues their certificate, and promotes it to a "Gold Medal" achievement (`CTZ-CERT-2026-0001`) so the operator UI + the achievement PDF have demo data. Verified: backend + app `tsc` clean; `db:seed:emc-demo` re-run (idempotent); Expo smoke test. Shipped on `feature/wave12-phase5-mobile-wrap`. | `app/services/certificates.service.ts` (new), `app/app/(competition)/certificates.tsx` (new), `app/app/(tabs)/my-registration-details.tsx`, `backend/src/db/seed-emc-demo.ts` |

### SPRINT 30 — EMC Wave 11: Mobile Rollout of Student Surfaces (May 17, 2026 — Session 11) ✅ COMPLETE
Wave 11 ports the student-facing competition features built on the web in Waves 7–10 to the React Native app (`app/`): the online exam player, registration-fee vouchers, announcements, study materials, and feedback. **Mobile-only — no backend changes, no migration** (every endpoint exists from Waves 7–10 and accepts the mobile `Authorization: Bearer` JWT). Plan: `/Users/mujtabo/.claude/plans/effervescent-booping-horizon.md`. Decisions: the mobile merchandise store is deferred to Wave 12; the exam player is **native screens** (not a WebView); mobile webcam proctoring is out of scope. New per-competition student screens live in a new `app/app/(competition)/` Stack route group, reached from a "Competition Hub" card on `my-registration-details.tsx`. Phases: (1) exam services + exam list; (2) native exam player + result; (3) voucher entry in the pay flow; (4) announcements feed; (5) materials library; (6) feedback + wave wrap.
| Task | What | Key files |
|---|---|---|
| 30.6 | **Phase 6 — feedback form + Wave 11 wrap — Wave 11 COMPLETE.** `marketing.service.ts` — `sendSuggestion(compId, content)` (wraps `POST /api/suggestions`). New `(competition)/feedback.tsx` — a multiline `TextInput` → `sendSuggestion` → a thank-you state. A "Send Feedback" entry added to the `my-registration-details.tsx` Competition Hub (which now carries all four Wave-11 surfaces: Take exam / Announcements / Study Materials / Send Feedback). Verified: `tsc` clean. Shipped on `feature/wave11-phase6-feedback`. | `app/services/marketing.service.ts`, `app/app/(competition)/feedback.tsx` (new), `app/app/(tabs)/my-registration-details.tsx` |
| 30.5 | **Phase 5 — materials library.** `marketing.service.ts` — `getMaterials(compId)` + the `Material` type. New `(competition)/materials.tsx` — the study-material library: materials grouped by `category` (the API orders by it), each card showing the body, platform + grade `Pill`s, and a "Download" button opening the signed file URL via `WebBrowser`. A "Study Materials" entry added to the `my-registration-details.tsx` Competition Hub. Verified: `tsc` clean. Shipped on `feature/wave11-phase5-materials`. | `app/services/marketing.service.ts`, `app/app/(competition)/materials.tsx` (new), `app/app/(tabs)/my-registration-details.tsx` |
| 30.4 | **Phase 4 — announcements feed.** New `app/services/marketing.service.ts` — `getAnnouncements(compId)` (wraps `GET /api/announcements`) + the `Announcement` type. New `(competition)/announcements.tsx` — a React-Query feed: featured posts in a sunshine-tinted card up top, the rest below; each card shows the featured/type/platform `Pill`s + date, a remote `Image`, the body, and an "Open attachment" button (`WebBrowser`). A second button ("Announcements") added to the `my-registration-details.tsx` Competition Hub. Verified: `tsc` clean. Shipped on `feature/wave11-phase4-announcements`. | `app/services/marketing.service.ts` (new), `app/app/(competition)/announcements.tsx` (new), `app/app/(tabs)/my-registration-details.tsx` |
| 30.3 | **Phase 3 — voucher entry in the pay flow.** `app/services/payments.service.ts` — new `validateVoucher(registrationId, code)`; `createSnapToken` gains an optional `voucherCode` arg; `SnapTokenResponse` fields made optional + `covered?`/`status?` added (a fully-covering voucher settles server-side). `app/app/(payment)/pay.tsx` — a "Have a voucher?" `Card` in the payer-select step: a code `TextInput` + Apply → `validateVoucher` previews the discounted fee (struck-through original); `startPayment` passes the applied code to `createSnapToken` and, on a `covered:true` response, goes straight to the success state with no WebBrowser. New `rupiah()` helper (manual thousands separator — Hermes-safe). Verified: `tsc` clean. Shipped on `feature/wave11-phase3-voucher-pay`. | `app/services/payments.service.ts`, `app/app/(payment)/pay.tsx` |
| 30.2 | **Phase 2 — native exam player + result.** `(competition)/exam.tsx` rewritten from the Phase-1 stub into the native player: a `ScreenHeader` with a `Pill` countdown (`trailing`) seeded from `getSession`'s `remainingSeconds`, ticking down once a second and **auto-submitting at zero** (a `submitted` ref guards a double-submit); a `ScrollView` of question `Card`s — MC as pressable radio rows, short-answer as a multiline `TextInput`; per-answer autosave (`saveAnswer` — MC on select, short-answer on blur); a "Submit exam" `Button` with a confirm `Alert`; a finished session redirects to the result. `(competition)/exam-result.tsx` rewritten — a violet score hero (`totalPoint`), correct/wrong/blank `StatTile`s, and an "awaiting grading" banner while short answers are unmarked (the `result` block from `getSession`). Verified: `tsc` clean. Shipped on `feature/wave11-phase2-exam-player`. | `app/app/(competition)/exam.tsx`, `app/app/(competition)/exam-result.tsx` |
| 30.1 | **Phase 1 — exam services + exam list.** New `app/services/exams.service.ts` — `getAvailableExams` / `startSession` / `getSession` / `saveAnswer` / `submitSession` (the latter three feed Phase 2) wrapping the Wave-7 exam endpoints. New `app/app/(competition)/` Stack route group (`_layout.tsx`, registered in the root `app/app/_layout.tsx`). New `(competition)/exams.tsx` — the exam list (a React-Query `getAvailableExams` fetch; per exam a windowed CTA — Start / Resume / View-result / a locked note); `exam.tsx` + `exam-result.tsx` shipped as placeholder stubs so navigation works (Phase 2 replaces them). `my-registration-details.tsx` gains a **"Competition Hub"** card with a "Take exam" button → `(competition)/exams`. Verified: `tsc` clean. Shipped on `feature/wave11-phase1-exam-list`. | `app/services/exams.service.ts` (new), `app/app/(competition)/{_layout,exams,exam,exam-result}.tsx` (new), `app/app/_layout.tsx`, `app/app/(tabs)/my-registration-details.tsx` |

### SPRINT 29 — EMC Wave 10: Referrals, Announcements & Materials (Marketing) (May 17, 2026 — Session 11) ✅ COMPLETE
Wave 10 builds the marketing layer on the schema-only Wave-1 tables (`1748500000000_emc-marketing.sql` — `referrals`, `clicks`, `announcements`, `materials`, `suggestions`): a per-competition affiliate-referral program (share link + click tracking + a click→account→registration→paid funnel that auto-increments + commission accrual), an in-portal announcements feed (per-comp or platform-wide, with an optional "also notify" push), a study-materials library, and student suggestions. Plan: `/Users/mujtabo/.claude/plans/effervescent-booping-horizon.md`. Decisions: referrals = full attribution + commission; announcements = feed + optional push; suggestions included; mobile deferred to Wave 11. Phases: (1) referrals + clicks backend; (2) referrals operator UI + `?ref=` link; (3) announcements backend + operator UI; (4) announcements student feed; (5) materials; (6) suggestions + wave wrap. Operator UI lives in the `(question-bank)` workspace as a "Marketing" nav section.
| Task | What | Key files |
|---|---|---|
| 29.6 | **Phase 6 — suggestions + Wave 10 wrap — Wave 10 COMPLETE.** Backend (`marketing.routes.ts`): `POST /api/suggestions` (student-facing, per-route `authMiddleware`, outside `/marketing` — `{compId, examId?, content}`), `GET /marketing/suggestions?compId=` (operator inbox — `hasCompAccess`-gated, joins the submitter + exam name when tied), `DELETE /marketing/suggestions/:id` (`softDelete`). Web — a student `(competitions)/competitions/[slug]/feedback/{layout,page}.tsx` form (textarea → `POST /suggestions` → thank-you state) + an operator `/question-bank/suggestions` inbox (competition picker + feedback cards with submitter / exam badge / delete). "Suggestions" added to the Marketing nav; "Feedback" added to the dashboard header; `competitionPaths` gains `feedback`. Seeder extended — 2 demo suggestions (1 exam-tied). Verified: `tsc` clean, `npm run build` passes (both routes), `db:seed:emc-demo` run; curl — a student submits feedback, the operator inbox lists it with the submitter name + exam tie. Shipped on `feature/wave10-phase6-suggestions`. | `backend/src/routes/marketing.routes.ts`, `backend/src/db/seed-emc-demo.ts`, `web/lib/competitions/registry.ts`, `web/app/(question-bank)/layout.tsx`, `web/app/(question-bank)/question-bank/suggestions/page.tsx` (new), `web/app/(competitions)/competitions/[slug]/feedback/*` (new), `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx` |
| 29.5 | **Phase 5 — materials.** Backend (`marketing.routes.ts`): materials CRUD (`/marketing/materials`) — T2 scoped exactly like announcements (`resolveScope`: a real comp ⇒ `hasCompAccess`, `"platform"` ⇒ admin-only), `category` + `grades` (JSONB string array) + `published`/`is_active`, `POST /:id/upload?kind=image|file` (the shared 10 MB `assetUpload`). Student `GET /api/materials?compId=` (per-route `authMiddleware`) — published+active materials for the comp **plus** platform-wide, ordered by category. `materials` is in `SOFT_DELETE_TABLES` (Phase 1). Web — new `/question-bank/materials` operator page (scope `Select`, table, editor `Dialog` with category / grade-toggle chips / a file upload) + a student `(competitions)/competitions/[slug]/materials/{layout,page}.tsx` library (its own guard; materials grouped by category, grade badges, signed-URL downloads). "Materials" added to the Marketing nav + the dashboard header; `competitionPaths` gains `materials`. Seeder extended — 3 demo materials (2 competition / 1 platform-wide, each with a downloadable file). Verified: `tsc` clean, `npm run build` passes (both routes), `db:seed:emc-demo` run; curl — operator list (category + grades), the student library returns 3 with signed file URLs, create/delete. Shipped on `feature/wave10-phase5-materials`. | `backend/src/routes/marketing.routes.ts`, `backend/src/db/seed-emc-demo.ts`, `web/lib/competitions/registry.ts`, `web/app/(question-bank)/layout.tsx`, `web/app/(question-bank)/question-bank/materials/page.tsx` (new), `web/app/(competitions)/competitions/[slug]/materials/*` (new), `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx` |
| 29.4 | **Phase 4 — announcements student feed.** Backend: `GET /api/announcements?compId=` (`marketing.routes.ts`, per-route `authMiddleware` — outside the `/marketing` operator namespace) returns the published + active announcements for the competition **plus** every platform-wide post, ordered `is_featured DESC, published_at DESC`. Web — new `(competitions)/competitions/[slug]/announcements/{layout,page}.tsx` (its own student guard mirroring dashboard/store): a news feed — featured posts in an amber-bordered card up top, the rest below; each card shows the type/platform badge, signed image, body, and a signed-URL download for an attachment. A "News" link added to the competition dashboard header; `competitionPaths` gains `announcements`. Verified: backend + web `tsc` clean, `npm run build` passes (`/competitions/[slug]/announcements` route); curl — the student feed returns the comp's featured post first + the platform-wide post. Shipped on `feature/wave10-phase4-announcement-feed`. | `backend/src/routes/marketing.routes.ts`, `web/lib/competitions/registry.ts`, `web/app/(competitions)/competitions/[slug]/announcements/*` (new), `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx` |
| 29.3 | **Phase 3 — announcements backend + operator UI.** Backend (`marketing.routes.ts`): announcement CRUD (`/marketing/announcements`) — T2 scoped, the `compId` value `"platform"` ⇒ a platform-wide post (`comp_id` NULL, **admin-only** via `resolveScope`), a real comp ⇒ `hasCompAccess`-gated; `published`/`is_active`/`is_featured` flags (`published_at` = `COALESCE(published_at, now())` when published, NULL when unpublished); `POST /:id/upload?kind=image|file` (multer 10 MB → `storeFile`); a `notify` flag → `sendBatchNotifications` to the competition's registrants (or every student/parent/teacher for a platform post). Web — new `/question-bank/announcements` operator page: a local scope `Select` (the operator's competitions + a "Platform-wide" option shown only to admins via `useQuestionBankAuth`), a list table + an editor `Dialog` (title/type/body/image + published/featured/visible/also-notify toggles). "Announcements" added to the Marketing nav. Seeder extended — 2 demo announcements (1 competition, 1 platform-wide). Verified: `tsc` clean, `npm run build` passes, `db:seed:emc-demo` run; curl — comp + platform lists, an organizer is 403'd from the platform scope, create-as-draft → publish-via-PUT sets `published_at`, delete. Shipped on `feature/wave10-phase3-announcements`. | `backend/src/routes/marketing.routes.ts`, `backend/src/db/seed-emc-demo.ts`, `web/app/(question-bank)/layout.tsx`, `web/app/(question-bank)/question-bank/announcements/page.tsx` (new) |
| 29.2 | **Phase 2 — referrals operator UI + public `?ref=` link.** New `marketingHttp` client alias. A **"Marketing"** `NavSection` added to the `(question-bank)` workspace (Referrals — more items land in later phases). New `/question-bank/referrals` page — a CRUD table (affiliate, code + copy-share-link button, the click/sign-up/registration/paid funnel columns, accrued commission) + an add/edit `Dialog` (name/email/phone/`code`[immutable on edit]/`commissionPerPaid`/`bonus`/`year`) + search + `Pager`; reuses `<CompetitionPicker>`/`useQuestionBank`, the share link is `…/competitions/[slug]/register?ref=CODE`. The competition register page (`…/[slug]/register/page.tsx`) now captures `?ref=` (read from `window.location.search` — no `useSearchParams`, so no Suspense boundary needed): fires `POST /referrals/click` once per visit (sessionStorage-guarded), calls `POST /referrals/signup` after a new signup, and threads `referralCode` into the `POST /registrations` body. Verified: `tsc` clean, `npm run build` passes (`/question-bank/referrals` route). Shipped on `feature/wave10-phase2-referrals-ui`. | `web/lib/api/client.ts`, `web/app/(question-bank)/layout.tsx`, `web/app/(question-bank)/question-bank/referrals/page.tsx` (new), `web/app/(competitions)/competitions/[slug]/register/page.tsx` |
| 29.1 | **Phase 1 — referrals + clicks backend.** Migration `1749800000000_referral-commission-rate.sql` adds `referrals.commission_per_paid NUMERIC`. New `marketing.routes.ts` (mounted `/api`, **path-scoped** `/marketing/*` `requireRole("admin","organizer")` guard): referral CRUD `GET /marketing/referrals?compId=&search=&page=` / `GET /:id` / `POST` (auto `REF-NNN` or a normalized custom `code`) / `PUT` / `DELETE` (`softDelete`) — all `hasCompAccess`-gated, `audit()`. Public attribution endpoints **outside** the `/marketing` namespace: `POST /api/referrals/click` (no auth — inserts a `clicks` row + `click++`, best-effort) and `POST /api/referrals/signup` (auth — `account++`). Attribution wiring: `POST /registrations` does `registration++` on a matching live referral; the payment **webhook + verify** make the `pending_review` UPDATE conditional (`WHERE status NOT IN (...) RETURNING id`) — a returned row ⇒ first settle ⇒ `creditReferralPaid` does `paid++` + `commission += commission_per_paid` (idempotent against a webhook+verify double-settle, the same guard now also gates voucher redemption + the success push). `referrals`/`clicks`/`announcements`/`materials`/`suggestions` added to `SOFT_DELETE_TABLES`. **Mount-order fix:** `marketing.routes.ts` is mounted before the bare-`/api` routers that carry an unscoped `router.use(authMiddleware)` (`competition-flows`/`affiliated-credentials`/`exam-session`) so its public `/referrals/click` is reached before they 401 unauthenticated fall-through. Seeder extended — 2 demo referrals (`REF-D01/D02`) with a populated funnel + `clicks`. Verified: `tsc` clean, migration applied, `db:seed:emc-demo` run; curl — referral CRUD, a public click bumps `click`, a registration-via-referral bumps `registration`, a forged settlement webhook bumps `paid` + accrues `commission` (25000), a verify double-settle does **not** double-count, fall-through (`GET /api/competitions`) intact. Shipped on `feature/wave10-phase1-referrals-api`. | `backend/migrations/1749800000000_referral-commission-rate.sql` (new), `backend/src/routes/marketing.routes.ts` (new), `backend/src/routes/{registrations,payments}.routes.ts`, `backend/src/db/{query-helpers,seed-emc-demo}.ts`, `backend/src/index.ts` |

### SPRINT 28 — EMC Wave 9: Vouchers, Products & Orders (Commerce) (May 17, 2026 — Session 11) ✅ COMPLETE
Wave 9 builds the commerce layer on the schema-only Wave-1 tables (`1748400000000_emc-commerce.sql` — `voucher_groups`, `vouchers`, `products`, `orders`, `order_items`): a per-competition merchandise store (products → student storefront → orders → operator fulfillment) and registration-fee vouchers (batches of discount codes that reduce the fee a student pays to register). Plan: `/Users/mujtabo/.claude/plans/effervescent-booping-horizon.md`. Decisions: full scope (operator management + student storefront + Midtrans checkout); vouchers discount **competition registration fees** (not orders). Phases: (1) product catalog API; (2) commerce UI + voucher minting; (3) voucher application in the registration-fee flow; (4) order payments via Midtrans (payments-row polymorphism); (5) operator order management; (6) student storefront + checkout. Operator commerce UI lives in the existing `(question-bank)` workspace as a "Commerce" nav section.
| Task | What | Key files |
|---|---|---|
| 28.1 | **Phase 1 — operator product catalog API.** New `commerce.routes.ts` (mounted `/api`, **path-scoped** `requireRole("admin","organizer")` guard so the bare-`/api` router doesn't 403 fall-through traffic): `GET /commerce/competitions` (the operator's native comps — admin all, organizer own), `GET /commerce/products?compId=&search=&active=&page=` (paginated), `GET /commerce/products/:id`, `POST` (auto `code` = `PRD-NNN` comp-monotonic + a unique `slug` from the name with a dedup loop, `audit()`), `PUT`, `DELETE` (`softDelete`), `POST /commerce/products/:id/image` (multer `memoryStorage`, 5 MB, `image/*` → `storeFile` → signed URL). All writes `hasCompAccess`-gated (native-only); cross-comp access → 404. `products` added to `SOFT_DELETE_TABLES`. Verified: `tsc` clean; curl — create (`PRD-001`), a same-named product slug-dedups (`emc-2026-t-shirt-2`), update, list, `GET /commerce/competitions` (21 native), soft-delete. Shipped on `feature/wave9-phase1-products-api`. | `backend/src/routes/commerce.routes.ts` (new), `backend/src/db/query-helpers.ts`, `backend/src/index.ts` |
| 28.6 | **Phase 6 — student storefront + checkout — Wave 9 COMPLETE.** Backend: `GET /commerce/storefront/products?compId=` — the student catalog (active+live products, no operator gate, under the `authMiddleware`-only `/commerce/storefront/*` sub-tree). Web — new `(competitions)/competitions/[slug]/store/*`: `layout.tsx` (student guard mirroring dashboard/pay), `page.tsx` (product grid + a localStorage cart via the new `web/lib/competitions/use-cart.ts` hook — qty steppers, a cart summary card), `checkout/page.tsx` (typed name/phone/address → `POST /commerce/orders` → `POST /commerce/orders/:id/pay` → opens the Midtrans tab + polls `/orders/:id/verify`; a zero-total order is `covered`), `orders/page.tsx` (the student's order history from `GET /commerce/storefront/orders`). A "Store" link added to the competition dashboard header; `competitionPaths` gains `pay` + `store`. The demo seeder (`seed-emc-demo.ts`) extended — 4 `products` (`PRD-D01`–`D04`, demo image via `storeFile`), a 10-code voucher batch (`VG-D01`, discounted 20000), a sample paid order (`ORD-D01`, 2 items). Verified: `tsc` clean, `npm run build` passes (3 store routes); `db:seed:emc-demo` re-run; curl — storefront products (5 incl. demo, signed images) + storefront orders (`ORD-D01` paid). Shipped on `feature/wave9-phase6-storefront`. | `backend/src/routes/commerce.routes.ts`, `backend/src/db/seed-emc-demo.ts`, `web/lib/competitions/{registry,use-cart}.ts`, `web/app/(competitions)/competitions/[slug]/store/*` (new), `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx` |
| 28.5 | **Phase 5 — operator order management.** Backend (`commerce.routes.ts`): `POST /commerce/orders` (student — `{compId, items[], name, phone, address}`; products re-validated live+active+same-comp, **server-priced** — never trusts client prices; one `orders` row [`code` auto `ORD-NNN`, `status='ordered'`, `expires_at=+24h`] + the `order_items` in a transaction), `GET /commerce/orders?compId=&status=&page=` (operator — `hasCompAccess`-gated, paginated, per-order `item_count` + buyer), `GET /commerce/storefront/orders` (the caller's own orders across comps), `GET /commerce/orders/:id` (owner or operator — order + items), `PUT /commerce/orders/:id/status` (operator — the `ordered→canceled` / `paid→shipped`[requires `trackingNumber`]/`paid→canceled` / `shipped→delivered` state machine, sets the matching timestamp, `audit()`). `orders`/`order_items` added to `SOFT_DELETE_TABLES`. Web — new `/question-bank/orders` (status-tab table + a detail `Dialog` — items, ship-to address, fulfillment actions); "Orders" added to the Commerce nav. Verified: `tsc` clean, `npm run build` passes; curl — place an order (`ORD-001`, total 135000, 2 items), storefront + operator list, detail, the invalid `ordered→shipped` + ship-without-tracking rejections, `paid→shipped`(tracking)→`delivered`, a student 403'd from the operator list. Shipped on `feature/wave9-phase5-order-management`. | `backend/src/routes/commerce.routes.ts`, `backend/src/db/query-helpers.ts`, `web/app/(question-bank)/layout.tsx`, `web/app/(question-bank)/question-bank/orders/page.tsx` (new) |
| 28.4 | **Phase 4 — order payments via Midtrans (payments-row polymorphism).** Migration `1749700000000_payments-order-kind.sql` drops the `NOT NULL` on `payments.registration_id` + adds `payments.kind TEXT NOT NULL DEFAULT 'registration'` (`registration`|`order`) + a CHECK that a `registration`-kind row still carries a `registration_id` (so an order payment can have none). The settlement webhook (`payments.routes.ts`) now LEFT JOINs `orders` + branches on `kind`: an `order`-kind success flips `orders.status='paid'`+`paid_at` and returns early; `registration`-kind keeps the existing pending_review/voucher/push logic untouched. `commerce.routes.ts` — its router-level operator guard re-scoped to the operator-only sub-trees (`/commerce/{competitions,products,voucher-groups}`) so `/commerce/orders/*` can be owner-scoped; new owner-only `POST /commerce/orders/:id/pay` (a `kind='order'` `payments` row, fresh Snap token, links `orders.payment_id`; reuses a still-pending matching token; a zero-total order settles without Midtrans) + `GET /commerce/orders/:id/verify` (Midtrans Status poll → syncs `orders.status`). Verified: `tsc` clean, migration applied; curl — order pay → `kind=order`/`registration_id` NULL `payments` row, a **forged settlement webhook** (real `MIDTRANS_SERVER_KEY` signature) flips `orders.status='paid'`, verify on a paid order returns `paid`, and a registration-kind webhook still settles → `pending_review` (regression). Shipped on `feature/wave9-phase4-order-payments`. | `backend/migrations/1749700000000_payments-order-kind.sql` (new), `backend/src/routes/payments.routes.ts`, `backend/src/routes/commerce.routes.ts` |
| 28.3 | **Phase 3 — voucher application in the registration-fee flow.** Migration `1749600000000_registration-voucher-code.sql` adds `registrations.voucher_code TEXT`. Backend (`payments.routes.ts`): new helpers `loadRegContext` (comp + fee + the student's `students.npsn`) / `checkVoucher` (live / `is_active` / `used<max` / per-comp / optional school-NPSN match) / `redeemVoucher` (idempotent — `used++` + links `payment_id`, guarded by `payment_id IS NULL` so a webhook+verify double-settle is safe). New endpoints `POST /payments/voucher/validate` (preview — no mutation) + `PUT /payments/registration/:id/voucher` (persist/clear). `POST /payments/snap` accepts `voucherCode` (else reads the persisted code) → charges `voucher_groups.discounted`; a full-cover voucher (discount → 0) settles **without Midtrans** (`covered:true`). The settlement webhook + the `verify` endpoint both `redeemVoucher` on success. Web — new `(competitions)/competitions/[slug]/pay/{layout,page}.tsx`: a fee summary, a "Have a voucher?" input (live `/voucher/validate` preview + discount line), then `POST /snap` → opens the Midtrans tab + polls `/verify`; the dashboard's `payment` step + the `pending_payment` fallback card now link to `/pay`. Verified: `tsc` clean, `npm run build` passes; curl — validate (valid/bad), PUT, snap charges the discounted `amount` (20000 vs 35000), a full-cover voucher → `covered:true` + `redeemVoucher` bumps `used` 0→1 + registration → `pending_review`. Shipped on `feature/wave9-phase3-voucher-redemption`. | `backend/migrations/1749600000000_registration-voucher-code.sql` (new), `backend/src/routes/payments.routes.ts`, `web/lib/competitions/registry.ts`, `web/app/(competitions)/competitions/[slug]/pay/*` (new), `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx` |
| 28.2 | **Phase 2 — operator commerce UI + voucher minting.** Backend (`commerce.routes.ts`): voucher-group endpoints — `GET /commerce/voucher-groups?compId=`, `POST` (creates the `voucher_groups` row + **mints `usableCount` `vouchers` in one transaction** — codes `VG-NNN-XXXXXX` from an unambiguous alphabet, optional `npsn`, `used=0`/`max=1`; 1–1000 cap), `PUT /:id` (name/`discounted`/`price`/`is_active` — the minted codes + `usable_count` are immutable), `DELETE /:id` (soft-deletes the group **and** its vouchers), `GET /:id/vouchers` (the minted codes + claim status). `voucher_groups`/`vouchers` added to `SOFT_DELETE_TABLES`; `voucher_groups.discounted` = the absolute registration fee a holder pays. New `commerceHttp` client alias. Web — a **"Commerce" nav section** in the `(question-bank)` workspace (Products / Vouchers); new `/question-bank/products` (catalog table + add/edit `Dialog` with **image upload** + search + `Pager`) and `/question-bank/vouchers` (voucher-batch table + create `Dialog` that mints codes + edit + a **"Codes" viewer** `Dialog`). Reuses `<CompetitionPicker>` / `useQuestionBank`. Verified: `tsc` clean, `npm run build` passes (both routes); curl — create a 20-code batch (`VG-001`, sample `VG-001-C6VDHR`), list, update, delete cascades to vouchers; confirmed in-browser. Shipped on `feature/wave9-phase2-commerce-ui`. | `backend/src/routes/commerce.routes.ts`, `backend/src/db/query-helpers.ts`, `web/lib/api/client.ts`, `web/app/(question-bank)/layout.tsx`, `web/app/(question-bank)/question-bank/{products,vouchers}/page.tsx` (new) |

### SPRINT 27 — EMC Wave 8: Test Centers, Areas & Webcam Proctoring (May 17, 2026 — Session 11) ✅ COMPLETE
Wave 8 builds venue management (areas + test centers — the global `1748200000000_emc-venues.sql` tables) and best-effort webcam proctoring of online exams (the `webcams` table). The legacy `supervisor` role is **dropped** — venue management + proctoring fold into admin + organizer (mirrors Wave 6 dropping `question_maker`). Plan: `/Users/mujtabo/.claude/plans/effervescent-booping-horizon.md`. Decisions: drop `supervisor`; full scope (venues + webcam proctoring); best-effort webcam (capture if permission granted, exam proceeds either way). Phases: (1) drop supervisor + venues API; (2) venues UI; (3) wire `test_center_id` onto paper exams; (4) webcam capture; (5) proctoring review. The `area_user`/`test_center_user` pivots lose their actor — left schema-only.
| Task | What | Key files |
|---|---|---|
| 27.1 | **Phase 1 — drop `supervisor` + venues backend API.** Migration `1749400000000_remove-supervisor-role.sql` drops `supervisor` from `users_role_check` (now **6 roles** — mirrors `1749300000000`); the `create-supervisor.ts` seed + `db:create-supervisor` script deleted. New `venues.routes.ts` (mounted `/api`) — areas + test-centers CRUD; **path-scoped** guards (`/venues/*` admin+organizer read, `/admin/venues/*` admin-only write) so the bare-`/api` router doesn't 403 fall-through traffic; `audit()`-wrapped; `code` UNIQUE → 409; `softDelete` + `liveFilter`. `areas`/`test_centers` added to `SOFT_DELETE_TABLES`. The test-center POST/PUT re-load via a join (`loadTestCenter`) so the response carries the area province. Verified: migration applied (0 supervisor users, 6-role constraint); `tsc` clean; curl — admin CRUDs areas + test centers, duplicate code 409, organizer reads but write 403, soft-delete hidden by `liveFilter`, fall-through intact. Shipped on `feature/wave8-phase1-venues-api`. | `backend/migrations/1749400000000_remove-supervisor-role.sql` (new), `backend/src/routes/venues.routes.ts` (new), `backend/src/db/query-helpers.ts`, `backend/src/index.ts`, `backend/package.json` |
| 27.2 | **Phase 2 — venues UI.** New `web/app/(dashboard)/venues/page.tsx` — a "Venues" admin page with **Areas** / **Test Centers** `Tabs`; each panel has search + a shadcn `Table` + an add/edit `Dialog` + delete + `Pager` (reuses the `schools` page CRUD pattern + `adminHttp`). The test-center dialog has an Area `Select`. "Venues" added to the admin sidebar (`MapPin` icon). Verified: `tsc` clean, `npm run build` passes (`/venues` route), confirmed in-browser. Shipped on `feature/wave8-phase2-venues-ui`. | `web/app/(dashboard)/venues/page.tsx` (new), `web/app/(dashboard)/layout.tsx` |
| 27.3 | **Phase 3 — wire `test_center_id` onto paper exams.** Backend (`exam.routes.ts`): `POST /question-bank/paper-exams` accepts an optional `testCenterId` (validated against a live `test_centers` row → 400 if unknown), writes it; `GET /question-bank/paper-exams` + `loadPaperExamDetail` LEFT JOIN `test_centers` → return `testCenterName`. Create-only for Wave 8. Web: the paper-exams "Add paper result" dialog gains a **Test center** `Select` (from `GET /venues/test-centers`); the results table gains a Test Center column; the paper-exam detail subtitle shows the venue. Verified: `tsc` clean, `npm run build` passes; curl — create with/without `testCenterId`, invalid id 400, list + detail carry the name; confirmed in-browser. Shipped on `feature/wave8-phase3-paper-test-center`. | `backend/src/routes/exam.routes.ts`, `web/app/(question-bank)/question-bank/paper/page.tsx`, `web/app/(question-bank)/question-bank/paper/[id]/page.tsx` |
| 27.4 | **Phase 4 — webcam capture during the online exam.** Migration `1749500000000_session-camera-flag.sql` adds `sessions.camera_available BOOLEAN` (nullable — best-effort proctoring record); `webcams` added to `SOFT_DELETE_TABLES`. `exam-session.routes.ts`: `PUT /sessions/:id/camera-status` (records camera availability) + `POST /sessions/:id/webcams` (multer `memoryStorage`, `image/jpeg` ≤ 2 MB; session owned by `req.userId` + unfinished; `storeFile` → `webcams` row). The web exam player requests `getUserMedia` on start — on grant a hidden `<video>`+`<canvas>` capture a 320px JPEG every 30s (+ at submit) uploaded via `emcHttp.postFormData`, a "Proctored" chip in the bar; on denial a "No camera" chip, exam proceeds (best-effort). The demo seeder extended — the `EMC-PRAC` session gets `camera_available=true` + 3 webcam snapshots (Phase-5 review data). Verified: `tsc` clean, `npm run build` passes; curl — camera-status flips, webcam upload stores a row, ownership 404. Shipped on `feature/wave8-phase4-webcam-capture`. | `backend/migrations/1749500000000_session-camera-flag.sql` (new), `backend/src/routes/exam-session.routes.ts`, `backend/src/db/query-helpers.ts`, `backend/src/db/seed-emc-demo.ts`, `web/app/(competitions)/competitions/[slug]/exam/[sessionId]/page.tsx` |
| 27.5 | **Phase 5 — proctoring review surface — Wave 8 COMPLETE.** Backend (`exam.routes.ts`, inherits the `/question-bank` operator guard): `GET /question-bank/proctoring/sessions?compId=` (`hasCompAccess`-gated — admin: any competition, organizer: own; sessions + student/exam names + `cameraAvailable` + `webcamCount` + timings) and `GET /question-bank/proctoring/sessions/:id` (session meta + its `webcams` resolved to 15-min `getSignedUrl` snapshot URLs). Web — new `(question-bank)` pages: `/question-bank/proctoring` (sessions list — competition picker + table with a camera badge + snapshot count) and `/question-bank/proctoring/[sessionId]` (the **snapshot gallery** — a signed-URL image grid with capture timestamps; distinct empty states for camera-denied vs no-snapshots). "Proctoring" added to the workspace nav. Verified: `tsc` clean, `npm run build` passes; curl — sessions list + a session detail with signed snapshot URLs; confirmed in-browser. Shipped on `feature/wave8-phase5-proctoring`. | `backend/src/routes/exam.routes.ts`, `web/app/(question-bank)/question-bank/proctoring/page.tsx` (new), `web/app/(question-bank)/question-bank/proctoring/[sessionId]/page.tsx` (new), `web/app/(question-bank)/layout.tsx` |

### SPRINT 26 — EMC Wave 7: Exam Delivery (May 16, 2026 — Session 11) ✅ COMPLETE
Wave 7 makes the native-competition `exam` step real: operators assemble exams from the Wave-6 question bank, students sit them online in the web portal, and results are produced — plus the paper (offline) workflow. Built on the Wave-1 exam schema (`1748300000000_emc-exam-delivery.sql` — `exams`, `exam_question`, `sessions`, `periods`, `answer_keys`, `paper_exams`, `paper_answers`, `webcams`); **no new SQL migration**. Plan: `/Users/mujtabo/.claude/plans/effervescent-booping-horizon.md`. Decisions: online + paper both; short-answer **manually marked only** (MC auto-grades); web exam player this wave (mobile = Wave 11). Phases: (1) exam builder API; (2) exam builder UI; (3) online attempt API; (4) manual grading queue; (5) paper workflow; (6) student exam player + dashboard. Webcam proctoring + test centers are Wave 8.
| Task | What | Key files |
|---|---|---|
| 26.1 | **Phase 1 — exam blueprint + builder API.** New `exam.routes.ts` (mounted `/api`, `requireRole("admin","organizer")`, `audit()`-wrapped) — operator exam CRUD over the `/question-bank/exams/*` namespace: `GET` list (`?compId=`), `GET /:id` (detail + attached questions), `POST` create (UNIQUE `(comp_id,code)` → 409), `PUT /:id`, `DELETE /:id` (soft-delete), `PUT /:id/questions` (replace the `exam_question` set transactionally — rejects non-`approved` or cross-competition questions). The native-competition + ownership gate `hasCompAccess` extracted from `question-bank.routes.ts` into the shared `comp-access.service.ts`. **`SOFT_DELETE_TABLES` fix** — `exams`/`sessions`/`periods`/`paper_exams`/`paper_answers` were absent (so `softDelete()` on them threw); added. No new migration — the Wave-1 8-table exam schema is used as-is. Verified: `tsc` clean; curl — create exam, duplicate code 409, attach an approved question, reject a non-approved one (400), detail, soft-delete. Shipped on `feature/wave7-phase1-exam-blueprint-api`. | `backend/src/routes/exam.routes.ts` (new), `backend/src/services/comp-access.service.ts` (new), `backend/src/db/query-helpers.ts`, `backend/src/routes/question-bank.routes.ts`, `backend/src/index.ts` |
| 26.2 | **Phase 2 — exam builder UI.** New pages in the `(question-bank)` workspace: `/question-bank/exams` (list — competition picker + table of code/name/date/grades/question-count, edit/delete, "New exam") and `/question-bank/exams/[id]` (the builder — `new` creates, a UUID edits). The builder: a **blueprint** card (name, code, year, date, start/end time, duration, grades checkboxes, MC/short-answer toggles, description), a **per-grade scoring** card (correct/wrong points per selected grade), and a **question set** picker (edit mode — a searchable checklist of the competition's `approved` questions, saved via `PUT /:id/questions`). "Exams" added to the workspace nav. Verified: `tsc` clean, `npm run build` passes (2 new routes), confirmed in-browser. Shipped on `feature/wave7-phase2-exam-builder-ui`. | `web/app/(question-bank)/question-bank/exams/page.tsx` (new), `web/app/(question-bank)/question-bank/exams/[id]/page.tsx` (new), `web/app/(question-bank)/layout.tsx` |
| 26.3 | **Phase 3 — online attempt lifecycle API.** New `exam-session.routes.ts` (student-facing — `authMiddleware`, every session owned by `req.userId`): `GET /exams/available?compId=` (the student's grade-matched exams + their session state, each annotated `unscheduled/upcoming/open/closed` by the date+time window), `POST /exams/:examId/sessions` (start — window must be `open`, snapshots one `periods` row per `exam_question` so a later exam edit can't change the paper; resumes an unfinished session, blocks a finished re-attempt), `GET /sessions/:id` (**leak-safe** — serves question content + answer-option text only, never `answers.is_correct` or `questions.explanation`; includes the server-computed remaining time), `PUT /sessions/:id/periods/:periodId` (save one answer, deadline-checked), `POST /sessions/:id/submit` (finish — MC auto-grades, short-answer stays `is_correct=NULL`, recompute rollups; idempotent). New `exam-grading.service.ts` — `autoGradeMcPeriods` (MC ↔ the selected answer's `is_correct`, scored by the exam's per-grade `correct_score`/`wrong_score`), `recomputeSessionRollups` (per-section `corrects`/`wrongs`/`blanks`/`points` + `total_point` — an answered-but-ungraded short period is counted nowhere, so the score stays partial until manual grading), `sessionHasPendingGrading`. **Route-mounting fix:** `question-bank.routes.ts` (and now `exam.routes.ts`) carried a router-level `requireRole` that — being mounted at bare `/api` — 403'd *all* `/api/*` fall-through traffic, blocking the new student routes; the guards are now path-scoped to `/question-bank/*`. Verified: `tsc` clean; curl — full lifecycle (available → start → resume → leak-safe serve → save → submit → MC graded `total_point` 4.00); admin still reaches `/question-bank/*`. Shipped on `feature/wave7-phase3-online-attempt-api`. | `backend/src/routes/exam-session.routes.ts` (new), `backend/src/services/exam-grading.service.ts` (new), `backend/src/index.ts`, `backend/src/routes/{question-bank,exam}.routes.ts` |
| 26.4 | **Phase 4 — manual grading queue.** Backend (`exam.routes.ts`, `audit()`-wrapped): `GET /question-bank/grading/queue?compId=` (finished sessions with answered-but-ungraded short-answer periods), `GET /question-bank/grading/sessions/:id` (the operator session view — unlike the student view this DOES expose answer keys + explanations + per-grade suggested points), `PUT /question-bank/grading/periods/:periodId` (mark a short period `is_correct`+`point` → `recomputeSessionRollups`, so the score is live and the session leaves the queue once nothing is pending — no separate finalize step). The exam router's path-scoped guard broadened `/question-bank/exams` → `/question-bank` (covers grading + Phase-5 paper). Web — new `(question-bank)` pages: `/question-bank/grading` (queue — competition picker + table) and `/question-bank/grading/[sessionId]` (grade a session — score summary, every question with the student's answer; short answers show the typed text + answer key + explanation + a points input pre-filled to the per-grade score + Mark correct / Mark wrong). "Grading" added to the workspace nav. Verified: `tsc` clean, `npm run build` passes; curl — submit a short-answer exam → it enters the queue → grade → leaves the queue, `total_point` recomputed; confirmed in-browser. Shipped on `feature/wave7-phase4-grading-queue`. | `backend/src/routes/exam.routes.ts`, `web/app/(question-bank)/question-bank/grading/page.tsx` (new), `web/app/(question-bank)/question-bank/grading/[sessionId]/page.tsx` (new), `web/app/(question-bank)/layout.tsx` |
| 26.5 | **Phase 5 — paper-exam workflow.** The offline path — an operator records a student's paper attempt. Backend (`exam.routes.ts`, `audit()`-wrapped): `GET /question-bank/exams/:examId/students` (cleared registrants for the picker — flags who already has a paper result), `GET/POST/DELETE /question-bank/paper-exams` (list `?examId=` / create — one `paper_exams` envelope + a snapshot `paper_answers` row per exam question, one-per-`(exam,user)` → 409 / soft-delete), `GET /question-bank/paper-exams/:id` (the answer sheet — each `paper_answer` joined to its question by code-order `number`, + MC options / short keys), `PUT /question-bank/paper-exams/:id/answers` (bulk save: MC entries `value=optionId` auto-grade against the key with the per-grade score; short entries take the operator's text + Correct/Wrong + points; recompute the rollup). New `recomputePaperRollups` in `exam-grading.service.ts` (mirrors the session version; the MC/short split is derived by joining `paper_answers.number` to the exam's questions in code order — `paper_answers` has no type column). Web — new `(question-bank)` pages: `/question-bank/paper` (competition + exam pickers → paper-results table; "Add paper result" → searchable student picker) and `/question-bank/paper/[id]` (the answer sheet — per question an option dropdown for MC or a text input + Correct/Wrong/Ungraded + points for short; "Save answer sheet"). "Paper Exams" added to the workspace nav. Fixed: `row_number()` returns `bigint` → node-pg gives a string, so the `byNumber` Map lookup is coerced with `Number()`. Verified: `tsc` clean, `npm run build` passes; curl — create paper exam, MC auto-grades (total 4), short marked correct (total 5), duplicate 409, delete; confirmed in-browser. Shipped on `feature/wave7-phase5-paper-workflow`. | `backend/src/routes/exam.routes.ts`, `backend/src/services/exam-grading.service.ts`, `web/app/(question-bank)/question-bank/paper/page.tsx` (new), `web/app/(question-bank)/question-bank/paper/[id]/page.tsx` (new), `web/app/(question-bank)/layout.tsx` |
| 26.6 | **Phase 6 — student exam player + dashboard integration — Wave 7 COMPLETE.** Backend: `GET /sessions/:id` now also returns a `result` block (`totalPoint` + `corrects`/`wrongs`/`blanks` + `awaitingGrading`) — only once the session is finished (the score stays hidden mid-attempt). Web — the student exam experience in the competition portal: new `(competitions)/competitions/[slug]/exam/` route group — `layout.tsx` (student guard mirroring the dashboard guard), `[sessionId]/page.tsx` (the **player** — a sticky bar with a live server-seeded countdown that auto-submits at zero, MC radio options + short-answer textareas, autosave on every answer via `PUT …/periods/:id`, "Submit exam" with a confirm), `[sessionId]/result/page.tsx` (the **result** — big score, correct/wrong/blank tiles, an awaiting-grading banner while short answers are unmarked). The dashboard stepper special-cases `step_key='exam'` (mirroring `external_access`) with an `ExamBlock` — fetches `GET /exams/available`, renders **Start exam** / **Resume** / **View result** per exam. Verified: `tsc` clean, `npm run build` passes (2 new routes); confirmed in-browser — start an exam, countdown, answer MC + short, submit, the result page. Shipped on `feature/wave7-phase6-exam-player`. | `web/app/(competitions)/competitions/[slug]/exam/*` (new), `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx`, `backend/src/routes/exam-session.routes.ts` |

### SPRINT 25 — EMC Wave 6: The Question Bank (May 16, 2026 — Session 11) ✅ COMPLETE
Wave 6 builds the question bank on the Wave-1 schema: the subject/topic/subtopic taxonomy, multiple-choice + short-answer questions, and the `draft → submitted → approved` review workflow. **Re-scoped mid-wave (per the user):** there is **no `question_maker` role** — the question bank is folded into the **admin + organizer** portals and exists for **native competitions only** (affiliated competitions have no bank). An admin manages every native competition's bank; an organizer manages the banks of the competitions they created. **Self-approval is allowed** so a solo organizer is never blocked. The first build (a standalone `question_maker` role + portal + `accesses` grants) was scrapped before merge — PRs #15/#16/#17 closed; Wave 6 reshipped as **one consolidated PR (#18)** on `feature/wave6-question-bank` (based on `fix/admin-tables-and-school-location`). Plan: `/Users/mujtabo/.claude/plans/effervescent-booping-horizon.md`.
| Task | What | Key files |
|---|---|---|
| 25.1 | **Question-bank API — `question-bank.routes.ts`** (mounted `/api`, `requireRole("admin","organizer")`). `hasCompAccess` = the competition is `kind='native'` AND the caller owns it (admin → any; organizer → `created_by` match). `GET /question-bank/competitions` (native comps the caller may manage). Taxonomy: subjects/topics/subtopics CRUD (one generic handler set over the 3 comp-scoped tables). Questions CRUD — list (filter status/subject/grade), get, create, update (draft-only), soft-delete; child `answers` (MC options or the short-answer key) + `question_topics` tags written in a transaction; per-comp auto code `Q-NNN`. Review: `POST …/questions/:id/{submit,approve,send-back}` (the `draft→submitted→approved` machine — submit draft-only, approve/send-back submitted-only, send-back needs a comment, **no writer ≠ reviewer restriction**); proofreads `GET`/`POST`/`PUT` (review passes — level/cognitive/answer/comment, `done`→`done_at`). All `audit()`-wrapped. Migration `1749300000000_remove-question-maker-role` drops `question_maker` from `users_role_check` (now 7 roles) + removes the seed fixtures (audit_log/accesses cleared first, authored questions cascade). The obsolete admin `/admin/question-makers` `accesses` endpoints + the `create-question-maker` seed deleted. Verified: `tsc` clean; curl — admin builds taxonomy, creates an MC question, submit → self-approve → `approved`; organizer scoped to its competitions. | `backend/src/routes/question-bank.routes.ts` (new), `backend/migrations/1749300000000_remove-question-maker-role.sql` (new), `backend/src/routes/admin.routes.ts`, `backend/src/index.ts` |
| 25.2 | **Question-bank workspace (web).** New `(question-bank)` route group reached from a **"Question Bank" link in both the admin and organizer sidebars** — `questionBankHttp` client; `question-bank-context.tsx` (`createRoleAuth`, accepts `admin` + `organizer`); `(question-bank)/layout.tsx` on the shared `AppShell` (nav: Dashboard / Taxonomy / Questions + a "Back to portal" link). `lib/question-bank/context.tsx` — `QuestionBankProvider` holds the caller's native competitions + the selected one (persisted to `localStorage`) + a shared `<CompetitionPicker>` scoping the whole workspace. Pages: `/question-bank` dashboard (picker + 4 `StatCard`s — Total/Draft/Awaiting Review/Approved); `/question-bank/taxonomy` (3-column subject → topic → subtopic drill-down, inline add/rename/delete); `/question-bank/questions` (list — status/subject/grade filters, status badges, delete) + `/question-bank/questions/[id]` (the editor — `new` creates / a UUID edits; type MC/short-answer, content, answer options or key, classification, grades, subject→topic→subtopic tag builder; "Save draft" or "Save & submit for review"; non-draft questions open read-only). Verified: `tsc` clean, `npm run build` passes, confirmed in-browser. | `web/lib/api/client.ts`, `web/lib/auth/question-bank-context.tsx` (new), `web/lib/question-bank/context.tsx` (new), `web/app/(question-bank)/*` (new), `web/app/(dashboard)/layout.tsx`, `web/app/(organizer)/layout.tsx`, `web/app/page.tsx` |
| 25.3 | **Review-workflow UI — completes Wave 6.** New `(question-bank)` pages: `/question-bank/review` — the review queue, a table of `submitted` questions for the picked competition; `/question-bank/review/[id]` — the review screen rendering the question read-only (content, the answer options with the correct one(s) marked, explanation, classification badges, named topic tags), the prior-proofread history, and an actions panel — a comment box + an optional difficulty/cognitive assessment + **Approve** / **Send back to draft** (send-back requires a comment). Both actions hit the Phase-2 endpoints and record a proofread; the question leaves the queue. "Review" added to the workspace nav. Verified: `tsc` clean, `npm run build` passes, confirmed in-browser (send-back → `draft`, re-submit → approve → `approved`). | `web/app/(question-bank)/question-bank/review/page.tsx` (new), `web/app/(question-bank)/question-bank/review/[id]/page.tsx` (new), `web/app/(question-bank)/layout.tsx` |

### SPRINT 24 — EMC Wave 5: Affiliated Competitions (May 16, 2026 — Session 11) ✅ COMPLETE
Competzy hosts **two competition kinds**: **native** (full lifecycle in-app — Waves 1–4) and **affiliated** (students register + pay on Competzy, then an operator issues each a login + password + an external link; they compete off-platform). Wave 5 built the affiliated path. Plan: `/Users/mujtabo/.claude/plans/effervescent-booping-horizon.md`. All phases shipped: (1) backend — `kind` flag + credentials + issuance APIs ✅; (2) web — `kind` selector (2a) + operator credential-issuing UI (2b) ✅; (3) student-facing — external-access step + mobile card ✅.
| Task | What | Key files |
|---|---|---|
| 24.4 | **Phase 3 — student-facing affiliated experience — Wave 5 COMPLETE.** Portal generalization: `getCompetitionConfig` now returns a derived default config for any slug absent from the hardcoded registry, so operator-created competitions get a working `/competitions/[slug]` portal (never `null`). Slug generation: admin + organizer competition `POST` generate a unique URL `slug` (name-derived + a base-36 timestamp suffix) so the catalog can link newly-created competitions; also fixed a stale `\s`-vs-`\\s` regex bug in the admin slug builder. Web dashboard: the step-flow `Stepper` renders the `external_access` step specially — a new `AccessBlock` showing the issued username + password + an "Open the competition platform" button (or "being prepared" if not yet issued), fed by `GET /registrations/:id/credentials`. Mobile: `my-registration-details` shows a "Competition Access" card (username + password + "Open Competition Site") once credentials are issued — new `getCredentials()` in `registrations.service.ts`. Verified: `tsc` clean (backend + web + app), `npm run build` passes, slug generation curl-tested. Shipped on `feature/wave5-phase3-student-facing`. | `web/lib/competitions/registry.ts`, `backend/src/routes/{admin,organizer}.routes.ts`, `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx`, `app/app/(tabs)/my-registration-details.tsx`, `app/services/registrations.service.ts` |
| 24.3 | **Phase 2b — operator credential-issuing UI (web).** New `CredentialIssueDialog` (per-registration username/password issue/edit + a generate-password helper → `POST /registrations/:id/credentials`) and `BulkCredentialDialog` (download a CSV template pre-filled with the competition's registrants, fill username/password, upload → `POST /competitions/:id/credentials/bulk`, with an issued/failed summary). The organizer Participants page gains — for **affiliated** competitions only — a "Credentials" column (Issued badge / "Issue login" per row → the issue dialog) + a header "Bulk credentials" action; native competitions unchanged. Admin-side credential-issuing is **deferred** — the admin portal has no per-competition participant browser (the `/registrations` page is a pending-review queue); the backend already permits admin issuing, so a dedicated admin UI is an optional follow-up. Verified: `tsc` clean, `npm run build` passes, confirmed in-browser. Shipped on `feature/wave5-phase2b-credential-ui`. | `web/components/credential-issue-dialog.tsx` (new), `web/components/bulk-credential-dialog.tsx` (new), `web/app/(organizer)/participants/page.tsx` |
| 24.2 | **Phase 2a — competition `kind` selector (web).** Phase 2 sub-split into **2a** (competition kind) + **2b** (operator credential-issuing UI). The shared organizer `competition-form.tsx` gains a "Competition type" section (native / affiliated `Select`); when affiliated, the post-payment-redirect-URL field is relabeled "Affiliated competition URL", marked required, and submit is blocked without it; `kind` flows in the submit payload. The admin competition dialog (`/admin/competitions`) gains the same `kind` select + URL field (added to `FORM_DEFAULTS`, `openEdit`, and the dialog grid). `kind` added to the `Competition` type; the organizer edit page maps `kind` into the form's initial values. Verified: `tsc` clean, `npm run build` passes. Shipped on `feature/wave5-phase2a-competition-kind`. | `web/components/competition-form.tsx`, `web/app/(dashboard)/admin/competitions/page.tsx`, `web/app/(organizer)/organizer-competitions/[id]/edit/page.tsx`, `web/types/index.ts` |
| 24.1 | **Phase 1 — affiliated backend.** Migration `1749100000000_affiliated-competitions.sql` adds `competitions.kind TEXT NOT NULL DEFAULT 'native' CHECK (kind IN ('native','affiliated'))` + the `affiliated_credentials` table (per-registration `username`/`password` — **plaintext**: a credential the student must read and type into an external partner site, not Competzy auth; comp-scoped, soft-delete, partial-unique on `registration_id`). New `competition-flow.service.ts` `seedDefaultFlow(client, compId, kind)` — called after every competition INSERT (admin + organizer POST): native → the 6-step flow, **affiliated → 5 steps ending in `external_access`** (`step_key='external_access'`, `check_type='none'`). Admin + organizer competition `POST`/`PUT` accept + write `kind`; the admin handlers also now write `post_payment_redirect_url` (they silently dropped it before). `kind` exposed in the competition read endpoints (`competitions.routes.ts` GET `/` + `/:id`, organizer GET `/competitions` + `/:id`; admin GET `/competitions` already returns raw rows). New `affiliated-credentials.routes.ts` (mounted at `/api`): `GET /registrations/:id/credentials` (owner-or-operator), `GET /competitions/:id/credentials` (operator), `POST` issue/update (ON CONFLICT upsert), `POST …/credentials/bulk`, `DELETE` revoke — `organizer`+`admin` only, organizers scoped to their own competitions, all `audit()`-wrapped. The affiliated external URL reuses `competitions.post_payment_redirect_url`. Verified: `tsc` clean; migration applied; credential lifecycle (issue/get/list/bulk/revoke) + affiliated-competition create (auto-seeds the `external_access` flow) all curl-tested. Shipped on `feature/wave5-phase1-affiliated-backend`. | `backend/migrations/1749100000000_affiliated-competitions.sql` (new), `backend/src/services/competition-flow.service.ts` (new), `backend/src/routes/affiliated-credentials.routes.ts` (new), `backend/src/db/query-helpers.ts`, `backend/src/index.ts`, `backend/src/routes/{admin,organizer,competitions}.routes.ts` |

### SPRINT 23 — EMC Wave 4: The Student Competition Experience (May 16, 2026 — Session 11) ✅ COMPLETE
Wave 4 turns the student-facing competition flow from scaffolding into a real experience: a `/competitions` catalog, the admin competitions page relocated to `/admin/competitions`, and a config-driven per-competition step-flow engine (`competition_flows` table). The question-bank authoring UI was split out to Wave 6; affiliated competitions are Wave 5. Plan: `/Users/mujtabo/.claude/plans/effervescent-booping-horizon.md`. All 3 phases shipped: (1) catalog + admin move ✅; (2) `competition_flows` schema + step-flow API ✅; (3) stepper UI + admin flow editor ✅.
| Task | What | Key files |
|---|---|---|
| 23.3 | **Phase 3 — student stepper UI + admin flow editor — Wave 4 COMPLETE.** The competition dashboard's bare status card is replaced with a **vertical stepper** driven by `GET /registrations/:id/flow-progress` — each step renders done (check) / current (accent ring + description + a contextual hint) / upcoming (muted); falls back to the old status copy when a competition has no flow; added an "All competitions" link back to the catalog. New **`FlowEditorDialog`** — per-competition step list with add / edit / reorder (↑↓, via the two-pass `/flow/reorder`) / delete wired to the Phase-2 admin endpoints; opened by a new "Flow" button on each `/admin/competitions` row. Verified: `tsc` clean, `npm run build` passes, confirmed in-browser. Shipped on `feature/wave4-phase3-stepper`. | `web/app/(competitions)/competitions/[slug]/dashboard/page.tsx`, `web/components/flow-editor-dialog.tsx` (new), `web/app/(dashboard)/admin/competitions/page.tsx` |
| 23.2 | **Phase 2 — `competition_flows` schema + step-flow API.** New migration `1749000000000_competition-flows.sql` (renamed up from `1748800000000` — node-pg-migrate enforces migration order and `1748900000000` had already run): the `competition_flows` table — comp-scoped (`comp_id`), soft-delete, partial-unique `(comp_id, step_order)`, `check_type ∈ {profile,documents,payment,approval,none}`. Seeds the EMC 6-step flow (profile → documents → review → payment → exam → results) keyed on the stable `slug='emc-2026'` — the competition's TEXT `id` is env-specific (`comp-1-eduversal-mathematics-competition` locally, **not** `comp_emc_2026_main`). New `backend/src/services/readiness.service.ts` — `computeCompleteness()` extracted from the `/completeness` route so it is the single source of truth; the `/completeness` response stays byte-identical. New `competition-flows.routes.ts` (mounted at `/api`): `GET /competitions/:id/flow`, `GET /registrations/:id/flow-progress` (joins the flow with readiness → per-step `done | current | upcoming`), and admin `POST` / `PUT :stepId` / `PUT /reorder` (two-pass to dodge the partial-unique index) / `DELETE` (`adminOnly` + `audit()`, soft-delete). Verified: `tsc` clean; migration applied + 6 steps seeded; all endpoints curl-tested (flow → 6 steps; flow-progress evaluates correctly; POST/DELETE round-trip). Shipped on `feature/wave4-phase2-flow-api`. | `backend/migrations/1749000000000_competition-flows.sql` (new), `backend/src/services/readiness.service.ts` (new), `backend/src/routes/competition-flows.routes.ts` (new), `backend/src/routes/registrations.routes.ts`, `backend/src/index.ts`, `backend/src/db/query-helpers.ts` |
| 23.1 | **Phase 1 — `/competitions` catalog + admin competitions move.** The admin competition-management page relocated `web/app/(dashboard)/competitions/` → `web/app/(dashboard)/admin/competitions/` (URL `/competitions` → `/admin/competitions`; page content unchanged — pure move); the admin sidebar "Competitions" link + the dashboard quick-action re-pointed. New auth-gated **`/competitions` catalog** (`web/app/(competitions)/competitions/page.tsx`) — a shadcn `Card` grid off the public `GET /api/competitions`; cards whose `slug` has a `competitionRegistry` portal config link to `/competitions/[slug]/dashboard`, others show "Portal coming soon". Guards via `useCompetitionAuth` (the `(competitions)` route-group layout has no guard of its own — the register page must stay reachable unauthenticated). Post-login routing for student/parent re-pointed from the hard-coded `/competitions/emc-2026/dashboard` placeholder to the catalog `/competitions` (`web/app/page.tsx` `destinationFor`). Verified: `npm run build` passes, both `/competitions` + `/admin/competitions` routes registered. Shipped on `feature/wave4-phase1-catalog`. | `web/app/(dashboard)/admin/competitions/page.tsx` (moved), `web/app/(dashboard)/{layout,dashboard/page}.tsx`, `web/app/(competitions)/competitions/page.tsx` (new), `web/app/page.tsx` |

### SPRINT 22 — EMC Wave 3: Brand Unification on the competzy.com identity (May 16, 2026 — Session 11) ✅ COMPLETE
Wave 3 unifies web + mobile on the **`competzy.com`** brand: Electric Indigo violet `#5627ff` + Hot Pink `#d9277b` + Ivory cream `#f4ecdc`, fonts Bricolage Grotesque (display) + Plus Jakarta Sans (body). Plan: `/Users/mujtabo/.claude/plans/synthetic-dreaming-boot.md`. All 6 phases shipped: (1) web re-point ✅; (2) mobile tokens + fonts + components ✅; (3) mobile student screens ✅; (4) parent + teacher ✅; (5) profile + shared ✅; (6) auth/onboarding/payment + polish ✅.
| Task | What | Key files |
|---|---|---|
| 22.1 | **Phase 1 — web re-pointed to the competzy.com identity.** `globals.css` shadcn tokens swapped teal → Electric Indigo violet primary, ivory `#f4ecdc` background, ink `#161214` text, charts re-tuned (violet/pink/gold/sirih/green), `--brand-pink` + `--brand-gold` added, dark mode redesigned to warm dark violet. `layout.tsx` fonts swapped DM Serif/Sans/Mono → Bricolage Grotesque + Plus Jakarta Sans + JetBrains Mono (Google Fonts). The two teal brand-panel gradients (`/` login + `HubAuthShell`) → violet. Web is fully token-driven (Wave 2) so all 34 pages re-brand with no page rewrites. Verified: `tsc` clean, build passes, confirmed in-browser. | `web/app/globals.css`, `web/app/layout.tsx`, `web/app/page.tsx`, `web/components/hub-auth-shell.tsx` |
| 22.2 | **Phase 2 — mobile design tokens + fonts + component kit on the competzy.com identity.** `constants/theme.ts` rewritten off the Sprint-19 "playful" palette: `Brand` → Electric Indigo violet `#5627FF` + Hot Pink `#D9277B` + Gold; `Surface` → ivory cream `#F4ECDC` paper; `Text` → ink `#161214`. Token *names* kept stable so all 31 screens re-colour for free. **`Type` scale right-sized** (the "text too big" fix): `displayLg` 36→28, `displayMd` 30→24, `h1` 26→22, `h2` 22→19, `body` 15→14; the heavy 900/800 weights dropped — new `FontFamily` const, roles point at named weighted font families (no `fontWeight`). `Radius` flattened (28–44 → 20–28), `Shadow` lightened to soft ink, the `playful` clay halo calmed. **Fonts:** Bricolage Grotesque (display) + Plus Jakarta Sans (body) via `@expo-google-fonts/{bricolage-grotesque,plus-jakarta-sans}` + `expo-font` `useFonts` in `_layout.tsx` — native splash held until loaded, nav theme background → ivory. **Components:** `Button` pill → 16px radius + smaller label, `StatTile` value 26→22, `GeometricHeader` SVG blobs → restrained concentric-ring motif; `Pill`/`Card`/`SubjectCircle`/`SectionHeader`/`ScreenHeader`/`EmptyState` auto-adapt via tokens. Tab bar hardcoded slate hex → tokens (ivory bg, violet active tint, Jakarta label). Verified: `tsc` clean, confirmed in Expo. | `app/constants/theme.ts`, `app/app/_layout.tsx`, `app/app/(tabs)/_layout.tsx`, `app/components/ui/{Button,StatTile,GeometricHeader}.tsx`, `app/package.json` |
| 22.3 | **Phase 3 — mobile student screens on the competzy.com identity.** The four student surfaces (`competitions` Discover, `my-competitions`, `competitions/[id]`, `my-registration-details`) audited on the new tokens — they re-colour automatically since token names stayed stable. Polish: Discover search input `fontWeight` → `FontFamily.bodyMedium` (was falling back to the system font) + a dead `isActive ? #fff : #fff` ternary removed; `my-competitions` count-badge `fontWeight` → `FontFamily.bodyBold`, and an Indonesian leftover fixed — Remove button "Menghapus..." → "Removing...". Editorial flatten: removed the Sprint-19 decorative blobs from the `competitions/[id]` pastel hero and the `my-registration-details` violet hero. Verified: `tsc` clean, confirmed in Expo. First per-phase branch under the new workflow — shipped as PR #2 (`feature/wave3-phase3-student-screens` → `main`). | `app/app/(tabs)/{competitions,my-competitions,my-registration-details}.tsx`, `app/app/(tabs)/competitions/[id].tsx` |
| 22.4 | **Phase 4 — mobile parent + teacher screens on the competzy.com identity.** The five parent/teacher surfaces (`children`, `teacher-dashboard`, `teacher-students`, `teacher-analytics`, `teacher-actions`) audited on the new tokens. Polish: every `fontWeight` literal → `FontFamily.*` (RN custom fonts are per-weight families — `fontWeight` is silently ignored, so avatar initials / badges / buttons were falling back to the system font); cool-slate hardcoded hex (`#fff`, `#94A3B8`, `#64748B`, `#CBD5E1`, `#F1F5F9`, `rgba(15,23,42…)`) → warm ivory tokens; `teacher-analytics` stray `#EEF2FF` fallback → `Brand.primarySoft`; `teacher-actions` quick-action tile colors → brand palette (violet / sirih / pink / green). Editorial flatten: removed the Sprint-19 decorative `heroBlob` from the `teacher-dashboard` hero. Dead code removed: `teacher-actions` unused `recentActivities` const + 6 `activity*` styles. Verified: `tsc` clean, confirmed in Expo. Shipped on `feature/wave3-phase4-parent-teacher`. | `app/app/(tabs)/{children,teacher-dashboard,teacher-students,teacher-analytics,teacher-actions}.tsx` |
| 22.5 | **Mobile login eye-icon + test-account student roster** (alongside Phase 4). Login (`(auth)/login.tsx`): the "Show/Hide password" text link replaced with an eye icon inside the password field (`AppInput`'s existing `rightIcon`/`onRightIconPress` API + Ionicons `eye-outline`/`eye-off-outline`). Seed (`create-test-accounts.ts`): now links a 6-student roster (the test student + 5 random real students, `ORDER BY random()`) to `parent@test.local` (`parent_student_links`, status `active`) and `teacher@test.local` (`teacher_student_links`) so the My Children / My Students screens have data in QA; still idempotent. | `app/app/(auth)/login.tsx`, `backend/src/db/create-test-accounts.ts` |
| 22.7 | **Phase 6 — mobile auth + onboarding + payment + final polish — Wave 3 COMPLETE.** Six surfaces. `(onboarding)/index` — removed the Sprint-19 decorative SVG blobs, calmed the icon-tile's heavy per-slide colored halo → soft ink `Shadow.lg`, title `fontWeight:800` → `FontFamily.displayBold` + right-sized 30→28, description got `FontFamily.bodyRegular` (was falling back to the system font). `(auth)/login` — `🏆` logo emoji → Ionicons `trophy`; `fontWeight` literals → `FontFamily`. `(auth)/claim-account` — `🏅` → Ionicons `ribbon`; **`"✓ HP terverifikasi"` → `"✓ Phone verified"`** (Indonesian leftover); show/hide-password text link → eye icon inside the field (`AppInput` `rightIcon`, matches the 22.5 login pattern); `fontWeight` → `FontFamily`. `(payment)/pay` — all emoji → Ionicons: payer options (`person`/`people`/`business`/`briefcase`), result states (`checkmark-circle`/`time`/`close-circle`/`arrow-undo`/`alert-circle`), hero `card`, loading `time`; **`"View Competitionku"` → `"View My Competitions"`** (Indonesian leftover); style keys de-`Emoji`'d. `bulk-registration` — a stray `#EF4444` icon color → `Brand.error` (Phase-5 straggler caught by a corrected-path hex sweep). `app.json` — splash background `#ffffff` → ivory `#F4ECDC`, Android adaptive-icon background `#E6F4FE` → ivory (icon/splash *artwork* left as-is — redrawing is out of scope). `app/index.tsx` reviewed — pure `<Redirect>`, nothing to theme. Corrected-path sweep confirms zero hardcoded hex / screen-rendered emoji / Indonesian / `fontWeight` literals across all 14 Wave-3 mobile screens. Verified: `tsc` clean, confirmed in Expo. Shipped on `feature/wave3-phase6-auth-payment`. | `app/app/(auth)/{login,claim-account}.tsx`, `app/app/(onboarding)/index.tsx`, `app/app/(payment)/pay.tsx`, `app/app/bulk-registration.tsx`, `app/app.json` |
| 22.6 | **Phase 5 — mobile profile + shared screens on the competzy.com identity.** Nine surfaces. **Three predated the Sprint-19 redesign and got full style-block rewrites** off the hardcoded cool-slate palette (`#F8FAFC`/`#64748B`/`#1E293B`/`#94A3B8`/`#E2E8F0`/…) onto ivory/violet tokens: `document-vault` (upload buttons + doc-card icons + info-box + delete `✕` → Ionicons, custom dashed empty state → shared `EmptyState`, Indonesian leftover **"Mengunggah…" → "Uploading…"**), `setup` (`ℹ️` → Ionicons, ivory screen bg), `link-parent` (custom header → shared `ScreenHeader`, `IconSymbol` SF-symbols → Ionicons `information-circle`/`checkmark-circle`/`close-circle`, debug box re-tinted gold). **Two had hardcoded lower halves re-tokened:** `school-dashboard` (search / grade-chips / student+reg cards / badges / stat cards) and `bulk-registration` (job cards / progress / stats / errors) — `getStatusColor` + status badges moved onto the brand palette (paid→success green, processing→violet, else→ink). **Four already-token screens got light polish:** `profile/index`, `notifications`, `profile/history`, `profile/edit` — every `fontWeight` literal → `FontFamily.*` (RN custom fonts are per-weight families, so `fontWeight` was silently ignored — avatar initials / menu labels fell back to the system font), leftover emoji → Ionicons (`📷` camera badge, `🏆`/`🔍` empty states), two unused imports dropped. Verified: `tsc` clean, confirmed in Expo. Shipped on `feature/wave3-phase5-profile-shared`. | `app/app/(tabs)/profile/{index,edit,setup,history,document-vault,link-parent}.tsx`, `app/app/(tabs)/notifications.tsx`, `app/app/{school-dashboard,bulk-registration}.tsx` |

### SPRINT 21 — EMC Wave 2: Professional Web UI/UX Unification (May 15–16, 2026 — Session 11) ✅ COMPLETE
Wave 2 was re-scoped mid-session: the user reviewed the running portals and redirected from feature work to a full UI/UX redesign — one shared design system (Tailwind v4 + shadcn/ui + recharts + lucide-react) across every web surface. Plan: `/Users/mujtabo/.claude/plans/synthetic-dreaming-boot.md`. Phases: (0) role foundation ✅; (1) design system + admin dashboard; (2) admin portal; (3) organizer; (4) school; (5) student portal + auth/public pages.
| Task | What | Key files |
|---|---|---|
| 21.0 | **Phase 0 — EMC-port role foundation** (backend-only; landed ahead of the redesign, needed by Wave 4+ feature work). Migration `1748700000000_emc-roles` adds `supervisor` + `question_maker` to the `users_role_check` constraint (mirrors `1745800000000_add-organizer-role`; no profile tables — `question_maker` is keyed via `questions.writer_id` + `accesses`, `supervisor` via the `area_user`/`test_center_user` pivots). New generic `requireRole(...roles)` middleware (403s unless `req.userRole` is in the set; existing per-role middleware left untouched). `query-helpers.ts`: new `compFilter(tableOrAlias, paramIndex)` — the `comp_id` scoping fragment mirroring `liveFilter`, `comp_id` stays a bound param; the `softDelete`/`restore` whitelist hoisted to a shared `SOFT_DELETE_TABLES` const + extended with the EMC question-bank tables (`subjects`, `topics`, `subtopics`, `questions`, `answers`, `proofreads`) + `competition_flows`. New arg-aware seed scripts `create-question-maker.ts` + `create-supervisor.ts` + `db:create-question-maker` / `db:create-supervisor` npm scripts. Verified: migration applies (8 roles in the constraint), both seed users created, `tsc` clean on web + backend, admin + question_maker login regression HTTP 200. | `backend/migrations/1748700000000_emc-roles.sql`, `backend/src/middleware/require-role.ts`, `backend/src/db/query-helpers.ts`, `backend/src/db/create-{question-maker,supervisor}.ts`, `backend/package.json` |
| 21.1 | **Phase 1 — design-system foundation + framework upgrade + admin dashboard.** Adopted **Tailwind v4 + shadcn/ui** in `web/` (18 primitives via the shadcn CLI into `components/ui/`, + `recharts` + `lucide-react` + `class-variance-authority`/`clsx`/`tailwind-merge`). `globals.css` rewritten with the shadcn token system (teal primary + indigo accent, `--chart-1..5`, sidebar tokens, light + dark via the `.dark` class) — the **legacy CSS is kept transitionally** so not-yet-migrated pages still render; legacy bare-`table` rules scoped with `:not([class])` so they cannot bleed into shadcn tables; removed in the final Wave 2 phase. New shared shell in `components/shell/`: `AppShell` (collapsible sectioned sidebar + sticky top bar with theme toggle / notifications / user dropdown), `StatCard`, `PageHeader`, `ChartCard`. **`web/` upgraded Next 14 → 16 + React 18 → 19** — mandatory: current shadcn components are written for React 19's ref-as-prop model and silently drop refs on React 18, breaking every `asChild` composition (dropdowns, tooltips). `turbopack.root` pinned in `next.config.mjs` (two lockfiles confused Next's root inference). Admin `(dashboard)/layout.tsx` rebuilt on `AppShell`; `(dashboard)/dashboard/page.tsx` rebuilt (recharts 90-day area chart, KPI `StatCard`s, top-competitions panel, quick-actions grid); old `components/Sidebar.tsx` deleted. `theme/context.tsx` toggles both `.dark` (shadcn) and `data-theme` (legacy). Verified: `tsc` clean, `npm run build` succeeds (34 routes), admin dashboard + sidebar + theme toggle + logout confirmed in-browser. | `web/{components.json,postcss.config.mjs,next.config.mjs,package.json,tsconfig.json}`, `web/app/globals.css`, `web/app/layout.tsx`, `web/lib/{utils.ts,theme/context.tsx}`, `web/components/ui/*` (18 shadcn), `web/components/shell/{app-shell,stat-card,page-header,chart-card}.tsx`, `web/app/(dashboard)/{layout,dashboard/page}.tsx`, `web/hooks/use-mobile.ts` |
| 21.2 | **Phase 2a — admin portal (registrations, competitions, users) on the new design system.** All three rebuilt with shadcn `Table`/`Tabs`/`Dialog`/`Badge`/`Button`/`Input`/`Select` + the shared `PageHeader`; behavior unchanged (same API calls, approve/reject, competition CRUD, user search + pagination). Competitions create/edit form moved from an inline card into a `Dialog`. New shared `Pager` (`components/shell/pager.tsx`). sonner toasts replace the legacy flash banners; legacy `components/ui` (Spinner/Toast/Pager/PageHeader) no longer used by these pages. Verified: `tsc` clean, build passes, all three confirmed in-browser. | `web/app/(dashboard)/{registrations,competitions,users}/page.tsx`, `web/components/shell/pager.tsx` |
| 21.3 | **Phase 2b — admin schools, pending schools, segments, notifications — completes the admin portal.** All four rebuilt with shadcn `Table`/`Dialog`/`Select`/`Badge`/`Card` + the shared `PageHeader`/`Pager`. Schools: add-school form → `Dialog`. Pending Schools: reject-reason → `Dialog` (was a `window.prompt`). Segments: audience cards restyled. Notifications: two-pane composer — checkable school multi-select (search + province filter) + message composer (`Select`s, title, body, schedule) + live preview. Behavior unchanged. Verified: `tsc` clean, build passes. | `web/app/(dashboard)/{schools,schools-pending,segments,notifications}/page.tsx` |
| 21.4 | **Phase 3 (organizer portal, part 1).** `(organizer)/layout.tsx` rewritten to render the shared `AppShell` (same sidebar/topbar as admin; the bespoke orange inline sidebar removed). Rebuilt `organizer-dashboard` (KPI `StatCard`s + quick-link cards + recent-activity panel), `organizer-competitions` (shadcn table + publish/close), `participants` (competition `Select` + registrations table + approve/reject `Dialog`), `revenue` (KPI cards + per-competition share table). Behavior unchanged. The 3 competition-form pages (`organizer-competitions/{new,[id],[id]/edit}`) still render old-style content inside the new shell — pending (Phase 3 part 2). Verified: `tsc` clean, build passes, confirmed in-browser. | `web/app/(organizer)/{layout,organizer-dashboard/page,organizer-competitions/page,participants/page,revenue/page}.tsx` |
| 21.5 | **Schools directory auto-populates from student registrations** (backend — done as a side task, not part of the UI redesign). Previously `/schools` only listed manually-added schools; a student's NPSN-chosen school lived only on the `students` row. Migration `1748900000000_backfill-schools-from-students` backfills `schools` from every student who registered with an NPSN (deduped by NPSN; leading "`<npsn> - `" name-prefix junk stripped) and links those students' `school_id`. New `upsertSchoolFromNpsn()` helper (`backend/src/db/upsert-school.ts`) — student **signup** (`auth.routes.ts`) and **profile-update** (`users.routes.ts`) now upsert the chosen school into the directory, NPSN-keyed, `ON CONFLICT (npsn) DO NOTHING`. Students with no NPSN are not added (`schools.npsn` is a required unique column — decision locked with the user). Verified: migration backfilled 6 schools (1 → 7), signup-upsert confirmed end-to-end, admin login regression HTTP 200. | `backend/migrations/1748900000000_backfill-schools-from-students.sql`, `backend/src/db/upsert-school.ts`, `backend/src/routes/{auth,users}.routes.ts` |
| 21.6 | **Phase 3 (organizer portal, part 2) — organizer portal complete.** New shared `CompetitionForm` component — the New and Edit pages were ~420 near-identical lines each; now one sectioned form (Basic info / Registration & pricing / Dates / Required documents / Media & links / Descriptions) built on shadcn `Input`/`Select`/`Label`/`Badge`. `organizer-competitions/new` + `[id]/edit` are now thin wrappers around it; `[id]` detail page rebuilt (status badge, Edit/Delete actions, detail sections, media links, CSV-template upload). Behavior unchanged. Verified: `tsc` clean, build passes, confirmed in-browser. | `web/components/competition-form.tsx`, `web/app/(organizer)/organizer-competitions/{new/page,[id]/page,[id]/edit/page}.tsx` |
| 21.7 | **Phase 4 (school portal, part 1).** `(school)/layout.tsx` → shared `AppShell` with role-aware nav (school-admin: Roster / Bulk Registration / Bulk Payment / Registrations; teacher: My Students / My Competitions / Registrations / Deadlines); the `school_admin`-unverified → `/school-pending` gate and the bare unauthenticated `/school-signup` route are preserved. `school-dashboard` rebuilt — role-aware KPI `StatCard`s + quick-actions grid. The other 8 school pages still render old content inside the new shell — pending (Phase 4 part 2). Verified: `tsc` clean, build passes, confirmed in-browser. | `web/app/(school)/{layout,school-dashboard/page}.tsx` |
| 21.8 | **Phase 4 (school portal, part 2) — school portal complete.** The remaining 9 school pages rebuilt on the design system: `school-students` / `school-my-students` (roster tables), `school-registrations` (competition filter + status tabs + CSV export), `school-my-competitions` (competition cards), `school-deadline` (deadlines table), `bulk-registration` (4-step wizard — stepper + shadcn tables + progress bar), `bulk-payment` (selectable list + payment-opened/confirmed states), `school-signup` + `school-pending` (centered forms/cards). Legacy raw `fetch` + dead `localStorage` token calls swapped for `schoolHttp`. Behavior unchanged. Verified: `tsc` clean, build passes, confirmed in-browser as school-admin + teacher. | `web/app/(school)/{school-students,school-my-students,school-registrations,school-my-competitions,school-deadline,school-signup,school-pending,bulk-registration,bulk-payment}/page.tsx` |
| 21.9 | **Phase 5 (student competition portal, part 1).** The per-competition portal rebuilt on the design system: `SplitScreenAuth` + `BrandPanel` re-skinned with Tailwind (each competition keeps its own gradient brand on the left panel); `[slug]/register` (signup form on shadcn inputs + lucide icons, inline validation preserved), `[slug]/dashboard` (status / enroll card), `[slug]/admin` (registrations table + status tabs + approve/reject), both portal layouts' loading states modernized. Behavior unchanged. Verified: `tsc` clean, build passes, confirmed in-browser. | `web/components/competition-portal/{SplitScreenAuth,BrandPanel}.tsx`, `web/app/(competitions)/competitions/[slug]/{register/page,dashboard/{layout,page},admin/{layout,page}}.tsx` |
| 21.10 | **Phase 5 (auth/public pages) — Wave 2 redesign COMPLETE.** `/` unified login rebuilt (split-screen, email ⇄ phone-OTP via shadcn `Tabs`, theme toggle, hydrate skeleton — all auth logic + the load-bearing hard-nav role routing preserved); `forgot-password` + `reset-password` rebuilt on a new shared `HubAuthShell`; `privacy` + `terms` rebuilt as prose pages (Tailwind preflight had reset their headings/lists). **Legacy CSS fully stripped** from `globals.css` — it is now purely the shadcn token layer; deleted the now-unused `ThemeToggle.tsx` + `competition-portal/icons.tsx`. Every web page is on the new design system. Verified: `tsc` clean, build passes, login confirmed in-browser. | `web/app/{page,forgot-password/page,reset-password/page,privacy/page,terms/page}.tsx`, `web/components/hub-auth-shell.tsx`, `web/app/globals.css` |

### SPRINT 20 — EMC Wave 1: De-Brand + Production-Quality Auth + Slug Routes + 31-Table Multi-Tenant Schema (May 13, 2026 Session 10) ✅ COMPLETE
| Task | What | Key files |
|---|---|---|
| 20.1 | **Ship May 12–13 working tree** — unified email/password login at `/` (split-screen, role auto-route, session detection, theme toggle); per-role login pages deleted (`/login`, `/organizer-login`, `/school-login`); competition-portal scaffolding (`/emc/{register,dashboard,admin}` with generic `SplitScreenAuth` + `BrandPanel` + `CompetitionAuthProvider`); slug migration `1748000000000_add-competition-slug` (adds `competitions.slug` + seeds EMC 2026 de-branded); backend CORS opens any `http://localhost:<port>` in dev. | `web/app/page.tsx`, `web/app/(competitions)/emc/{register,dashboard,admin}/*`, `web/components/competition-portal/*`, `web/lib/auth/{factory,emc-context}.tsx`, `web/lib/competitions/emc.ts`, `backend/migrations/1748000000000_add-competition-slug.sql`, `backend/src/index.ts` |
| 20.2 | **Migration `1747600000000` hot-fix** — column-name typos: `competition_id` → `comp_id`, `p.status='settled'` → `p.payment_status='settlement'`. | `backend/migrations/1747600000000_promote-legacy-approved-registrations.sql` |
| 20.3 | **De-brand to Competzy-only** — every user-visible "Eduversal" / "Eduversal Foundation" string replaced with Competzy or competition-specific names: login page brand label + footers (4 strings), generic `BrandPanel` label + footer (2 strings), EMC wordmark (`Eduversal Mathematics Competition` → `Mathematics Competition`), Privacy + Terms boilerplate ("operated by Eduversal" → platform-neutral), mobile profile/history copy (2 strings), Achievement PDF footer + subtitle (2 strings), `schools.routes.ts` spec comment. Seed data: `organizer_name: "Eduversal Foundation"` → `"Competzy Foundation"` in `seed.ts` (3 rows), `organizerName: "Eduversal"` → `"Competzy"` in `seed-competitions-from-csv.ts`. Slug migration `1748000000000` body de-branded + idempotent `UPDATE` clauses appended so any previously-seeded local DB row gets renamed on re-run. | `web/app/page.tsx`, `web/components/competition-portal/BrandPanel.tsx`, `web/lib/competitions/emc.ts`, `web/app/{terms,privacy}/page.tsx`, `app/app/(tabs)/profile/history.tsx`, `backend/src/routes/schools.routes.ts`, `backend/src/db/{seed,seed-competitions-from-csv}.ts`, `backend/migrations/1748000000000_add-competition-slug.sql` |
| 20.4 | **Intentionally kept** (internal-only) — test fixture emails `admin@eduversal.com` / `organizer@eduversal.com` (working dev credentials); historical-import script's `Eduversal_Database.xlsx` filename ref; migration header comments mentioning Eduversal as data source; historical sprint logs in CLAUDE.md. See `feedback_brand_competzy_only.md` for the rule. | n/a |
| 20.5 | **`.gitignore`** — added `graphify-out/.rebuild.lock` so the local rebuild lock doesn't keep appearing in `git status`. | `.gitignore` |
| 20.6 | **Forgot-password flow (Phase B)** — new migration `1748050000000_password-reset-tokens` (UUID PK, FK users CASCADE, SHA-256 token_hash UNIQUE, 15-min `expires_at`, single-use `used_at`, partial live index). New endpoints `POST /api/auth/{forgot,reset}-password` — first always returns 200 (no enumeration), second requires ≥ 8 char password and invalidates all of a user's outstanding tokens on success. Both rate-limited 5/15min per IP+identifier. Email template via `sendPasswordResetEmail()` in `email.service.ts` (Competzy-branded button + plaintext fallback, no Eduversal refs). Frontend: `/forgot-password` + `/reset-password` pages match the unified split-screen design; login page's "Forgot password?" now routes to `/forgot-password` instead of the placeholder alert. | `backend/migrations/1748050000000_password-reset-tokens.sql`, `backend/src/routes/auth.routes.ts`, `backend/src/middleware/rate-limit.ts`, `backend/src/services/email.service.ts`, `backend/src/config/env.ts` (APP_URL), `web/app/{forgot-password,reset-password}/page.tsx`, `web/app/page.tsx` |
| 20.7 | **Phone OTP on web (Phase B)** — login page gains a pill Email ↔ Phone mode toggle. Phone form: E.164 input → "Send code" → 6-digit OTP → "Verify & Sign In". Reuses existing `POST /api/auth/phone/{send,verify}-otp`; same cookie issuance; same `destinationFor(role)` routing. Resend + "use a different number" controls in the OTP step. Dev OTP bypass still applies (code `000000` when `TWILIO_VERIFY_SID` unset). | `web/app/page.tsx` |
| 20.8 | **Microcopy + responsive + register polish (Phase B)** — login page: inline email validation hint, specific error microcopy on bad password ("That email and password don't match. Try again, or use Forgot password."), skeleton on `/auth/me` hydrate (replaces blank "Checking session…"). Register page: inline email + phone-format validation, password minLength bumped 6 → 8 with inline hint, duplicate-email error special-cased ("That email is already registered. Sign in instead."), submit gated on `canSubmit`. `globals.css`: new `.portal-hint`, `.hub-mode-toggle*`, `.hub-skeleton*` rules + narrow-viewport polish below 600px. | `web/app/page.tsx`, `web/app/(competitions)/[slug]/register/page.tsx`, `web/app/globals.css` |
| 20.9 | **Generalize `/emc/*` → `/competitions/[slug]/*` (Phase C)** — all 5 portal route files (register, dashboard layout+page, admin layout+page) moved from `web/app/(competitions)/emc/*` to `web/app/(competitions)/[slug]/*` with dynamic-slug routing. Pages read `useParams<{slug}>()`, look up `getCompetitionConfig(slug)` from a new slug→config registry, and call `notFound()` for unknown slugs. New `web/lib/competitions/registry.ts` exports `CompetitionPortalConfig`, `competitionRegistry` (seeded with `emc-2026`), `DEFAULT_COMPETITION_SLUG`, `getCompetitionConfig()`, `competitionPaths(slug)`. New `web/lib/auth/competition-context.tsx` exports `CompetitionAuthProvider` + `useCompetitionAuth` (replaces `EmcAuthProvider` + `useEmcAuth` — same role gate, slug-agnostic). `web/app/(competitions)/layout.tsx` mounts the new provider. `web/components/competition-portal/{SplitScreenAuth,BrandPanel}.tsx` import the type from registry now. `web/app/page.tsx` post-login routes student/parent to `competitionPaths(DEFAULT_COMPETITION_SLUG).dashboard` and links "Create a student account" to the same default slug's register page. Adding ISPO/OSEBI is now a single registry entry, no new route files. | `web/app/(competitions)/[slug]/{register/page,dashboard/{layout,page},admin/{layout,page}}.tsx`, `web/lib/competitions/registry.ts`, `web/lib/auth/competition-context.tsx`, `web/app/(competitions)/layout.tsx`, `web/components/competition-portal/{SplitScreenAuth,BrandPanel}.tsx`, `web/app/page.tsx` (also removed: `web/app/(competitions)/emc/*`, `web/lib/auth/emc-context.tsx`, `web/lib/competitions/emc.ts`) |
| 20.10 | **Wave 1 Phase D — 31 multi-tenant EMC tables across 6 migrations.** `1748100000000_emc-content` (subjects, topics, subtopics, questions, answers, question_topics pivot, proofreads — 7 tables). `1748200000000_emc-venues` (areas, test_centers — both global — plus area_user + test_center_user pivots — 4 tables). `1748300000000_emc-exam-delivery` (exams, exam_question pivot, sessions, periods, answer_keys, paper_exams, paper_answers, webcams — 8 tables). `1748400000000_emc-commerce` (voucher_groups, vouchers, products, orders, order_items — 5 tables). `1748500000000_emc-marketing` (referrals, clicks, announcements [T2 nullable comp_id], materials [T2 nullable comp_id], suggestions — 5 tables). `1748600000000_emc-config` (settings [T3 global k/v], accesses — 2 tables). All entity tables use `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, pure pivots use BIGSERIAL or composite PK. JSONB everywhere (legacy TEXT-JSON migrated). `deleted_at TIMESTAMPTZ NULL` on every non-pivot table matching the Sprint 14 soft-delete pattern. All FK columns indexed. Per-comp UNIQUE keys on questions/exams/voucher_groups/orders/referrals (`(comp_id, code)`), products (`(comp_id, slug)` AND `(comp_id, code)`), accesses (`(comp_id, user_id)`). Verified locally: 31 tables present, T1 strict comp_id NOT NULL on questions, T2 nullable on announcements, T3 absent on areas, partial-live indexes throughout. Existing login still HTTP 200. | `backend/migrations/1748{1,2,3,4,5,6}00000000_emc-*.sql` |

### SPRINT 16 — School Portal Soft Launch (May 9, 2026 Session 5) ✅ COMPLETE
| Task | What | Key files |
|---|---|---|
| 16.1 | **School signup + verification flow** — new migration `1747500000000_school-verification.sql` adds `verification_status`, `verification_letter_url`, `applied_by_user_id`, `applied_at`, `verified_at`, `verified_by_user_id`, `rejection_reason` columns. Existing rows default to `verified` to preserve seeded data. New `POST /api/schools/signup` (public) creates school + school_admin user in one transaction. New admin endpoints `GET /api/admin/schools/pending`, `POST /api/admin/schools/:id/verify`, `POST /api/admin/schools/:id/reject` (all wrapped in `audit()`). `/api/auth/me` exposes `schoolVerificationStatus` + rejection reason. | `backend/migrations/1747500000000_school-verification.sql`, `backend/src/routes/schools.routes.ts`, `backend/src/routes/admin.routes.ts`, `backend/src/routes/auth.routes.ts` |
| 16.2 | **Layout gating** — unverified `school_admin` users land on `/school-pending`; `/school-signup` is reachable unauthenticated. New web pages `/school-signup` and `/school-pending`. New admin page `/schools-pending` with verify/reject UI. Sidebar gains "Pending Schools" link. | `web/app/(school)/layout.tsx`, `web/app/(school)/school-signup/page.tsx`, `web/app/(school)/school-pending/page.tsx`, `web/app/(dashboard)/schools-pending/page.tsx`, `web/components/Sidebar.tsx` |
| 16.3 | **Achievement PDF export** — new `GET /api/schools/export/achievement.pdf` renders A4 PDF with school name + NPSN header, per-student rows from historical_participants + recent registrations, page-break safety, Eduversal/Competzy brand strip. School dashboard gains "Achievement PDF" tile. | `backend/src/routes/schools.routes.ts`, `web/app/(school)/school-dashboard/page.tsx` |
| 16.4 | **Bulk-payment payer attribution** — `/api/payments/school-batch` now passes the school's name as Midtrans `customer_name` (was the coordinator's personal name). Receipts go to coordinator email but receipt-bearing party is the school. | `backend/src/routes/payments.routes.ts` |

### SPRINT 15 — Launch 1 Polish (May 8, 2026 Session 4) ✅ COMPLETE
| Task | What | Key files |
|---|---|---|
| 15.1 | **3 missing organizer CRUD pages** — pages were already built by teammate; added missing `postPaymentRedirectUrl` field (form input + backend POST/PUT support) for the redirect-to-existing-platform flow. | `backend/src/routes/organizer.routes.ts`, `web/app/(organizer)/organizer-competitions/{new,[id]/edit}/page.tsx` |
| 15.2 | **/completeness endpoint** — `GET /api/registrations/:id/completeness` returns per-requirement boolean (profile, documents, payment, school NPSN, parent linked) + `is_ready`. Powers pre-payment gating now and pre-exam gating in Launch 2. | `backend/src/routes/registrations.routes.ts` |
| 15.3 | **Person-KID** — new migration adds `users.kid` (`KX-2026-NNNNNNN`) with sequence + backfill. UNIQUE indexed. Exposed in `/api/auth/me`. Distinct from registration_number. | `backend/migrations/1747200000000_add-person-kid.sql`, `backend/src/routes/auth.routes.ts` |
| 15.4 | **Parent-payer attribution UI** — new migration adds `payments.payer_user_id` + `payer_kind`. Backend `POST /api/payments/snap` accepts optional `payerKind` + `payerUserId`. Mobile `pay.tsx` adds 4-option "Dibayar Oleh" radio screen before Snap launches; auto-launch removed. | `backend/migrations/1747400000000_add-payer-user-id.sql`, `backend/src/routes/payments.routes.ts`, `app/services/payments.service.ts`, `app/app/(payment)/pay.tsx` |
| 15.5 | **Profile snapshot mobile display** — already implemented (line 162 `my-registration-details.tsx`). | `app/app/(tabs)/my-registration-details.tsx` |
| 15.6 | **Bulk CSV hard-match dedup** — when NISN + email both miss, `LOWER(full_name) + school + grade` exact match links to existing student instead of creating duplicate. Ambiguous (>1) matches throw. | `backend/src/services/bulk-processor.service.ts` |
| 15.7 | **Admin segments viewer** — `GET /api/admin/segments` returns 3 pre-built audiences: lapsed >1y, multi-comp veterans, EMC-only never tried KMC. New `/segments` web page. | `backend/src/routes/admin.routes.ts`, `web/app/(dashboard)/segments/page.tsx` |
| 15.8 | **Cross-comp KPI dashboard** — `GET /api/admin/kpi` returns totals (registrations, paid, free, revenue Rp), paid rate, avg time-to-payment, top-3 competitions, 90-day daily series. Admin dashboard page rewritten with stat cards + sparkline + top-3 panel. | `backend/src/routes/admin.routes.ts`, `web/app/(dashboard)/dashboard/page.tsx` |
| 15.9 | **Calendar/.ics export** — `GET /api/registrations/:id/calendar.ics` returns RFC 5545 single-event iCal with 3h default duration and proper escaping. | `backend/src/routes/registrations.routes.ts` |
| 15.10 | **Drop orphan column** — `students.parent_school_id` removed (was orphaned from earlier schema rev). | `backend/migrations/1747300000000_drop-orphan-parent-school-id.sql` |
| 15.11 | **Double-stringify audit** — confirmed `app/services/competitions.service.ts:96` was the only offender (already fixed). Other `JSON.stringify` usages all use `fetch()` directly, not `apiRequest`. | n/a |

### SPRINT 14 — Compliance & Security Hardening (May 8–9, 2026 Sessions 4–5) ✅ COMPLETE
| Task | What | Key files |
|---|---|---|
| 14.1 | **Audit log table + middleware** — new migration creates append-only `audit_log` (id, user_id, user_role snapshot, action, resource_type, resource_id, ip, user_agent, payload JSONB, created_at) with indexes. New `audit()` middleware redacts password/token/PIN/OTP from payload, fires async (never blocks request), logs only on 2xx. Wired to 16 admin/organizer/school routes. | `backend/migrations/1746900000000_audit-log.sql`, `backend/src/middleware/audit.ts`, `backend/src/routes/{admin,organizer}.routes.ts` |
| 14.2 | **Soft delete on 9 PII tables** — migration adds `deleted_at` to users, students, parents, teachers, registrations, payments, documents, historical_participants, notifications + partial "live" indexes. New `query-helpers.ts` with `liveFilter()`, `softDelete()`, `restore()` (whitelisted tables). Auth middleware now rejects soft-deleted users with 401. | `backend/migrations/1747000000000_soft-delete.sql`, `backend/src/db/query-helpers.ts`, `backend/src/middleware/auth.ts` |
| 14.3 | **Retention enforcement cron** — daily 02:00: soft-delete documents whose competition ended >1 year ago, hard-delete audit_log >5 years, hard-delete read notifications >1 year. | `backend/src/services/cron.service.ts` |
| 14.4 | **Signed URLs for documents** — new `getSignedUrl()` in storage.service: S3 presigned via `@aws-sdk/s3-request-presigner` (15-min expiry) for production, JWT-token URL via new `/uploads-signed/:token` endpoint for local dev. `/api/documents` returns signed URLs. | `backend/src/services/storage.service.ts`, `backend/src/index.ts`, `backend/src/routes/documents.routes.ts` |
| 14.5 | **httpOnly cookie auth migration** — backend installs cookie-parser, login endpoints set `competzy_token` httpOnly + SameSite=Lax + Secure-in-prod cookie via `issueAuthCookie()`. New `POST /api/auth/logout` clears it. Auth middleware reads Bearer header first then falls back to cookie. CORS opts in to credentials with explicit origin allowlist. Web client rewritten with `credentials: 'include'`; all 3 auth contexts hydrate via `/api/auth/me`. localStorage no longer used by web. **Single-session change**: a browser can't be both admin and organizer simultaneously. | `backend/src/index.ts`, `backend/src/middleware/auth.ts`, `backend/src/routes/auth.routes.ts`, `web/lib/api/client.ts`, `web/lib/auth/{context,organizer-context,school-context}.tsx` |
| 14.6 | **Rate limit bulk uploads** — new `bulkUploadLimiter` (3/hour per user) added to `/api/bulk-registration/upload`. | `backend/src/middleware/rate-limit.ts`, `backend/src/routes/bulk-registration.routes.ts` |
| 14.7 | **Webhook idempotency** — new `payment_webhook_events` table with UNIQUE(provider, order_id, signature_key). Webhook handler INSERTs ON CONFLICT DO NOTHING; if dup, returns 200 noop without re-processing. Prevents double-settlement on Midtrans retries. | `backend/migrations/1747100000000_payment-webhook-events.sql`, `backend/src/routes/payments.routes.ts` |
| 14.8 | **Privacy + Terms placeholder pages** — UU PDP-aware drafts at `/privacy` and `/terms` (DRAFT, needs counsel review). Footer link from `/` role selector. | `web/app/privacy/page.tsx`, `web/app/terms/page.tsx`, `web/app/page.tsx` |
| 14.9 | **Error handler upgrade** — respects `err.statusCode`, logs 4xx as warn (5xx as error), structured JSON response, doesn't leak internal messages on 5xx. body-parser 400s no longer pollute the error log as "Unhandled error". | `backend/src/middleware/error-handler.ts` |

### SPRINT 12 — Rebrand Kompetix → Competzy (May 8, 2026 Session 4) ✅ COMPLETE
| Fix | What | Key files |
|---|---|---|
| Display name | All UI strings, emails, push titles, splash text rebranded `Kompetix` → `Competzy` | `web/app/**`, `app/app/**`, `backend/src/services/email.service.ts` |
| App identity | Expo `name`/`slug`/`scheme` updated; deep-link scheme `kompetix://` → `competzy://`; Midtrans callbacks updated | `app/app.json`, `app/app/(payment)/pay.tsx`, `backend/src/services/midtrans.service.ts` |
| Package names | npm `name` fields rebranded across root, `app/`, `web/`, `backend/` | `package.json`, `app/package.json`, `web/package.json`, `backend/package.json` |
| GitHub URL | Updated to `github.com/codewithmujtabo/competzy` (manual repo rename still required on GitHub) | `CLAUDE.md` |
| Domain references | All `kompetix.id` / `kompetix.com` → `competzy.id` / `competzy.com` | `app/app/(tabs)/teacher-actions.tsx`, `app/app/(tabs)/web-portal-redirect.tsx`, `app/app/(auth)/register.tsx`, `docs/PROJECT_PLAN.md` |
| DB defaults | `MINIO_BUCKET` default `"kompetix"` → `"competzy"`; `SMTP_FROM` default updated; `DATABASE_URL` example updated | `backend/src/config/env.ts`, `backend/.env.example` |
| Reg-number prefix | New migration alters default to `CTZ-2026-XXXXX`. Existing `KMP-2026-*` rows untouched. | `backend/migrations/1746800000000_rebrand-registration-prefix-to-CTZ.sql` |
| On-disk folder | Disk folder renamed `kompetix/` → `competzy/` (May 8, 2026). Folder diagrams and `cd …` examples in docs updated. | n/a |

### SPRINT 11 — Bug Fixes (May 6, 2026 Session 3) ✅ COMPLETE
| Fix | What | Key files |
|---|---|---|
| Teacher DB migration | `teacher_student_links` table was missing locally → "relation does not exist" when adding student by email. Fix: `npm run db:migrate`. | `backend/migrations/1746500000000_teacher-student-links.sql` |
| Role-aware profile edit | `edit.tsx` was showing "Student Details" + all student fields for every role. Rewritten to render role-specific sections: student (full), teacher (personal + professional), parent (personal only). | `app/app/(tabs)/profile/edit.tsx` |
| Teacher profile update | Backend `PUT /api/users/me` only saved `subject` for teachers. Now also saves `school` and `department`. | `backend/src/routes/users.routes.ts` |
| Payment verify endpoint | After paying, app showed "Payment Completed!" based on redirect URL params, but DB still showed `registered` (webhook never fires to localhost in sandbox). Added `GET /api/payments/verify/:registrationId` that calls Midtrans Status API, force-updates DB if settled. | `backend/src/routes/payments.routes.ts`, `backend/src/services/midtrans.service.ts` |
| pay.tsx verify flow | App now always calls verify endpoint after browser close instead of trusting URL params. Polls up to 6× with 3s gaps. Only shows "Payment Completed!" when DB confirms `paid`. | `app/app/(payment)/pay.tsx`, `app/services/payments.service.ts` |

### SPRINT 8 — UX Fixes & Data Scoping (May 5, 2026) ✅ COMPLETE
| Task | What | Key files |
|---|---|---|
| T26 | **Historical phone login** — phone OTP matched to `historical_participants` returns `{ historicalMatch }` instead of NO_ACCOUNT; app routes to new `claim-account.tsx` screen pre-filled with name/email | `auth.routes.ts`, `app/(auth)/claim-account.tsx`, `app/(auth)/login.tsx`, `auth.service.ts` |
| T27 | **Remove payment proof upload** — Midtrans webhook auto-confirms; webhook now marks `registrations.status = 'paid'` on settlement; removed upload-proof + manual-intent + GET-proof endpoints | `payments.routes.ts`, `app/(payment)/pay.tsx`, `my-competitions.tsx` |
| T28 | **Admin registration approval flow** — all new registrations start as `pending_approval`; admin approves on web → `registered` (paid comp) or `paid` (free); student notified | `registrations.routes.ts`, `admin.routes.ts`, `web/app/(dashboard)/registrations/page.tsx`, `web/components/Sidebar.tsx`, `web/lib/api/index.ts`, `web/types/index.ts`, `my-competitions.tsx`, `AuthContext.tsx` |
| T29 | **Teacher roster scoping** — new `teacher_student_links` table; teacher adds students by email; all teacher queries (students, analytics, dashboard) now scoped to linked students only; `teacher-analytics.tsx` repurposed as "My Competitions" showing which students are registered for each competition | `1746500000000_teacher-student-links.sql`, `teachers.routes.ts`, `teachers.service.ts`, `teacher-dashboard.tsx`, `teacher-students.tsx`, `teacher-analytics.tsx`, `teacher-actions.tsx` |

### Dependency Map
```
T5 (reg_number) ──────────► T16 (mobile shows it)
T6 (profile_snapshot) ────► T17 (mobile shows it)
T7 (organizer role) ──────► T8 (organizer routes) ──► teammate can build portal
T9 (JWT redirect) ────────► T17 (mobile redirect button)
T11 (DB table) ──► T12 (import) ──► T13 (auto-link) ──► T14 (claim API) ──► T18 (mobile history)
T21 (MinIO) ──────────────► T22 (storage migration)
```

### Week-by-Week Schedule
| Week | Dates | Sprint | Key Deliverable |
|---|---|---|---|
| 1 | May 5–11 | 0 + 1 | App renamed, admin removed from mobile, reg_number, profile_snapshot, organizer role |
| 2 | May 12–18 | 2 | All 12 organizer endpoints → teammate unblocked |
| 3 | May 19–25 | 3 + 4 setup | Payment fixes, historical table + import script |
| 4 | May 26–Jun 1 | 4 complete | Auto-link, claim API, historical mobile screen |
| 5 | Jun 2–8 | 5 | Mobile polish — recommendations, reg numbers, history, teacher cleanup |
| 6 | Jun 9–15 | 6 | MinIO migration |
| 7–8 | Jun 16–30 | Integration + QA | End-to-end testing with teammate's web portals |
| Buffer | Jul 1–10 | Launch prep | App Store submission, production keys |

---

## Known Issues / Quirks

- **pnpm migration (SPRINT 38):** project switched from npm to pnpm. Per-subproject `pnpm-workspace.yaml` files hold the **`allowBuilds`** whitelist required by pnpm 11+ (it blocks native postinstall scripts by default for security). Native modules whitelisted: `bcrypt` (backend — login crashes without `bcrypt_lib.node`), `sharp` (web — Next.js image optimization falls back to slow JS path otherwise), `@sentry/cli` + `@shopify/react-native-skia` + `unrs-resolver` (mobile app). Per-subproject `.npmrc` files hold `public-hoist-pattern[]=@types/*` to flatten transitive `@types/*` packages — without that hoist, TypeScript throws TS2742 on every `const router = Router()` because the type lives under `node_modules/.pnpm/@types+express-serve-static-core@.../`. Mobile `app/.npmrc` additionally has `node-linker=hoisted` for Metro/Expo resolver compatibility. Dockerfiles enable pnpm via `corepack enable && corepack prepare pnpm@11.2.2 --activate` in both backend + web. `backend/docker-entrypoint.sh` now calls `pnpm run db:migrate` on container boot. All route files have explicit `const router: Router = Router()` annotation (pnpm's strict node_modules layout breaks TypeScript's inferred type without it).

- **`express-rate-limit` v8 trust-proxy gotcha (SPRINT 37):** the library throws `ERR_ERL_UNEXPECTED_X_FORWARDED_FOR` as a hard error when a request carries `X-Forwarded-For` but Express isn't told to trust any proxy. Production's host nginx terminates TLS and always adds the header, so every rate-limited endpoint (login / OTP / password reset / parent PIN / bulk upload) was 500-ing. Fix lives at `backend/src/index.ts` — `app.set('trust proxy', 1)` near the top, before any rate-limit middleware. Local dev never sees the header so it's a no-op there. **Don't dismiss `ERR_ERL_*` codes in container logs as "warning noise"** — at v7+ they're hard throws.
- **`students.nisn` empty-string vs partial unique index (SPRINT 37):** `idx_students_nisn` is a partial unique index defined as `UNIQUE (nisn) WHERE nisn IS NOT NULL`. Empty string is NOT NULL, so two rows with `nisn = ''` collide. The profile-update route now coerces `'' → null` (`users.routes.ts:171`), and migration `1751500000000_normalize-empty-nisn.sql` cleans existing rows. If any other write path lands on `students.nisn` (admin import, CSV bulk-reg), it needs the same coercion. Same pattern applies to any other partial-unique TEXT column — `users.email`/`users.phone` are safe (their unique index also excludes NULL but those fields are never empty by code path), but any new partial-unique TEXT column needs the same defensive code.
- **PG SQLSTATE → HTTP 4xx mapping helper (SPRINT 37):** `backend/src/lib/db-errors.ts` exports `dbErrorResponse(err, constraints?)` for translating Postgres errors to actionable 4xx responses (23505→409, 23503/23502/23514/22001/22P02→400, others null). Pattern in every catch block that wraps a `pool.query`: call `dbErrorResponse(err, { constraintName: 'friendly message' })` first; if it returns a mapped status, send that; otherwise log + 500. Wired in `auth.routes.ts` signup + `users.routes.ts` profile + `admin.routes.ts` user-update — extend to other routes that have user-facing constraint collisions (eg. duplicate slug/code on competition create).
- **Local Pathvance dev on port 3000 (developer machine `ululilmi`):** the user runs an unrelated `Pathvance` Next.js dev server on port 3000 of their Mac. Competzy backend is therefore pinned to **port 3010** in `backend/.env` (`PORT=3010`) and `web/.env.local` carries `BACKEND_URL=http://localhost:3010` so the Next.js `/api/*` rewrite points at the right place. The web dev script `next dev -H 0.0.0.0 -p 3001` pins web to **3001** explicitly so it never collides either. Production is unaffected (Coolify host port 3010 → container 3000 was already the prod mapping). When fresh-cloning on a different machine, the `backend/.env` doesn't carry over (gitignored) — start with the canonical port 3000 and shift only if needed.
- **Coolify path-based auto-deploy (SPRINT 37):** Both prod apps have **Watch Paths** set so pushes only redeploy the affected app. `competzy-backend` watches `backend/**`, `competzy-arena` watches `web/**`. **Watch Paths are evaluated relative to the repo root**, not the "Base Directory" — the UI placeholder hint (`src/pages/**`) is misleading. Don't put `**` (matches everywhere → both deploy on every push). Don't forget the trailing `/**`. The GitHub webhook fires automatically on push to main.
- **No SSH to production for the agent (auto-mode policy):** the parallel-session memory `feedback-no-ssh-prod-devops-teammate.md` keeps SSH-to-prod off-limits for Claude in this project — even when the user says "ok ssh", the auto-mode classifier enforces the boundary. The teammate (`<DEPLOY_USER>@<PROD_HOST>`) is devops. To unblock for a specific task, the user adds an explicit Bash permission rule (e.g. `Bash(ssh <DEPLOY_USER>@<PROD_HOST> docker exec * npm run db:migrate)`) or runs the command themselves. Container-internal commands worth knowing: `docker exec $(docker ps -q --filter name=<BACKEND_CONTAINER>) npm run db:migrate` (backend auto-migrate is also wired via `docker-entrypoint.sh` on container boot per PR #19, so most Redeploys handle migrations automatically).

- **Web phone login fixed (May 19, 2026 — SPRINT 34):** the web "Sign in → Phone" path rejected real accounts with "That phone isn't linked to an account." `POST /api/auth/phone/verify-otp` looked the user up with `WHERE phone = $1 OR phone = $2` (`[rawInput, toE164(rawInput)]`) — but phone numbers are persisted in **mixed formats** (the mobile app saves `08xxx`, the web sends `+62xxx`; `signup` + `PUT /users/me` store `phone` un-normalized despite the stated E.164 rule), so an app-created account whose `users.phone` is `08…` never matched a web `+62…` lookup. Fix, two parts: (1) **lookup** — new `phoneVariants()` in `twilio.service.ts` returns every plausible stored form (`+62…` / `62…` / `0…` / national); the verify-otp `users` **and** `historical_participants` lookups match `WHERE phone = ANY($variants::text[])`. (2) **storage normalisation** — new `toLocalPhone()` canonicalises any input (`+62…` / `62…` / `8…` / `08…`, with spacing/punctuation) to the **local `0`-prefixed format** (`0897654321`); `signup` and `PUT /api/users/me` normalise `users.phone` on write, and migration `1750700000000_normalize-user-phones.sql` back-fills existing rows. The migration is **collision-safe** — `users.phone` carries a unique index and some accounts hold the same number in different raw formats (`+62…` vs `0…`), which only collide once normalised; it rewrites one row per normalised value and leaves the rest of a colliding group at their current distinct string (a diagnostic query for those shared-phone pairs is in the migration file). So both `+62…` and `0…` are accepted as input platform-wide, the DB always holds `0…`, and the format-agnostic lookup still tolerates any legacy stragglers. (`toE164()` is kept for outbound Twilio calls, which require `+62…`.) The `students` contact sub-fields — `school_phone` / `supervisor_*` / `parent_*` — are **not** normalised (contact info, not identity); normalise them too if needed.
- **Mobile sign-out fixed (May 17, 2026):** the Profile "Sign Out" button previously only `router.replace`'d to `/(auth)/login` **without calling `logout()`** — the auth context kept the prior session's `user`/role, which leaked into the next login (logging in as a student right after an admin landed on the Admin Panel screen, because `competitions.tsx`'s safety-net redirect fired on the stale `admin` role and `web-portal-redirect.tsx` had no guard to bounce them back). Also a security gap — sign-out didn't clear the JWT/session. Fix: Sign Out now `await logout()` before navigating; the context `logout()` clears `user`/registrations in a `finally` (always clears, even on a server error, no longer throws); `web-portal-redirect.tsx` gained a self-healing guard that bounces a non-admin to their role home once the role resolves.
- **Mobile role-guard (May 17, 2026):** `app/app/(tabs)/_layout.tsx` carries a proactive role-guard `useEffect` — `TAB_ROLES`/`ROLE_HOME` maps keep every role on its own tabs and bounce a signed-out user to login; `(auth)/login.tsx`'s `onSuccess` now `await`s `fetchUser()` before navigating so the tabs mount with the new role, not the previous one. Ported fresh onto `main` (`fix/mobile-role-guard`) from the never-merged `fix/mobile-role-routing` branch — that branch had forked off the stack so it was reverted out of the Wave 3–12 backlog merge; `fix/mobile-signout-routing` had covered only the sign-out half.
- **Exam grade-vocabulary match (May 17, 2026 — Session 12; SUPERSEDED by SPRINT 33):** SD/SMP/SMA is gone — grades are numeric **1–12** platform-wide (migration `1750000000000_grades-numeric`), and `gradeTokens()` is now a plain `"09"→"9"` normaliser (no level mapping). `npm run db:regrade` still re-grades finished sessions after a scoring change. (Original Session-12 note, now historical:) `students.grade` is stored numerically (`'9'`), but exams reference grades by school level (the Wave-7 exam builder offers `SD`/`SMP`/`SMA`) — both in their `grades` tag *and* in the per-grade `correct_score`/`wrong_score` maps. Two symptoms: (1) the exam gate's exact-containment check meant a numeric-grade student matched no exam ("no exams yet"); (2) `scoreFor()` did a direct `correct_score['9']` key lookup → `undefined` → every correct answer scored **0 points** → `total_point` 0 despite right answers. Fix: `gradeTokens(grade)` (shared helper `services/grade.service.ts`) maps a numeric grade to its level (1–6→SD, 7–9→SMP, 10–12→SMA) and returns every token to try. The gate (`exam-session.routes.ts`) matches with `grades ?| $tokens::text[]` / on any token; `scoreFor()` (`exam-grading.service.ts`) tries each token key. Reconciles the two vocabularies platform-wide — operator-built exams included; no re-seed needed. `npm run db:regrade` (`db/regrade-sessions.ts`) re-grades finished sessions after a scoring change or a grading-logic fix.
- **Mobile signed-URL resolution (May 17, 2026 — Session 12):** `getSignedUrl()` returns a *relative* `/uploads-signed/<token>` path served from the backend ORIGIN (not the `/api` base). Mobile `WebBrowser.openBrowserAsync` / `<Image>` need an absolute `http(s)://` URL, so study-material downloads errored. Fix: `resolveFileUrl()` in `app/services/api.ts` prepends the backend origin to a relative URL; `marketing.service.ts` runs `file`/`image` through it for materials + announcements. Other mobile screens that open a signed URL (e.g. `document-vault.tsx`) should adopt the same helper if they show the symptom.
- **Exam Results review surface (May 17, 2026 — Session 12):** the Question Bank workspace's Grading page only surfaces attempts with short answers awaiting a manual mark — a fully auto-graded MC exam never appears there, so operators had no way to review how a student answered it. New **`/question-bank/results`** page lists *every* finished attempt for a competition (exam / student / grade / date / score); **`/question-bank/results/[sessionId]`** is a read-only per-attempt breakdown — each question, the student's answer, the marked key, per-question points, total score. Backed by a new `GET /api/question-bank/results?compId=&examId=`; the detail reuses the existing `GET /api/question-bank/grading/sessions/:id`. The per-grade `scoreFor()` helper is now exported from `exam-grading.service.ts` and shared — which also fixed the Grading page's "suggested points" (it had the same numeric-grade-vs-`SD/SMP/SMA` lookup bug).
- **Admin tables fit-to-width (Session 11):** the Registrations (`/registrations`) + Competitions (`/admin/competitions`) admin tables use `table-fixed` layout with sized narrow columns and `truncate`d text cells, so they no longer overflow horizontally (the Competitions Category cell — a giant multi-value string on some rows — is `max-w-full truncate`). Migration `1749200000000_backfill-school-location` filled the `schools.city`/`province` the `1748900000000` backfill left empty — derived from the most common city/province of the students linked to each school. New schools created via signup still arrive without a location — `upsertSchoolFromNpsn` doesn't capture it yet (small future enhancement).
- ~~`next.config.ts` exists in `web/` alongside `next.config.mjs`.~~ Removed.
- ~~`web/tsconfig.tsbuildinfo` is committed.~~ Untracked + `*.tsbuildinfo` gitignored.
- ~~`app/constants/api.ts` and `app/config/api.ts` both exist.~~ Legacy `constants/api.ts` deleted; `admin.service.ts` now imports from `config/api.ts` directly.
- Competition `id` column is `TEXT` (not UUID) — changed in migration `1744070500000`. IDs look like `comp_emc_2026_main`.
- ~~The `students` and `parents` tables have orphaned `parent_school_id` columns (Sprint 7 to drop).~~ Dropped on `students` in Sprint 15 migration `1747300000000`. `parents` table never had the column despite earlier comments.
- Disk folder renamed `kompetix/` → `competzy/` on May 8, 2026. Reopen any IDE workspace, terminal tab, or Claude Code session that pointed at the old path: `/Users/<you>/Desktop/All/Internship Eduversal/competzy/`.
- DB rename history: `beyond_classroom` → `kompetix` (May 6, 2026, local only) → `competzy` (May 8, 2026, local **applied ✅**). VPS still pending: `ALTER DATABASE … RENAME TO competzy;` + update `DATABASE_URL`.
- Registration number prefix rebranded `KMP-2026-XXXXX` → `CTZ-2026-XXXXX` on May 8, 2026 via migration `1746800000000_rebrand-registration-prefix-to-CTZ.sql`. Existing rows keep their `KMP-` numbers; only new rows pick up the new default.
- ~~There are 3 registrations with status `approved` in the DB (legacy status, pre-T28).~~ Migration `1747600000000_promote-legacy-approved-registrations.sql` promotes them to `paid` (free comp or settled-payment) or `registered` (paid comp, no settled payment). Run on VPS to clear them there too.
- `competitions.tsx` defaults `userRole` to `""` (not "student") when user context hasn't loaded. Guard: `useEffect` redirects teachers/admins away. Same change in `_layout.tsx`.
- `pay.tsx` polling: after browser close (any path), calls `GET /api/payments/verify/:registrationId` up to 6× with 3s gaps. The verify endpoint calls Midtrans Status API and force-updates DB — this is what makes sandbox work without a live webhook. In production the webhook arrives first and verify is a no-op.
- **Sprint 14 dual-auth quirk:** auth middleware accepts both Authorization Bearer header AND `competzy_token` cookie. This means existing localStorage-based logins (from before the cookie migration) continue to work in the wild as long as the JWT hasn't expired. Once everyone re-logins or the JWT TTL elapses (7 days), only cookies will be in use.
- **Sprint 14 retention sweep**: kicks in nightly. If you accidentally delete a doc-related `competition_date` in the past, the next 02:00 run will soft-delete those docs. Use `restore()` from `query-helpers.ts` to recover.
- **`/uploads/...` static path is still served unsigned by the backend** for backward-compat in dev. In Coolify production, file uploads go to MinIO (presigned URLs only) — the unsigned `/uploads/*` route is effectively unreachable because no public client ever stores `MINIO_*`-less files. If MinIO env vars are ever cleared on the Coolify backend service, the backend falls back to local disk + JWT-signed `/uploads-signed/<token>` URLs (still no public unsigned access).
- **Cookie auth single-session caveat**: one browser → one session. Admins who used to keep admin + organizer tabs open simultaneously must now use two browsers or two profiles.
- **Hard nav after login is load-bearing.** The per-role `AuthProvider`s (admin/organizer/school/competition) each hydrate from `/auth/me` exactly once on mount. The unified login at `/` calls `adminHttp.post('/auth/login')` directly (it accepts any role) instead of going through the role-specific provider's `login()`, so the destination provider's `user` state never updates. Use `window.location.assign(destinationFor(role))` after login, not `router.replace`. Same in `web/app/(competitions)/[slug]/register/page.tsx` post-signup. (Earlier client-side `router.replace` caused a redirect loop: `/` → `/dashboard` → bounce back to `/` because provider still saw `user=null` → page re-detected the cookie via its own `/auth/me` → looked like a refresh.)
- **`docs/PROJECT_PLAN.md` and `docs/PROJECT_PLAN.docx`** are out-of-sync with this CLAUDE.md after Sprint 14–16. Treat CLAUDE.md as the source of truth; update PROJECT_PLAN later if needed for stakeholder reporting.
- **Brand rule (Sprint 20):** platform is Competzy-only — no user-visible "Eduversal" references. Internal dev artifacts (test fixture emails `admin@eduversal.com` / `organizer@eduversal.com`, historical-import script's `Eduversal_Database.xlsx` filename ref, migration header comments) are intentionally kept. See memory `feedback_brand_competzy_only.md`. When in doubt, ask before stripping.
- **Competition portal routes are slug-keyed** at `/competitions/[slug]/{register,dashboard,admin}` (Sprint 20 Phase C). To add a new competition (ISPO, OSEBI, …): (1) add a row to `competitions` with a unique `slug`, (2) add a `CompetitionPortalConfig` entry to `web/lib/competitions/registry.ts` keyed by that slug. No new route files needed. Default student/parent post-login landing is `/competitions/emc-2026/dashboard` (the slug is `DEFAULT_COMPETITION_SLUG`); Wave 4+ replaces this with a real `/competitions` catalog page.

---

## Session Workflow

- Tasks tracked in `docs/PROJECT_PLAN.md` — update status there as work completes.
- Commit after each logical unit of work with a descriptive message.
- Before context runs out: ask Claude to summarize session and update this CLAUDE.md.
- When starting fresh: read this file + `git log --oneline -10` + `git status` to orient.
